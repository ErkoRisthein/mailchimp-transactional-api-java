openapi: 3.0.1
info:
  contact:
    email: apihelp@mailchimp.com
    name: API Support
  title: Mailchimp Transactional API
  version: 1.0.55
  x-permalink: https://github.com/mailchimp/mailchimp-client-lib-codegen/blob/main/spec/transactional.json
servers:
- url: https://mandrillapp.com/api/1.0
paths:
  /allowlists/add:
    post:
      deprecated: false
      description: "Adds an email to your email rejection allowlist. If the address\
        \ is currently on your denylist, that denylist entry will be removed automatically."
      operationId: postAllowlistsAdd
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postAllowlistsAdd_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postAllowlistsAdd_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postAllowlistsAdd_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postAllowlistsAdd_200_response'
          description: ""
      summary: Add email to allowlist
      tags:
      - allowlists
      x-custom-config:
        methodNameCamel: add
        methodNameSnake: add
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /allowlists/list:
    post:
      deprecated: false
      description: Retrieves your email rejection allowlist. You can provide an email
        address or search prefix to limit the results. Returns up to 1000 results.
      operationId: postAllowlistsList
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postAllowlistsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: up to 1000 allowlist entries
                items:
                  $ref: '#/components/schemas/postAllowlistsList_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: up to 1000 allowlist entries
                items:
                  $ref: '#/components/schemas/postAllowlistsList_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: up to 1000 allowlist entries
                items:
                  $ref: '#/components/schemas/postAllowlistsList_200_response_inner'
                type: array
          description: ""
      summary: List allowlisted emails
      tags:
      - allowlists
      x-custom-config:
        methodNameCamel: list
        methodNameSnake: list
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /allowlists/delete:
    post:
      deprecated: false
      description: Removes an email address from the allowlist.
      operationId: postAllowlistsDelete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postAllowlistsDelete_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postAllowlistsDelete_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postAllowlistsDelete_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postAllowlistsDelete_200_response'
          description: ""
      summary: Remove email from allowlist
      tags:
      - allowlists
      x-custom-config:
        methodNameCamel: delete
        methodNameSnake: delete
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /exports/info:
    post:
      deprecated: false
      description: "Returns information about an export job. If the export job's state\
        \ is 'complete', the returned data will include a URL you can use to fetch\
        \ the results. Every export job produces a zip archive, but the format of\
        \ the archive is distinct for each job type. The api calls that initiate exports\
        \ include more details about the output format for that job type."
      operationId: postExportsInfo
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsInfo_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postExportsInfo_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postExportsInfo_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postExportsInfo_200_response'
          description: ""
      summary: View export info
      tags:
      - exports
      x-custom-config:
        methodNameCamel: info
        methodNameSnake: info
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /exports/list:
    post:
      deprecated: false
      description: Returns a list of your exports.
      operationId: postExportsList
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: the account's exports
                items:
                  $ref: '#/components/schemas/postExportsList_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: the account's exports
                items:
                  $ref: '#/components/schemas/postExportsList_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: the account's exports
                items:
                  $ref: '#/components/schemas/postExportsList_200_response_inner'
                type: array
          description: ""
      summary: List exports
      tags:
      - exports
      x-custom-config:
        methodNameCamel: list
        methodNameSnake: list
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /exports/rejects:
    post:
      deprecated: false
      description: "Begins an export of your rejection denylist. The denylist will\
        \ be exported to a zip archive containing a single file named rejects.csv\
        \ that includes the following fields: email, reason, detail, created_at, expires_at,\
        \ last_event_at, expires_at."
      operationId: postExportsRejects
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsRejects_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postExportsRejects_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postExportsRejects_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postExportsRejects_200_response'
          description: ""
      summary: Export denylist
      tags:
      - exports
      x-custom-config:
        methodNameCamel: rejects
        methodNameSnake: rejects
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /exports/whitelist:
    post:
      deprecated: false
      description: "Begins an export of your rejection allowlist. The allowlist will\
        \ be exported to a zip archive containing a single file named allowlist.csv\
        \ that includes the following fields: email, detail, created_at."
      operationId: postExportsWhitelist
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsRejects_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postExportsWhitelist_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postExportsWhitelist_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postExportsWhitelist_200_response'
          description: ""
      summary: Export Allowlist
      tags:
      - exports
      x-custom-config:
        methodNameCamel: whitelist
        methodNameSnake: whitelist
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /exports/allowlist:
    post:
      deprecated: false
      description: "Begins an export of your rejection allowlist. The allowlist will\
        \ be exported to a zip archive containing a single file named allowlist.csv\
        \ that includes the following fields: email, detail, created_at."
      operationId: postExportsAllowlist
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsRejects_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postExportsWhitelist_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postExportsWhitelist_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postExportsWhitelist_200_response'
          description: ""
      summary: Export Allowlist
      tags:
      - exports
      x-custom-config:
        methodNameCamel: allowlist
        methodNameSnake: allowlist
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /exports/activity:
    post:
      deprecated: false
      description: "Begins an export of your activity history. The activity will be\
        \ exported to a zip archive containing a single file named activity.csv in\
        \ the same format as you would be able to export from your account's activity\
        \ view. It includes the following fields: Date, Email Address, Sender, Subject,\
        \ Status, Tags, Opens, Clicks, Bounce Detail. If you have configured any custom\
        \ metadata fields, they will be included in the exported data."
      operationId: postExportsActivity
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsActivity_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postExportsActivity_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postExportsActivity_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postExportsActivity_200_response'
          description: ""
      summary: Export activity history
      tags:
      - exports
      x-custom-config:
        methodNameCamel: activity
        methodNameSnake: activity
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /inbound/domains:
    post:
      deprecated: false
      description: List the domains that have been configured for inbound delivery.
      operationId: postInboundDomains
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: the inbound domains associated with the account
                items:
                  $ref: '#/components/schemas/postInboundDomains_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: the inbound domains associated with the account
                items:
                  $ref: '#/components/schemas/postInboundDomains_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: the inbound domains associated with the account
                items:
                  $ref: '#/components/schemas/postInboundDomains_200_response_inner'
                type: array
          description: ""
      summary: List inbound domains
      tags:
      - inbound
      x-custom-config:
        methodNameCamel: domains
        methodNameSnake: domains
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /inbound/add-domain:
    post:
      deprecated: false
      description: Add an inbound domain to your account.
      operationId: postInboundAddDomain
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postInboundAddDomain_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postInboundAddDomain_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postInboundAddDomain_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postInboundAddDomain_200_response'
          description: ""
      summary: Add inbound domain
      tags:
      - inbound
      x-custom-config:
        methodNameCamel: addDomain
        methodNameSnake: add_domain
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /inbound/check-domain:
    post:
      deprecated: false
      description: Check the MX settings for an inbound domain. The domain must have
        already been added with the add-domain call.
      operationId: postInboundCheckDomain
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postInboundCheckDomain_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postInboundCheckDomain_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postInboundCheckDomain_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postInboundCheckDomain_200_response'
          description: ""
      summary: Check domain settings
      tags:
      - inbound
      x-custom-config:
        methodNameCamel: checkDomain
        methodNameSnake: check_domain
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /inbound/delete-domain:
    post:
      deprecated: false
      description: Delete an inbound domain from the account. All mail will stop routing
        for this domain immediately.
      operationId: postInboundDeleteDomain
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postInboundCheckDomain_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postInboundDeleteDomain_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postInboundDeleteDomain_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postInboundDeleteDomain_200_response'
          description: ""
      summary: Delete inbound domain
      tags:
      - inbound
      x-custom-config:
        methodNameCamel: deleteDomain
        methodNameSnake: delete_domain
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /inbound/routes:
    post:
      deprecated: false
      description: List the mailbox routes defined for an inbound domain.
      operationId: postInboundRoutes
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postInboundRoutes_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: the routes associated with the domain
                items:
                  $ref: '#/components/schemas/postInboundRoutes_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: the routes associated with the domain
                items:
                  $ref: '#/components/schemas/postInboundRoutes_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: the routes associated with the domain
                items:
                  $ref: '#/components/schemas/postInboundRoutes_200_response_inner'
                type: array
          description: ""
      summary: List mailbox routes
      tags:
      - inbound
      x-custom-config:
        methodNameCamel: routes
        methodNameSnake: routes
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /inbound/add-route:
    post:
      deprecated: false
      description: Add a new mailbox route to an inbound domain.
      operationId: postInboundAddRoute
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postInboundAddRoute_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postInboundAddRoute_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postInboundAddRoute_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postInboundAddRoute_200_response'
          description: ""
      summary: Add mailbox route
      tags:
      - inbound
      x-custom-config:
        methodNameCamel: addRoute
        methodNameSnake: add_route
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /inbound/update-route:
    post:
      deprecated: false
      description: "Update the pattern or webhook of an existing inbound mailbox route.\
        \ If null is provided for any fields, the values will remain unchanged."
      operationId: postInboundUpdateRoute
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postInboundUpdateRoute_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postInboundUpdateRoute_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postInboundUpdateRoute_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postInboundUpdateRoute_200_response'
          description: ""
      summary: Update mailbox route
      tags:
      - inbound
      x-custom-config:
        methodNameCamel: updateRoute
        methodNameSnake: update_route
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /inbound/delete-route:
    post:
      deprecated: false
      description: Delete an existing inbound mailbox route.
      operationId: postInboundDeleteRoute
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postInboundDeleteRoute_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postInboundDeleteRoute_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postInboundDeleteRoute_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postInboundDeleteRoute_200_response'
          description: ""
      summary: Delete mailbox route
      tags:
      - inbound
      x-custom-config:
        methodNameCamel: deleteRoute
        methodNameSnake: delete_route
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /inbound/send-raw:
    post:
      deprecated: false
      description: "Take a raw MIME document destined for a domain with inbound domains\
        \ set up, and send it to the inbound hook exactly as if it had been sent over\
        \ SMTP."
      operationId: postInboundSendRaw
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postInboundSendRaw_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: an array of the information for each recipient in the
                  message (usually one) that matched an inbound route
                items:
                  $ref: '#/components/schemas/postInboundSendRaw_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: an array of the information for each recipient in the
                  message (usually one) that matched an inbound route
                items:
                  $ref: '#/components/schemas/postInboundSendRaw_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: an array of the information for each recipient in the
                  message (usually one) that matched an inbound route
                items:
                  $ref: '#/components/schemas/postInboundSendRaw_200_response_inner'
                type: array
          description: ""
      summary: Send mime document
      tags:
      - inbound
      x-custom-config:
        methodNameCamel: sendRaw
        methodNameSnake: send_raw
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /ips/list:
    post:
      deprecated: false
      description: Lists your dedicated IPs.
      operationId: postIpsList
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: an array of objects for each dedicated IP
                items:
                  $ref: '#/components/schemas/postIpsList_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: an array of objects for each dedicated IP
                items:
                  $ref: '#/components/schemas/postIpsList_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: an array of objects for each dedicated IP
                items:
                  $ref: '#/components/schemas/postIpsList_200_response_inner'
                type: array
          description: ""
      summary: List ip addresses
      tags:
      - ips
      x-custom-config:
        methodNameCamel: list
        methodNameSnake: list
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /ips/info:
    post:
      deprecated: false
      description: Retrieves information about a single dedicated IP.
      operationId: postIpsInfo
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postIpsInfo_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postIpsInfo_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postIpsInfo_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postIpsInfo_200_response'
          description: ""
      summary: Get ip info
      tags:
      - ips
      x-custom-config:
        methodNameCamel: info
        methodNameSnake: info
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /ips/provision:
    post:
      deprecated: false
      description: "Requests an additional dedicated IP for your account. Accounts\
        \ may have one outstanding request at any time, and provisioning requests\
        \ are processed within 24 hours."
      operationId: postIpsProvision
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postIpsProvision_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postIpsProvision_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postIpsProvision_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postIpsProvision_200_response'
          description: ""
      summary: Request additional ip
      tags:
      - ips
      x-custom-config:
        methodNameCamel: provision
        methodNameSnake: provision
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /ips/start-warmup:
    post:
      deprecated: false
      description: "Begins the warmup process for a dedicated IP. During the warmup\
        \ process, the Transactional API will gradually increase the percentage of\
        \ your mail that is sent over the warming-up IP, over a period of roughly\
        \ 30 days. The rest of your mail will be sent over shared IPs or other dedicated\
        \ IPs in the same pool."
      operationId: postIpsStartWarmup
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postIpsStartWarmup_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postIpsStartWarmup_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postIpsStartWarmup_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postIpsStartWarmup_200_response'
          description: ""
      summary: Start ip warmup
      tags:
      - ips
      x-custom-config:
        methodNameCamel: startWarmup
        methodNameSnake: start_warmup
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /ips/cancel-warmup:
    post:
      deprecated: false
      description: Cancels the warmup process for a dedicated IP.
      operationId: postIpsCancelWarmup
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postIpsStartWarmup_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postIpsStartWarmup_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postIpsStartWarmup_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postIpsStartWarmup_200_response'
          description: ""
      summary: Cancel ip warmup
      tags:
      - ips
      x-custom-config:
        methodNameCamel: cancelWarmup
        methodNameSnake: cancel_warmup
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /ips/set-pool:
    post:
      deprecated: false
      description: Moves a dedicated IP to a different pool.
      operationId: postIpsSetPool
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postIpsSetPool_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postIpsSetPool_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postIpsSetPool_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postIpsSetPool_200_response'
          description: ""
      summary: Move ip to different pool
      tags:
      - ips
      x-custom-config:
        methodNameCamel: setPool
        methodNameSnake: set_pool
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /ips/delete:
    post:
      deprecated: false
      description: Deletes a dedicated IP. This is permanent and cannot be undone.
      operationId: postIpsDelete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postIpsDelete_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postIpsDelete_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postIpsDelete_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postIpsDelete_200_response'
          description: ""
      summary: Delete ip address
      tags:
      - ips
      x-custom-config:
        methodNameCamel: delete
        methodNameSnake: delete
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /ips/list-pools:
    post:
      deprecated: false
      description: Lists your dedicated IP pools.
      operationId: postIpsListPools
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: "the dedicated IP pools for your account, up to a maximum\
                  \ of 1,000"
                items:
                  $ref: '#/components/schemas/postIpsListPools_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: "the dedicated IP pools for your account, up to a maximum\
                  \ of 1,000"
                items:
                  $ref: '#/components/schemas/postIpsListPools_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: "the dedicated IP pools for your account, up to a maximum\
                  \ of 1,000"
                items:
                  $ref: '#/components/schemas/postIpsListPools_200_response_inner'
                type: array
          description: ""
      summary: List ip pools
      tags:
      - ips
      x-custom-config:
        methodNameCamel: listPools
        methodNameSnake: list_pools
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /ips/pool-info:
    post:
      deprecated: false
      description: Describes a single dedicated IP pool.
      operationId: postIpsPoolInfo
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postIpsPoolInfo_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postIpsPoolInfo_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postIpsPoolInfo_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postIpsPoolInfo_200_response'
          description: ""
      summary: Get ip pool info
      tags:
      - ips
      x-custom-config:
        methodNameCamel: poolInfo
        methodNameSnake: pool_info
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /ips/create-pool:
    post:
      deprecated: false
      description: "Creates a pool and returns it. If a pool already exists with this\
        \ name, no action will be performed."
      operationId: postIpsCreatePool
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postIpsCreatePool_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postIpsPoolInfo_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postIpsPoolInfo_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postIpsPoolInfo_200_response'
          description: ""
      summary: Add ip pool
      tags:
      - ips
      x-custom-config:
        methodNameCamel: createPool
        methodNameSnake: create_pool
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /ips/delete-pool:
    post:
      deprecated: false
      description: "Deletes a pool. A pool must be empty before you can delete it,\
        \ and you cannot delete your default pool."
      operationId: postIpsDeletePool
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postIpsDeletePool_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postIpsDeletePool_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postIpsDeletePool_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postIpsDeletePool_200_response'
          description: ""
      summary: Delete ip pool
      tags:
      - ips
      x-custom-config:
        methodNameCamel: deletePool
        methodNameSnake: delete_pool
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /ips/check-custom-dns:
    post:
      deprecated: false
      description: Tests whether a domain name is valid for use as the custom reverse
        DNS for a dedicated IP.
      operationId: postIpsCheckCustomDns
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postIpsCheckCustomDns_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postIpsCheckCustomDns_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postIpsCheckCustomDns_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postIpsCheckCustomDns_200_response'
          description: ""
      summary: Test custom dns
      tags:
      - ips
      x-custom-config:
        methodNameCamel: checkCustomDns
        methodNameSnake: check_custom_dns
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /ips/set-custom-dns:
    post:
      deprecated: false
      description: Configures the custom DNS name for a dedicated IP.
      operationId: postIpsSetCustomDns
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postIpsSetCustomDns_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postIpsSetCustomDns_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postIpsSetCustomDns_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postIpsSetCustomDns_200_response'
          description: ""
      summary: Set custom dns
      tags:
      - ips
      x-custom-config:
        methodNameCamel: setCustomDns
        methodNameSnake: set_custom_dns
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /messages/send:
    post:
      deprecated: false
      description: Send a new transactional message through the Transactional API.
      operationId: postMessagesSend
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postMessagesSend_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: "an array of objects for each recipient containing the\
                  \ key \"email\" with the email address, and details of the message\
                  \ status for that recipient"
                items:
                  $ref: '#/components/schemas/postMessagesSend_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: "an array of objects for each recipient containing the\
                  \ key \"email\" with the email address, and details of the message\
                  \ status for that recipient"
                items:
                  $ref: '#/components/schemas/postMessagesSend_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: "an array of objects for each recipient containing the\
                  \ key \"email\" with the email address, and details of the message\
                  \ status for that recipient"
                items:
                  $ref: '#/components/schemas/postMessagesSend_200_response_inner'
                type: array
          description: ""
      summary: Send new message
      tags:
      - messages
      x-custom-config:
        methodNameCamel: send
        methodNameSnake: send
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /messages/send-template:
    post:
      deprecated: false
      description: Send a new transactional message through the Transactional API
        using a template.
      operationId: postMessagesSendTemplate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postMessagesSendTemplate_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: "an array of objects for each recipient containing the\
                  \ key \"email\" with the email address, and details of the message\
                  \ status for that recipient"
                items:
                  $ref: '#/components/schemas/postMessagesSendTemplate_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: "an array of objects for each recipient containing the\
                  \ key \"email\" with the email address, and details of the message\
                  \ status for that recipient"
                items:
                  $ref: '#/components/schemas/postMessagesSendTemplate_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: "an array of objects for each recipient containing the\
                  \ key \"email\" with the email address, and details of the message\
                  \ status for that recipient"
                items:
                  $ref: '#/components/schemas/postMessagesSendTemplate_200_response_inner'
                type: array
          description: ""
      summary: Send using message template
      tags:
      - messages
      x-custom-config:
        methodNameCamel: sendTemplate
        methodNameSnake: send_template
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /messages/search:
    post:
      deprecated: false
      description: "Search recently sent messages and optionally narrow by date range,\
        \ tags, senders, and API keys. If no date range is specified, results within\
        \ the last 7 days are returned. This method may be called up to 20 times per\
        \ minute. If you need the data more often, you can use /messages/info.json\
        \ to get the information for a single message, or webhooks to push activity\
        \ to your own application for querying."
      operationId: postMessagesSearch
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postMessagesSearch_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: an array of objects for each matching message
                items:
                  $ref: '#/components/schemas/postMessagesSearch_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: an array of objects for each matching message
                items:
                  $ref: '#/components/schemas/postMessagesSearch_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: an array of objects for each matching message
                items:
                  $ref: '#/components/schemas/postMessagesSearch_200_response_inner'
                type: array
          description: ""
      summary: Search messages by date
      tags:
      - messages
      x-custom-config:
        methodNameCamel: search
        methodNameSnake: search
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /messages/search-time-series:
    post:
      deprecated: false
      description: Search the content of recently sent messages and return the aggregated
        hourly stats for matching messages.
      operationId: postMessagesSearchTimeSeries
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postMessagesSearchTimeSeries_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postMessagesSearchTimeSeries_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postMessagesSearchTimeSeries_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postMessagesSearchTimeSeries_200_response_inner'
                type: array
          description: ""
      summary: Search messages by hour
      tags:
      - messages
      x-custom-config:
        methodNameCamel: searchTimeSeries
        methodNameSnake: search_time_series
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /messages/info:
    post:
      deprecated: false
      description: Get the information for a single recently sent message.
      operationId: postMessagesInfo
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postMessagesInfo_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postMessagesInfo_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postMessagesInfo_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postMessagesInfo_200_response'
          description: ""
      summary: Get message info
      tags:
      - messages
      x-custom-config:
        methodNameCamel: info
        methodNameSnake: info
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /messages/content:
    post:
      deprecated: false
      description: Get the full content of a recently sent message.
      operationId: postMessagesContent
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postMessagesInfo_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postMessagesContent_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postMessagesContent_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postMessagesContent_200_response'
          description: ""
      summary: Get message content
      tags:
      - messages
      x-custom-config:
        methodNameCamel: content
        methodNameSnake: content
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /messages/parse:
    post:
      deprecated: false
      description: "Parse the full MIME document for an email message, returning the\
        \ content of the message broken into its constituent pieces."
      operationId: postMessagesParse
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postMessagesParse_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postMessagesParse_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postMessagesParse_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postMessagesParse_200_response'
          description: ""
      summary: Parse mime document
      tags:
      - messages
      x-custom-config:
        methodNameCamel: parse
        methodNameSnake: parse
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /messages/send-raw:
    post:
      deprecated: false
      description: "Take a raw MIME document for a message, and send it exactly as\
        \ if it were sent through the Transactional API's SMTP servers."
      operationId: postMessagesSendRaw
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postMessagesSendRaw_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: ""
                type: object
            application/x-php:
              schema:
                description: ""
                type: object
            application/x-yaml; charset=utf-8:
              schema:
                description: ""
                type: object
          description: ""
      summary: Send mime document
      tags:
      - messages
      x-custom-config:
        methodNameCamel: sendRaw
        methodNameSnake: send_raw
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /messages/list-scheduled:
    post:
      deprecated: false
      description: Queries your scheduled emails.
      operationId: postMessagesListScheduled
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postMessagesListScheduled_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: a list of up to 1000 scheduled emails
                items:
                  $ref: '#/components/schemas/postMessagesListScheduled_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: a list of up to 1000 scheduled emails
                items:
                  $ref: '#/components/schemas/postMessagesListScheduled_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: a list of up to 1000 scheduled emails
                items:
                  $ref: '#/components/schemas/postMessagesListScheduled_200_response_inner'
                type: array
          description: ""
      summary: List scheduled emails
      tags:
      - messages
      x-custom-config:
        methodNameCamel: listScheduled
        methodNameSnake: list_scheduled
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /messages/cancel-scheduled:
    post:
      deprecated: false
      description: Cancels a scheduled email.
      operationId: postMessagesCancelScheduled
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postMessagesCancelScheduled_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: information about the scheduled email that was cancelled.
                items:
                  $ref: '#/components/schemas/postMessagesListScheduled_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: information about the scheduled email that was cancelled.
                items:
                  $ref: '#/components/schemas/postMessagesListScheduled_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: information about the scheduled email that was cancelled.
                items:
                  $ref: '#/components/schemas/postMessagesListScheduled_200_response_inner'
                type: array
          description: ""
      summary: Cancel scheduled email
      tags:
      - messages
      x-custom-config:
        methodNameCamel: cancelScheduled
        methodNameSnake: cancel_scheduled
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /messages/reschedule:
    post:
      deprecated: false
      description: Reschedules a scheduled email.
      operationId: postMessagesReschedule
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postMessagesReschedule_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: information about the scheduled email that was rescheduled.
                items:
                  $ref: '#/components/schemas/postMessagesListScheduled_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: information about the scheduled email that was rescheduled.
                items:
                  $ref: '#/components/schemas/postMessagesListScheduled_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: information about the scheduled email that was rescheduled.
                items:
                  $ref: '#/components/schemas/postMessagesListScheduled_200_response_inner'
                type: array
          description: ""
      summary: Reschedule email
      tags:
      - messages
      x-custom-config:
        methodNameCamel: reschedule
        methodNameSnake: reschedule
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /metadata/list:
    post:
      deprecated: false
      description: Get the list of custom metadata fields indexed for the account.
      operationId: postMetadataList
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: the custom metadata fields for the account
                items:
                  $ref: '#/components/schemas/postMetadataList_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: the custom metadata fields for the account
                items:
                  $ref: '#/components/schemas/postMetadataList_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: the custom metadata fields for the account
                items:
                  $ref: '#/components/schemas/postMetadataList_200_response_inner'
                type: array
          description: ""
      summary: List metadata fields
      tags:
      - metadata
      x-custom-config:
        methodNameCamel: list
        methodNameSnake: list
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /metadata/add:
    post:
      deprecated: false
      description: Add a new custom metadata field to be indexed for the account.
      operationId: postMetadataAdd
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postMetadataAdd_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postMetadataAdd_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postMetadataAdd_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postMetadataAdd_200_response'
          description: ""
      summary: Add metadata field
      tags:
      - metadata
      x-custom-config:
        methodNameCamel: add
        methodNameSnake: add
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /metadata/update:
    post:
      deprecated: false
      description: Update an existing custom metadata field.
      operationId: postMetadataUpdate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postMetadataUpdate_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postMetadataUpdate_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postMetadataUpdate_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postMetadataUpdate_200_response'
          description: ""
      summary: Update metadata field
      tags:
      - metadata
      x-custom-config:
        methodNameCamel: update
        methodNameSnake: update
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /metadata/delete:
    post:
      deprecated: false
      description: "Delete an existing custom metadata field. Deletion isn't instataneous,\
        \ and /metadata/list will continue to return the field until the asynchronous\
        \ deletion process is complete."
      operationId: postMetadataDelete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postMetadataDelete_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postMetadataDelete_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postMetadataDelete_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postMetadataDelete_200_response'
          description: ""
      summary: Delete metadata field
      tags:
      - metadata
      x-custom-config:
        methodNameCamel: delete
        methodNameSnake: delete
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /rejects/add:
    post:
      deprecated: false
      description: Adds an email to your email rejection denylist. Addresses that
        you add manually will never expire and there is no reputation penalty for
        removing them from your denylist. Attempting to denylist an address that has
        been added to the allowlist will have no effect.
      operationId: postRejectsAdd
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postRejectsAdd_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postRejectsAdd_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postRejectsAdd_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postRejectsAdd_200_response'
          description: ""
      summary: Add email to denylist
      tags:
      - rejects
      x-custom-config:
        methodNameCamel: add
        methodNameSnake: add
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /rejects/list:
    post:
      deprecated: false
      description: "Retrieves your email rejection denylist. You can provide an email\
        \ address to limit the results. Returns up to 1000 results. By default, entries\
        \ that have expired are excluded from the results; set include_expired to\
        \ true to include them."
      operationId: postRejectsList
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postRejectsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: Up to 1000 rejection entries
                items:
                  $ref: '#/components/schemas/postRejectsList_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: Up to 1000 rejection entries
                items:
                  $ref: '#/components/schemas/postRejectsList_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: Up to 1000 rejection entries
                items:
                  $ref: '#/components/schemas/postRejectsList_200_response_inner'
                type: array
          description: ""
      summary: List denylisted emails
      tags:
      - rejects
      x-custom-config:
        methodNameCamel: list
        methodNameSnake: list
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /rejects/delete:
    post:
      deprecated: false
      description: "Deletes an email rejection. There is no limit to how many rejections\
        \ you can remove from your denylist, but keep in mind that each deletion has\
        \ an affect on your reputation."
      operationId: postRejectsDelete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postRejectsDelete_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postRejectsDelete_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postRejectsDelete_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postRejectsDelete_200_response'
          description: ""
      summary: Delete email from denylist
      tags:
      - rejects
      x-custom-config:
        methodNameCamel: delete
        methodNameSnake: delete
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /senders/list:
    post:
      deprecated: false
      description: Return the senders that have tried to use this account.
      operationId: postSendersList
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: "an array of sender data, one for each sending addresses\
                  \ used by the account"
                items:
                  $ref: '#/components/schemas/postSendersList_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: "an array of sender data, one for each sending addresses\
                  \ used by the account"
                items:
                  $ref: '#/components/schemas/postSendersList_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: "an array of sender data, one for each sending addresses\
                  \ used by the account"
                items:
                  $ref: '#/components/schemas/postSendersList_200_response_inner'
                type: array
          description: ""
      summary: List account senders
      tags:
      - senders
      x-custom-config:
        methodNameCamel: list
        methodNameSnake: list
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /senders/domains:
    post:
      deprecated: false
      description: Returns the sender domains that have been added to this account.
      operationId: postSendersDomains
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: "an array of sender domain data, one for each sending\
                  \ domain used by the account"
                items:
                  $ref: '#/components/schemas/postSendersDomains_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: "an array of sender domain data, one for each sending\
                  \ domain used by the account"
                items:
                  $ref: '#/components/schemas/postSendersDomains_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: "an array of sender domain data, one for each sending\
                  \ domain used by the account"
                items:
                  $ref: '#/components/schemas/postSendersDomains_200_response_inner'
                type: array
          description: ""
      summary: List sender domains
      tags:
      - senders
      x-custom-config:
        methodNameCamel: domains
        methodNameSnake: domains
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /senders/add-domain:
    post:
      deprecated: false
      description: "Adds a sender domain to your account. Sender domains are added\
        \ automatically as you send, but you can use this call to add them ahead of\
        \ time."
      operationId: postSendersAddDomain
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postSendersAddDomain_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postSendersAddDomain_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postSendersAddDomain_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postSendersAddDomain_200_response'
          description: ""
      summary: Add sender domain
      tags:
      - senders
      x-custom-config:
        methodNameCamel: addDomain
        methodNameSnake: add_domain
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /senders/check-domain:
    post:
      deprecated: false
      description: "Checks the SPF and DKIM settings for a domain, as well the domain\
        \ verification. If you haven't already added this domain to your account,\
        \ it will be added automatically."
      operationId: postSendersCheckDomain
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postSendersAddDomain_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postSendersCheckDomain_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postSendersCheckDomain_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postSendersCheckDomain_200_response'
          description: ""
      summary: Check domain settings
      tags:
      - senders
      x-custom-config:
        methodNameCamel: checkDomain
        methodNameSnake: check_domain
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /senders/verify-domain:
    post:
      deprecated: false
      description: "Sends a verification email in order to verify ownership of a domain.\
        \ Domain verification is a required step to confirm ownership of a domain.\
        \ Once a domain has been verified in a Transactional API account, other accounts\
        \ may not have their messages signed by that domain unless they also verify\
        \ the domain. This prevents other Transactional API accounts from sending\
        \ mail signed by your domain."
      operationId: postSendersVerifyDomain
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postSendersVerifyDomain_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postSendersVerifyDomain_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postSendersVerifyDomain_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postSendersVerifyDomain_200_response'
          description: ""
      summary: Verify domain
      tags:
      - senders
      x-custom-config:
        methodNameCamel: verifyDomain
        methodNameSnake: verify_domain
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /senders/info:
    post:
      deprecated: false
      description: "Return more detailed information about a single sender, including\
        \ aggregates of recent stats."
      operationId: postSendersInfo
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postSendersInfo_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postSendersInfo_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postSendersInfo_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postSendersInfo_200_response'
          description: ""
      summary: Get sender info
      tags:
      - senders
      x-custom-config:
        methodNameCamel: info
        methodNameSnake: info
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /senders/time-series:
    post:
      deprecated: false
      description: Return the recent history (hourly stats for the last 30 days) for
        a sender.
      operationId: postSendersTimeSeries
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postSendersInfo_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postSendersTimeSeries_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postSendersTimeSeries_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postSendersTimeSeries_200_response_inner'
                type: array
          description: ""
      summary: View sender history
      tags:
      - senders
      x-custom-config:
        methodNameCamel: timeSeries
        methodNameSnake: time_series
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /subaccounts/list:
    post:
      deprecated: false
      description: "Get the list of subaccounts defined for the account, optionally\
        \ filtered by a prefix."
      operationId: postSubaccountsList
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postSubaccountsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: "the subaccounts for the account, up to a maximum of\
                  \ 1,000"
                items:
                  $ref: '#/components/schemas/postSubaccountsList_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: "the subaccounts for the account, up to a maximum of\
                  \ 1,000"
                items:
                  $ref: '#/components/schemas/postSubaccountsList_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: "the subaccounts for the account, up to a maximum of\
                  \ 1,000"
                items:
                  $ref: '#/components/schemas/postSubaccountsList_200_response_inner'
                type: array
          description: ""
      summary: List subaccounts
      tags:
      - subaccounts
      x-custom-config:
        methodNameCamel: list
        methodNameSnake: list
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /subaccounts/add:
    post:
      deprecated: false
      description: Add a new subaccount.
      operationId: postSubaccountsAdd
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postSubaccountsAdd_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postSubaccountsAdd_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postSubaccountsAdd_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postSubaccountsAdd_200_response'
          description: ""
      summary: Add subaccount
      tags:
      - subaccounts
      x-custom-config:
        methodNameCamel: add
        methodNameSnake: add
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /subaccounts/info:
    post:
      deprecated: false
      description: "Given the ID of an existing subaccount, return the data about\
        \ it."
      operationId: postSubaccountsInfo
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postSubaccountsInfo_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postSubaccountsInfo_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postSubaccountsInfo_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postSubaccountsInfo_200_response'
          description: ""
      summary: Get subaccount info
      tags:
      - subaccounts
      x-custom-config:
        methodNameCamel: info
        methodNameSnake: info
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /subaccounts/update:
    post:
      deprecated: false
      description: Update an existing subaccount.
      operationId: postSubaccountsUpdate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postSubaccountsUpdate_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postSubaccountsUpdate_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postSubaccountsUpdate_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postSubaccountsUpdate_200_response'
          description: ""
      summary: Update subaccount
      tags:
      - subaccounts
      x-custom-config:
        methodNameCamel: update
        methodNameSnake: update
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /subaccounts/delete:
    post:
      deprecated: false
      description: "Delete an existing subaccount. Any email related to the subaccount\
        \ will be saved, but stats will be removed and any future sending calls to\
        \ this subaccount will fail."
      operationId: postSubaccountsDelete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postSubaccountsDelete_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postSubaccountsDelete_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postSubaccountsDelete_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postSubaccountsDelete_200_response'
          description: ""
      summary: Delete subaccount
      tags:
      - subaccounts
      x-custom-config:
        methodNameCamel: delete
        methodNameSnake: delete
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /subaccounts/pause:
    post:
      deprecated: false
      description: Pause a subaccount's sending. Any future emails delivered to this
        subaccount will be queued for a maximum of 3 days until the subaccount is
        resumed.
      operationId: postSubaccountsPause
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postSubaccountsPause_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postSubaccountsPause_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postSubaccountsPause_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postSubaccountsPause_200_response'
          description: ""
      summary: Pause subaccount
      tags:
      - subaccounts
      x-custom-config:
        methodNameCamel: pause
        methodNameSnake: pause
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /subaccounts/resume:
    post:
      deprecated: false
      description: Resume a paused subaccount's sending.
      operationId: postSubaccountsResume
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postSubaccountsResume_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postSubaccountsResume_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postSubaccountsResume_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postSubaccountsResume_200_response'
          description: ""
      summary: Resume subaccount
      tags:
      - subaccounts
      x-custom-config:
        methodNameCamel: resume
        methodNameSnake: resume
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /tags/list:
    post:
      deprecated: false
      description: Return all of the user-defined tag information.
      operationId: postTagsList
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: a list of user-defined tags
                items:
                  $ref: '#/components/schemas/postTagsList_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: a list of user-defined tags
                items:
                  $ref: '#/components/schemas/postTagsList_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: a list of user-defined tags
                items:
                  $ref: '#/components/schemas/postTagsList_200_response_inner'
                type: array
          description: ""
      summary: List tags
      tags:
      - tags
      x-custom-config:
        methodNameCamel: list
        methodNameSnake: list
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /tags/delete:
    post:
      deprecated: false
      description: "Deletes a tag permanently. Deleting a tag removes the tag from\
        \ any messages that have been sent, and also deletes the tag's stats. There\
        \ is no way to undo this operation, so use it carefully."
      operationId: postTagsDelete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postTagsDelete_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postTagsDelete_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postTagsDelete_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postTagsDelete_200_response'
          description: ""
      summary: Delete tag
      tags:
      - tags
      x-custom-config:
        methodNameCamel: delete
        methodNameSnake: delete
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /tags/info:
    post:
      deprecated: false
      description: "Return more detailed information about a single tag, including\
        \ aggregates of recent stats."
      operationId: postTagsInfo
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postTagsInfo_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postTagsInfo_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postTagsInfo_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postTagsInfo_200_response'
          description: ""
      summary: Get tag info
      tags:
      - tags
      x-custom-config:
        methodNameCamel: info
        methodNameSnake: info
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /tags/time-series:
    post:
      deprecated: false
      description: Return the recent history (hourly stats for the last 30 days) for
        a tag.
      operationId: postTagsTimeSeries
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postTagsInfo_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postMessagesSearchTimeSeries_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postMessagesSearchTimeSeries_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postMessagesSearchTimeSeries_200_response_inner'
                type: array
          description: ""
      summary: View tag history
      tags:
      - tags
      x-custom-config:
        methodNameCamel: timeSeries
        methodNameSnake: time_series
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /tags/all-time-series:
    post:
      deprecated: false
      description: Return the recent history (hourly stats for the last 30 days) for
        all tags.
      operationId: postTagsAllTimeSeries
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postMessagesSearchTimeSeries_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postMessagesSearchTimeSeries_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postMessagesSearchTimeSeries_200_response_inner'
                type: array
          description: ""
      summary: View all tags history
      tags:
      - tags
      x-custom-config:
        methodNameCamel: allTimeSeries
        methodNameSnake: all_time_series
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /templates/add:
    post:
      deprecated: false
      description: Add a new template.
      operationId: postTemplatesAdd
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postTemplatesAdd_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postTemplatesAdd_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postTemplatesAdd_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postTemplatesAdd_200_response'
          description: ""
      summary: Add template
      tags:
      - templates
      x-custom-config:
        methodNameCamel: add
        methodNameSnake: add
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /templates/info:
    post:
      deprecated: false
      description: Get the information for an existing template.
      operationId: postTemplatesInfo
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postTemplatesInfo_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postTemplatesInfo_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postTemplatesInfo_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postTemplatesInfo_200_response'
          description: ""
      summary: Get template info
      tags:
      - templates
      x-custom-config:
        methodNameCamel: info
        methodNameSnake: info
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /templates/update:
    post:
      deprecated: false
      description: "Update the code for an existing template. If null is provided\
        \ for any fields, the values will remain unchanged."
      operationId: postTemplatesUpdate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postTemplatesUpdate_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postTemplatesUpdate_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postTemplatesUpdate_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postTemplatesUpdate_200_response'
          description: ""
      summary: Update template
      tags:
      - templates
      x-custom-config:
        methodNameCamel: update
        methodNameSnake: update
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /templates/publish:
    post:
      deprecated: false
      description: Publish the content for the template. Any new messages sent using
        this template will start using the content that was previously in draft.
      operationId: postTemplatesPublish
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postTemplatesInfo_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postTemplatesPublish_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postTemplatesPublish_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postTemplatesPublish_200_response'
          description: ""
      summary: Publish template content
      tags:
      - templates
      x-custom-config:
        methodNameCamel: publish
        methodNameSnake: publish
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /templates/delete:
    post:
      deprecated: false
      description: Delete a template.
      operationId: postTemplatesDelete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postTemplatesInfo_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postTemplatesDelete_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postTemplatesDelete_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postTemplatesDelete_200_response'
          description: ""
      summary: Delete template
      tags:
      - templates
      x-custom-config:
        methodNameCamel: delete
        methodNameSnake: delete
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /templates/list:
    post:
      deprecated: false
      description: Return a list of all the templates available to this user.
      operationId: postTemplatesList
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postTemplatesList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: an array of structs with information about each template
                items:
                  $ref: '#/components/schemas/postTemplatesList_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: an array of structs with information about each template
                items:
                  $ref: '#/components/schemas/postTemplatesList_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: an array of structs with information about each template
                items:
                  $ref: '#/components/schemas/postTemplatesList_200_response_inner'
                type: array
          description: ""
      summary: List templates
      tags:
      - templates
      x-custom-config:
        methodNameCamel: list
        methodNameSnake: list
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /templates/time-series:
    post:
      deprecated: false
      description: Return the recent history (hourly stats for the last 30 days) for
        a template.
      operationId: postTemplatesTimeSeries
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postTemplatesTimeSeries_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postSendersTimeSeries_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postSendersTimeSeries_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postSendersTimeSeries_200_response_inner'
                type: array
          description: ""
      summary: Get template history
      tags:
      - templates
      x-custom-config:
        methodNameCamel: timeSeries
        methodNameSnake: time_series
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /templates/render:
    post:
      deprecated: false
      description: "Inject content and optionally merge fields into a template, returning\
        \ the HTML that results."
      operationId: postTemplatesRender
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postTemplatesRender_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postTemplatesRender_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postTemplatesRender_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postTemplatesRender_200_response'
          description: ""
      summary: Render html template
      tags:
      - templates
      x-custom-config:
        methodNameCamel: render
        methodNameSnake: render
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /urls/list:
    post:
      deprecated: true
      description: Get the 100 most clicked URLs.
      operationId: postUrlsList
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: the 100 most clicked URLs and their stats
                items:
                  $ref: '#/components/schemas/postUrlsList_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: the 100 most clicked URLs and their stats
                items:
                  $ref: '#/components/schemas/postUrlsList_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: the 100 most clicked URLs and their stats
                items:
                  $ref: '#/components/schemas/postUrlsList_200_response_inner'
                type: array
          description: ""
      summary: List most clicked urls
      tags:
      - urls
      x-custom-config:
        methodNameCamel: list
        methodNameSnake: list
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /urls/search:
    post:
      deprecated: true
      description: Return the 100 most clicked URLs that match the search query given.
      operationId: postUrlsSearchDeprecated
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postUrlsSearchDeprecated_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: the 100 most clicked URLs matching the search query
                items:
                  $ref: '#/components/schemas/postUrlsSearchDeprecated_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: the 100 most clicked URLs matching the search query
                items:
                  $ref: '#/components/schemas/postUrlsSearchDeprecated_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: the 100 most clicked URLs matching the search query
                items:
                  $ref: '#/components/schemas/postUrlsSearchDeprecated_200_response_inner'
                type: array
          description: ""
      summary: Search most clicked urls
      tags:
      - urls
      x-custom-config:
        methodNameCamel: search
        methodNameSnake: search
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /urls/time-series:
    post:
      deprecated: true
      description: Return the recent history (hourly stats for the last 30 days) for
        a URL
      operationId: postUrlsTimeSeries
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postUrlsTimeSeries_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postUrlsTimeSeries_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postUrlsTimeSeries_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: the array of history information
                items:
                  $ref: '#/components/schemas/postUrlsTimeSeries_200_response_inner'
                type: array
          description: ""
      summary: Get url history
      tags:
      - urls
      x-custom-config:
        methodNameCamel: timeSeries
        methodNameSnake: time_series
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /urls/tracking-domains:
    post:
      deprecated: false
      description: Get the list of tracking domains set up for this account.
      operationId: postUrlsTrackingDomains
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: the tracking domains and their status
                items:
                  $ref: '#/components/schemas/postUrlsTrackingDomains_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: the tracking domains and their status
                items:
                  $ref: '#/components/schemas/postUrlsTrackingDomains_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: the tracking domains and their status
                items:
                  $ref: '#/components/schemas/postUrlsTrackingDomains_200_response_inner'
                type: array
          description: ""
      summary: List tracking domains
      tags:
      - urls
      x-custom-config:
        methodNameCamel: trackingDomains
        methodNameSnake: tracking_domains
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /urls/add-tracking-domain:
    post:
      deprecated: false
      description: Add a tracking domain to your account.
      operationId: postUrlsAddTrackingDomain
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postSendersAddDomain_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postUrlsAddTrackingDomain_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postUrlsAddTrackingDomain_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postUrlsAddTrackingDomain_200_response'
          description: ""
      summary: Add tracking domains
      tags:
      - urls
      x-custom-config:
        methodNameCamel: addTrackingDomain
        methodNameSnake: add_tracking_domain
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /urls/check-tracking-domain:
    post:
      deprecated: false
      description: Checks the CNAME settings for a tracking domain. The domain must
        have been added already with the add-tracking-domain call.
      operationId: postUrlsCheckTrackingDomain
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postUrlsCheckTrackingDomain_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postUrlsAddTrackingDomain_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postUrlsAddTrackingDomain_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postUrlsAddTrackingDomain_200_response'
          description: ""
      summary: Check cname settings
      tags:
      - urls
      x-custom-config:
        methodNameCamel: checkTrackingDomain
        methodNameSnake: check_tracking_domain
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /users/info:
    post:
      deprecated: false
      description: Return the information about the API-connected user.
      operationId: postUsersInfo
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postUsersInfo_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postUsersInfo_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postUsersInfo_200_response'
          description: ""
      summary: Get user info
      tags:
      - users
      x-custom-config:
        methodNameCamel: info
        methodNameSnake: info
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /users/ping:
    post:
      deprecated: false
      description: Validate an API key and respond to a ping.
      operationId: postUsersPing
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: the string "PONG!"
                example: PONG!
                type: string
            application/x-php:
              schema:
                description: the string "PONG!"
                example: PONG!
                type: string
            application/x-yaml; charset=utf-8:
              schema:
                description: the string "PONG!"
                example: PONG!
                type: string
          description: ""
      summary: Ping
      tags:
      - users
      x-custom-config:
        methodNameCamel: ping
        methodNameSnake: ping
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /users/ping2:
    post:
      deprecated: false
      description: Validate an API key and respond to a ping (JSON parser version).
      operationId: postUsersPing2
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postUsersPing2_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postUsersPing2_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postUsersPing2_200_response'
          description: ""
      summary: Ping 2
      tags:
      - users
      x-custom-config:
        methodNameCamel: ping2
        methodNameSnake: ping2
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /users/senders:
    post:
      deprecated: false
      description: "Return the senders that have tried to use this account, both verified\
        \ and unverified."
      operationId: postUsersSenders
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: "an array of sender data, one for each sending addresses\
                  \ used by the account"
                items:
                  $ref: '#/components/schemas/postSendersList_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: "an array of sender data, one for each sending addresses\
                  \ used by the account"
                items:
                  $ref: '#/components/schemas/postSendersList_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: "an array of sender data, one for each sending addresses\
                  \ used by the account"
                items:
                  $ref: '#/components/schemas/postSendersList_200_response_inner'
                type: array
          description: ""
      summary: List account senders
      tags:
      - users
      x-custom-config:
        methodNameCamel: senders
        methodNameSnake: senders
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /webhooks/list:
    post:
      deprecated: false
      description: Get the list of all webhooks defined on the account.
      operationId: postWebhooksList
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postExportsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: the webhooks associated with the account
                items:
                  $ref: '#/components/schemas/postWebhooksList_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: the webhooks associated with the account
                items:
                  $ref: '#/components/schemas/postWebhooksList_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: the webhooks associated with the account
                items:
                  $ref: '#/components/schemas/postWebhooksList_200_response_inner'
                type: array
          description: ""
      summary: List webhooks
      tags:
      - webhooks
      x-custom-config:
        methodNameCamel: list
        methodNameSnake: list
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /webhooks/add:
    post:
      deprecated: false
      description: Add a new webhook.
      operationId: postWebhooksAdd
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postWebhooksAdd_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postWebhooksAdd_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postWebhooksAdd_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postWebhooksAdd_200_response'
          description: ""
      summary: Add webhook
      tags:
      - webhooks
      x-custom-config:
        methodNameCamel: add
        methodNameSnake: add
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /webhooks/info:
    post:
      deprecated: false
      description: "Given the ID of an existing webhook, return the data about it."
      operationId: postWebhooksInfo
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postWebhooksInfo_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postWebhooksInfo_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postWebhooksInfo_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postWebhooksInfo_200_response'
          description: ""
      summary: Get webhook info
      tags:
      - webhooks
      x-custom-config:
        methodNameCamel: info
        methodNameSnake: info
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /webhooks/update:
    post:
      deprecated: false
      description: Update an existing webhook.
      operationId: postWebhooksUpdate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postWebhooksUpdate_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postWebhooksUpdate_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postWebhooksUpdate_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postWebhooksUpdate_200_response'
          description: ""
      summary: Update webhook
      tags:
      - webhooks
      x-custom-config:
        methodNameCamel: update
        methodNameSnake: update
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /webhooks/delete:
    post:
      deprecated: false
      description: Delete an existing webhook.
      operationId: postWebhooksDelete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postWebhooksInfo_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postWebhooksDelete_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postWebhooksDelete_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postWebhooksDelete_200_response'
          description: ""
      summary: Delete webhook
      tags:
      - webhooks
      x-custom-config:
        methodNameCamel: delete
        methodNameSnake: delete
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /whitelists/add:
    post:
      deprecated: false
      description: "Adds an email to your email rejection allowlist. If the address\
        \ is currently on your denylist, that denylist entry will be removed automatically."
      operationId: postWhitelistsAdd
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postAllowlistsAdd_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postAllowlistsAdd_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postAllowlistsAdd_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postAllowlistsAdd_200_response'
          description: ""
      summary: Add email to allowlist
      tags:
      - whitelists
      x-custom-config:
        methodNameCamel: add
        methodNameSnake: add
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /whitelists/list:
    post:
      deprecated: false
      description: Retrieves your email rejection allowlist. You can provide an email
        address or search prefix to limit the results. Returns up to 1000 results.
      operationId: postWhitelistsList
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postAllowlistsList_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                description: up to 1000 allowlist entries
                items:
                  $ref: '#/components/schemas/postAllowlistsList_200_response_inner'
                type: array
            application/x-php:
              schema:
                description: up to 1000 allowlist entries
                items:
                  $ref: '#/components/schemas/postAllowlistsList_200_response_inner'
                type: array
            application/x-yaml; charset=utf-8:
              schema:
                description: up to 1000 allowlist entries
                items:
                  $ref: '#/components/schemas/postAllowlistsList_200_response_inner'
                type: array
          description: ""
      summary: List allowlisted emails
      tags:
      - whitelists
      x-custom-config:
        methodNameCamel: list
        methodNameSnake: list
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
  /whitelists/delete:
    post:
      deprecated: false
      description: Removes an email address from the allowlist.
      operationId: postWhitelistsDelete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/postAllowlistsDelete_request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/postAllowlistsDelete_200_response'
            application/x-php:
              schema:
                $ref: '#/components/schemas/postAllowlistsDelete_200_response'
            application/x-yaml; charset=utf-8:
              schema:
                $ref: '#/components/schemas/postAllowlistsDelete_200_response'
          description: ""
      summary: Remove email from allowlist
      tags:
      - whitelists
      x-custom-config:
        methodNameCamel: delete
        methodNameSnake: delete
      x-codegen-request-body-name: body
      x-content-type: application/json
      x-accepts: application/json
components:
  schemas:
    postIpsList_200_response_inner:
      description: information about a single dedicated IP
      example:
        custom_dns:
          valid: true
          error: error
          enabled: true
        ip: ip
        domain: domain
        pool: pool
        created_at: 2000-01-23T04:56:07.000+00:00
        warmup:
          end_at: 2000-01-23T04:56:07.000+00:00
          warming_up: true
          start_at: 2000-01-23T04:56:07.000+00:00
      properties:
        ip:
          description: the ip address
          type: string
        created_at:
          description: the date and time that the dedicated IP was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        pool:
          description: the name of the pool that this dedicated IP belongs to
          type: string
        domain:
          description: the domain name (reverse dns) of this dedicated IP
          type: string
        custom_dns:
          $ref: '#/components/schemas/postIpsList_200_response_inner_custom_dns'
        warmup:
          $ref: '#/components/schemas/postIpsList_200_response_inner_warmup'
      type: object
    postMessagesSend_request_message_recipient_metadata_inner_values:
      description: "an associated array containing the recipient's unique metadata.\
        \ If a key exists in both the per-recipient metadata and the global metadata,\
        \ the per-recipient metadata will be used."
      properties:
        user_id:
          description: ""
          type: integer
      type: object
    postInboundAddDomain_200_response:
      description: information about the domain
      example:
        domain: domain
        valid_mx: true
        created_at: 2000-01-23T04:56:07.000+00:00
      properties:
        domain:
          description: the domain name that is accepting mail
          type: string
        created_at:
          description: the date and time that the inbound domain was added as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        valid_mx:
          description: true if this inbound domain has successfully set up an MX record
            to deliver mail to the Mandrill servers
          type: boolean
      type: object
    postTemplatesList_200_response_inner:
      description: the information on each template in the account
      example:
        code: code
        from_email: from_email
        subject: subject
        publish_from_email: publish_from_email
        created_at: 2000-01-23T04:56:07.000+00:00
        from_name: from_name
        is_broken_template: true
        labels:
        - labels
        - labels
        publish_name: publish_name
        publish_subject: publish_subject
        updated_at: 2000-01-23T04:56:07.000+00:00
        publish_text: publish_text
        name: name
        text: text
        publish_from_name: publish_from_name
        published_at: 2000-01-23T04:56:07.000+00:00
        slug: slug
        publish_code: publish_code
      properties:
        slug:
          description: the immutable unique code name of the template
          type: string
        name:
          description: the name of the template
          type: string
        labels:
          description: the list of labels applied to the template
          items:
            description: a single label
            type: string
          type: array
        code:
          description: "the full HTML code of the template, with mc:edit attributes\
            \ marking the editable elements - draft version"
          type: string
        subject:
          description: "the subject line of the template, if provided - draft version"
          type: string
        from_email:
          description: "the default sender address for the template, if provided -\
            \ draft version"
          type: string
        from_name:
          description: "the default sender from name for the template, if provided\
            \ - draft version"
          type: string
        text:
          description: "the default text part of messages sent with the template,\
            \ if provided - draft version"
          type: string
        publish_name:
          description: the same as the template name - kept as a separate field for
            backwards compatibility
          type: string
        publish_code:
          description: "the full HTML code of the template, with mc:edit attributes\
            \ marking the editable elements that are available as published, if it\
            \ has been published"
          type: string
        publish_subject:
          description: "the subject line of the template, if provided"
          type: string
        publish_from_email:
          description: "the default sender address for the template, if provided"
          format: email
          type: string
        publish_from_name:
          description: "the default sender from name for the template, if provided"
          type: string
        publish_text:
          description: "the default text part of messages sent with the template,\
            \ if provided"
          type: string
        published_at:
          description: "the date and time the template was last published as a UTC\
            \ string in YYYY-MM-DD HH:MM:SS format, or null if it has not been published"
          format: date-time
          type: string
        created_at:
          description: the date and time the template was first created as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        updated_at:
          description: the date and time the template was last modified as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        is_broken_template:
          description: indicates if the template is malformed or corrupt
          type: boolean
      type: object
    postIpsListPools_200_response_inner_ips_inner:
      description: information about each dedicated IP
      example:
        custom_dns:
          valid: true
          error: error
          enabled: true
        ip: ip
        domain: domain
        pool: pool
        created_at: 2000-01-23T04:56:07.000+00:00
        warmup:
          end_at: 2000-01-23T04:56:07.000+00:00
          warming_up: true
          start_at: 2000-01-23T04:56:07.000+00:00
      properties:
        ip:
          description: the ip address
          type: string
        created_at:
          description: the date and time that the dedicated IP was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        pool:
          description: the name of the pool that this dedicated IP belongs to
          type: string
        domain:
          description: the domain name (reverse dns) of this dedicated IP
          type: string
        custom_dns:
          $ref: '#/components/schemas/postIpsList_200_response_inner_custom_dns'
        warmup:
          $ref: '#/components/schemas/postIpsList_200_response_inner_warmup'
      type: object
    postMessagesSend_request_message:
      description: the information on the message to send
      properties:
        html:
          description: the full HTML content to be sent
          type: string
        text:
          description: optional full text content to be sent
          type: string
        subject:
          description: the message subject
          type: string
        from_email:
          description: the sender email address
          format: email
          type: string
        from_name:
          description: optional from name to be used
          type: string
        to:
          description: an array of recipient information.
          items:
            $ref: '#/components/schemas/postMessagesSend_request_message_to_inner'
          type: array
        headers:
          description: optional extra headers to add to the message (most headers
            are allowed)
          properties: {}
          type: object
        important:
          description: "whether or not this message is important, and should be delivered\
            \ ahead of non-important messages"
          type: boolean
        track_opens:
          description: whether or not to turn on open tracking for the message
          type: boolean
        track_clicks:
          description: whether or not to turn on click tracking for the message
          type: boolean
        auto_text:
          description: whether or not to automatically generate a text part for messages
            that are not given text
          type: boolean
        auto_html:
          description: whether or not to automatically generate an HTML part for messages
            that are not given HTML
          type: boolean
        inline_css:
          description: whether or not to automatically inline all CSS styles provided
            in the message HTML - only for HTML documents less than 256KB in size
          type: boolean
        url_strip_qs:
          description: whether or not to strip the query string from URLs when aggregating
            tracked URL data
          type: boolean
        preserve_recipients:
          description: whether or not to expose all recipients in to "To" header for
            each email
          type: boolean
        view_content_link:
          description: set to false to remove content logging for sensitive emails
          type: boolean
        bcc_address:
          description: an optional address to receive an exact copy of each recipient's
            email
          format: email
          type: string
        tracking_domain:
          description: a custom domain to use for tracking opens and clicks instead
            of mandrillapp.com
          type: string
        signing_domain:
          description: a custom domain to use for SPF/DKIM signing instead of mandrill
            (for "via" or "on behalf of" in email clients)
          type: string
        return_path_domain:
          description: a custom domain to use for the messages's return-path
          type: string
        merge:
          description: whether to evaluate merge tags in the message. Will automatically
            be set to true if either merge_vars or global_merge_vars are provided.
          type: boolean
        merge_language:
          description: "the merge tag language to use when evaluating merge tags,\
            \ either mailchimp or handlebars"
          enum:
          - mailchimp
          - handlebars
          type: string
        global_merge_vars:
          description: global merge variables to use for all recipients. You can override
            these per recipient.
          items:
            $ref: '#/components/schemas/postMessagesSend_request_message_global_merge_vars_inner'
          type: array
        merge_vars:
          description: "per-recipient merge variables, which override global merge\
            \ variables with the same name."
          items:
            $ref: '#/components/schemas/postMessagesSend_request_message_merge_vars_inner'
          type: array
        tags:
          description: "an array of string to tag the message with. Stats are accumulated\
            \ using tags, though we only store the first 100 we see, so this should\
            \ not be unique or change frequently. Tags should be 50 characters or\
            \ less. Any tags starting with an underscore are reserved for internal\
            \ use and will cause errors."
          items:
            description: a single tag - must not start with an underscore
            type: string
          type: array
        subaccount:
          description: the unique id of a subaccount for this message - must already
            exist or will fail with an error
          type: string
        google_analytics_domains:
          description: an array of strings indicating for which any matching URLs
            will automatically have Google Analytics parameters appended to their
            query string automatically.
          items:
            type: string
          type: array
        google_analytics_campaign:
          description: optional string indicating the value to set for the utm_campaign
            tracking parameter. If this isn't provided the email's from address will
            be used instead.
          format: email
          type: string
        metadata:
          $ref: '#/components/schemas/postMessagesSend_request_message_metadata'
        recipient_metadata:
          description: Per-recipient metadata that will override the global values
            specified in the metadata parameter.
          items:
            $ref: '#/components/schemas/postMessagesSend_request_message_recipient_metadata_inner'
          type: array
        attachments:
          description: an array of supported attachments to add to the message
          items:
            $ref: '#/components/schemas/postMessagesSend_request_message_attachments_inner'
          type: array
        images:
          description: an array of embedded images to add to the message
          items:
            $ref: '#/components/schemas/postMessagesSend_request_message_images_inner'
          type: array
      type: object
    postSubaccountsList_200_response_inner:
      description: the individual subaccount info
      example:
        custom_quota: 0
        name: name
        first_sent_at: 2000-01-23T04:56:07.000+00:00
        reputation: 60
        created_at: 2000-01-23T04:56:07.000+00:00
        sent_total: 5
        id: id
        sent_monthly: 5
        sent_weekly: 1
        status: active
      properties:
        id:
          description: a unique indentifier for the subaccount
          type: string
        name:
          description: an optional display name for the subaccount
          type: string
        custom_quota:
          description: "an optional manual hourly quota for the subaccount. If not\
            \ specified, the hourly quota will be managed based on reputation"
          type: integer
        status:
          description: the current sending status of the subaccount
          enum:
          - active
          - paused
          type: string
        reputation:
          description: the subaccount's current reputation on a scale from 0 to 100
          maximum: 100
          minimum: 0
          type: integer
        created_at:
          description: the date and time that the subaccount was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        first_sent_at:
          description: the date and time that the subaccount first sent as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        sent_weekly:
          description: "the number of emails the subaccount has sent so far this week\
            \ (weeks start on midnight Monday, UTC)"
          type: integer
        sent_monthly:
          description: "the number of emails the subaccount has sent so far this month\
            \ (months start on midnight of the 1st, UTC)"
          type: integer
        sent_total:
          description: the number of emails the subaccount has sent since it was created
          type: integer
      type: object
    postUrlsAddTrackingDomain_200_response:
      description: information about the tracking domain
      example:
        last_tested_at: 2000-01-23T04:56:07.000+00:00
        valid_tracking: true
        domain: domain
        cname:
          valid: true
          valid_after: valid_after
          error: error
        created_at: 2000-01-23T04:56:07.000+00:00
      properties:
        domain:
          description: the tracking domain name
          type: string
        created_at:
          description: the date and time that the tracking domain was added as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        last_tested_at:
          description: when the domain's DNS settings were last tested as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        cname:
          $ref: '#/components/schemas/postUrlsTrackingDomains_200_response_inner_cname'
        valid_tracking:
          description: whether this domain can be used as a tracking domain for email.
          type: boolean
      type: object
    postWebhooksAdd_request:
      properties:
        key:
          description: a valid api key
          type: string
        url:
          description: the URL to POST batches of events
          type: string
        description:
          description: an optional description of the webhook
          type: string
        events:
          description: an optional list of events that will be posted to the webhook
          items:
            description: the individual event to listen for
            enum:
            - send
            - hard_bounce
            - soft_bounce
            - open
            - click
            - spam
            - unsub
            - reject
            - blacklist
            - whitelist
            type: string
          type: array
      required:
      - key
      - url
      type: object
    postTemplatesRender_request_merge_vars_inner:
      description: a single merge variable
      properties:
        name:
          description: the merge variable's name. Merge variable names are case-insensitive
            and may not start with _
          type: string
        content:
          description: the merge variable's content
          type: string
      type: object
    postMessagesSend_request_message_recipient_metadata_inner:
      description: metadata for a single recipient
      properties:
        rcpt:
          description: the email address of the recipient that the metadata is associated
            with
          format: email
          type: string
        values:
          $ref: '#/components/schemas/postMessagesSend_request_message_recipient_metadata_inner_values'
      type: object
    postSendersInfo_200_response_stats_last_30_days:
      description: stats for this sender in the last 30 days
      example:
        rejects: 6
        hard_bounces: 2
        unique_clicks: 0
        unsubs: 6
        complaints: 5
        opens: 3
        clicks: 7
        soft_bounces: 6
        sent: 1
        unique_opens: 3
      properties:
        sent:
          description: the number of emails sent for this sender in the last 30 days
          type: integer
        hard_bounces:
          description: the number of emails hard bounced for this sender in the last
            30 days
          type: integer
        soft_bounces:
          description: the number of emails soft bounced for this sender in the last
            30 days
          type: integer
        rejects:
          description: the number of emails rejected for sending this sender in the
            last 30 days
          type: integer
        complaints:
          description: the number of spam complaints for this sender in the last 30
            days
          type: integer
        unsubs:
          description: the number of unsubscribes for this sender in the last 30 days
          type: integer
        opens:
          description: the number of times emails have been opened for this sender
            in the last 30 days
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent for this sender
            in the last 30 days
          type: integer
        clicks:
          description: the number of URLs that have been clicked for this sender in
            the last 30 days
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent for this sender
            in the last 30 days
          type: integer
      type: object
    postExportsRejects_request:
      properties:
        key:
          description: a valid api key
          type: string
        notify_email:
          description: an optional email address to notify when the export job has
            finished.
          format: email
          type: string
      required:
      - key
      type: object
    postMetadataAdd_request:
      properties:
        key:
          description: a valid api key
          type: string
        name:
          description: a unique identifier for the metadata field
          maxLength: 64
          type: string
        view_template:
          description: Mustache template to control how the metadata is rendered in
            your activity log
          maxLength: 1024
          type: string
      required:
      - key
      - name
      type: object
    postTagsDelete_request:
      properties:
        key:
          description: a valid api key
          type: string
        tag:
          description: a tag name
          type: string
      required:
      - key
      - tag
      type: object
    postMessagesSearch_200_response_inner_opens_detail_inner:
      description: information on an individual open
      example:
        ip: ip
        location: location
        ua: ua
        ts: 1
      properties:
        ts:
          description: the unix timestamp from when the message was opened
          type: integer
        ip:
          description: the IP address that generated the open
          type: string
        location:
          description: the approximate region and country that the opening IP is located
          type: string
        ua:
          description: the email client or browser data of the open
          type: string
      type: object
    postMessagesSend_request_message_metadata:
      description: "metadata an associative array of user metadata. Mandrill will\
        \ store this metadata and make it available for retrieval. In addition, you\
        \ can select up to 10 metadata fields to index and make searchable using the\
        \ Mandrill search api."
      properties:
        website:
          description: a valid website url
          type: string
      type: object
    postUrlsSearchDeprecated_200_response_inner:
      description: the URL matching the query
      example:
        unique_clicks: 1
        clicks: 6
        sent: 0
        url: url
      properties:
        url:
          description: the URL to be tracked
          type: string
        sent:
          description: the number of emails that contained the URL
          type: integer
        clicks:
          description: the number of times the URL has been clicked from a tracked
            email
          type: integer
        unique_clicks:
          description: the number of unique emails that have generated clicks for
            this URL
          type: integer
      type: object
    postMessagesSend_request_message_images_inner:
      description: a single embedded image
      properties:
        type:
          description: the MIME type of the image - must start with "image/"
          type: string
        name:
          description: the Content ID of the image - use <img src="cid:THIS_VALUE">
            to reference the image in your HTML content
          type: string
        content:
          description: the content of the image as a base64-encoded string
          type: string
      type: object
    postWebhooksUpdate_request:
      properties:
        key:
          description: a valid api key
          type: string
        id:
          description: the unique identifier of a webhook belonging to this account
          type: number
        url:
          description: the URL to POST batches of events. Requires webhook to exist.
          type: string
        description:
          description: an optional description of the webhook
          type: string
        events:
          description: an optional list of events that will be posted to the webhook
          items:
            description: the individual event to listen for
            enum:
            - send
            - hard_bounce
            - soft_bounce
            - open
            - click
            - spam
            - unsub
            - reject
            - blacklist
            - whitelist
            type: string
          type: array
      required:
      - id
      - key
      - url
      type: object
    postRejectsList_200_response_inner_sender:
      description: "the sender that this denylist entry applies to, or null if none."
      example:
        rejects: 5
        address: address
        hard_bounces: 6
        unique_clicks: 2
        unsubs: 2
        complaints: 5
        created_at: 2000-01-23T04:56:07.000+00:00
        opens: 7
        clicks: 9
        soft_bounces: 1
        sent: 0
        unique_opens: 3
      properties:
        address:
          description: the sender's email address
          format: email
          type: string
        created_at:
          description: the date and time that the sender was first seen by Mandrill
            as a UTC date string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        sent:
          description: the total number of messages sent by this sender
          type: integer
        hard_bounces:
          description: the total number of hard bounces by messages by this sender
          type: integer
        soft_bounces:
          description: the total number of soft bounces by messages by this sender
          type: integer
        rejects:
          description: the total number of rejected messages by this sender
          type: integer
        complaints:
          description: the total number of spam complaints received for messages by
            this sender
          type: integer
        unsubs:
          description: the total number of unsubscribe requests received for messages
            by this sender
          type: integer
        opens:
          description: the total number of times messages by this sender have been
            opened
          type: integer
        clicks:
          description: the total number of times tracked URLs in messages by this
            sender have been clicked
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent for this sender
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent for this sender
          type: integer
      type: object
    postExportsList_request:
      properties:
        key:
          description: a valid api key
          type: string
      required:
      - key
      type: object
    postMessagesSend_request_message_merge_vars_inner_vars_inner:
      properties:
        name:
          description: the merge variable's name. Merge variable names are case-insensitive
            and may not start with _
          type: string
        content:
          description: the merge variable's content
          type: string
      type: object
    postTemplatesDelete_200_response:
      description: the template that was deleted
      example:
        code: code
        from_email: from_email
        subject: subject
        publish_from_email: publish_from_email
        created_at: 2000-01-23T04:56:07.000+00:00
        from_name: from_name
        is_broken_template: true
        labels:
        - labels
        - labels
        publish_name: publish_name
        publish_subject: publish_subject
        updated_at: 2000-01-23T04:56:07.000+00:00
        publish_text: publish_text
        name: name
        text: text
        publish_from_name: publish_from_name
        published_at: 2000-01-23T04:56:07.000+00:00
        slug: slug
        publish_code: publish_code
      properties:
        slug:
          description: the immutable unique code name of the template
          type: string
        name:
          description: the name of the template
          type: string
        labels:
          description: the list of labels applied to the template
          items:
            description: a single label
            type: string
          type: array
        code:
          description: "the full HTML code of the template, with mc:edit attributes\
            \ marking the editable elements - draft version"
          type: string
        subject:
          description: "the subject line of the template, if provided - draft version"
          type: string
        from_email:
          description: "the default sender address for the template, if provided -\
            \ draft version"
          type: string
        from_name:
          description: "the default sender from name for the template, if provided\
            \ - draft version"
          type: string
        text:
          description: "the default text part of messages sent with the template,\
            \ if provided - draft version"
          type: string
        publish_name:
          description: the same as the template name - kept as a separate field for
            backwards compatibility
          type: string
        publish_code:
          description: "the full HTML code of the template, with mc:edit attributes\
            \ marking the editable elements that are available as published, if it\
            \ has been published"
          type: string
        publish_subject:
          description: "the subject line of the template, if provided"
          type: string
        publish_from_email:
          description: "the default sender address for the template, if provided"
          format: email
          type: string
        publish_from_name:
          description: "the default sender from name for the template, if provided"
          type: string
        publish_text:
          description: "the default text part of messages sent with the template,\
            \ if provided"
          type: string
        published_at:
          description: "the date and time the template was last published as a UTC\
            \ string in YYYY-MM-DD HH:MM:SS format, or null if it has not been published"
          format: date-time
          type: string
        created_at:
          description: the date and time the template was first created as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        updated_at:
          description: the date and time the template was last modified as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        is_broken_template:
          description: indicates if the template is malformed or corrupt
          type: boolean
      type: object
    postSubaccountsResume_request:
      properties:
        key:
          description: a valid api key
          type: string
        id:
          description: the unique identifier of the subaccount to resume
          type: string
      required:
      - id
      - key
      type: object
    postMessagesSend_request_message_attachments_inner:
      description: a single supported attachment
      properties:
        type:
          description: the MIME type of the attachment
          type: string
        name:
          description: the file name of the attachment
          type: string
        content:
          description: the content of the attachment as a base64-encoded string
          type: string
      type: object
    postMessagesSendTemplate_request_message:
      description: "the other information on the message to send - same as /messages/send,\
        \ but without the html content"
      properties:
        html:
          description: optional full HTML content to be sent if not in template
          type: string
        text:
          description: optional full text content to be sent
          type: string
        subject:
          description: the message subject
          type: string
        from_email:
          description: the sender email address
          format: email
          type: string
        from_name:
          description: optional from name to be used
          type: string
        to:
          description: an array of recipient information.
          items:
            $ref: '#/components/schemas/postMessagesSend_request_message_to_inner'
          type: array
        headers:
          description: optional extra headers to add to the message (most headers
            are allowed)
          properties: {}
          type: object
        important:
          description: "whether or not this message is important, and should be delivered\
            \ ahead of non-important messages"
          type: boolean
        track_opens:
          description: whether or not to turn on open tracking for the message
          type: boolean
        track_clicks:
          description: whether or not to turn on click tracking for the message
          type: boolean
        auto_text:
          description: whether or not to automatically generate a text part for messages
            that are not given text
          type: boolean
        auto_html:
          description: whether or not to automatically generate an HTML part for messages
            that are not given HTML
          type: boolean
        inline_css:
          description: whether or not to automatically inline all CSS styles provided
            in the message HTML - only for HTML documents less than 256KB in size
          type: boolean
        url_strip_qs:
          description: whether or not to strip the query string from URLs when aggregating
            tracked URL data
          type: boolean
        preserve_recipients:
          description: whether or not to expose all recipients in to "To" header for
            each email
          type: boolean
        view_content_link:
          description: set to false to remove content logging for sensitive emails
          type: boolean
        bcc_address:
          description: an optional address to receive an exact copy of each recipient's
            email
          format: email
          type: string
        tracking_domain:
          description: a custom domain to use for tracking opens and clicks instead
            of mandrillapp.com
          type: string
        signing_domain:
          description: a custom domain to use for SPF/DKIM signing instead of mandrill
            (for "via" or "on behalf of" in email clients)
          type: string
        return_path_domain:
          description: a custom domain to use for the messages's return-path
          type: string
        merge:
          description: whether to evaluate merge tags in the message. Will automatically
            be set to true if either merge_vars or global_merge_vars are provided.
          type: boolean
        merge_language:
          description: "the merge tag language to use when evaluating merge tags,\
            \ either mailchimp or handlebars"
          enum:
          - mailchimp
          - handlebars
          type: string
        global_merge_vars:
          description: global merge variables to use for all recipients. You can override
            these per recipient.
          items:
            $ref: '#/components/schemas/postMessagesSend_request_message_global_merge_vars_inner'
          type: array
        merge_vars:
          description: "per-recipient merge variables, which override global merge\
            \ variables with the same name."
          items:
            $ref: '#/components/schemas/postMessagesSend_request_message_merge_vars_inner'
          type: array
        tags:
          description: "an array of string to tag the message with. Stats are accumulated\
            \ using tags, though we only store the first 100 we see, so this should\
            \ not be unique or change frequently. Tags should be 50 characters or\
            \ less. Any tags starting with an underscore are reserved for internal\
            \ use and will cause errors."
          items:
            description: a single tag - must not start with an underscore
            type: string
          type: array
        subaccount:
          description: the unique id of a subaccount for this message - must already
            exist or will fail with an error
          type: string
        google_analytics_domains:
          description: an array of strings indicating for which any matching URLs
            will automatically have Google Analytics parameters appended to their
            query string automatically.
          items:
            type: string
          type: array
        google_analytics_campaign:
          description: optional string indicating the value to set for the utm_campaign
            tracking parameter. If this isn't provided the email's from address will
            be used instead.
          format: email
          type: string
        metadata:
          $ref: '#/components/schemas/postMessagesSend_request_message_metadata'
        recipient_metadata:
          description: Per-recipient metadata that will override the global values
            specified in the metadata parameter.
          items:
            $ref: '#/components/schemas/postMessagesSend_request_message_recipient_metadata_inner'
          type: array
        attachments:
          description: an array of supported attachments to add to the message
          items:
            $ref: '#/components/schemas/postMessagesSend_request_message_attachments_inner'
          type: array
        images:
          description: an array of embedded images to add to the message
          items:
            $ref: '#/components/schemas/postMessagesSend_request_message_images_inner'
          type: array
      type: object
    postInboundRoutes_request:
      properties:
        key:
          description: a valid api key
          type: string
        domain:
          description: the domain to check
          type: string
      required:
      - domain
      - key
      type: object
    postExportsActivity_request:
      properties:
        key:
          description: a valid api key
          type: string
        notify_email:
          description: an optional email address to notify when the export job has
            finished
          format: email
          type: string
        date_from:
          description: start date as a UTC string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        date_to:
          description: end date as a UTC string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        tags:
          description: an array of tag names to narrow the export to; will match messages
            that contain ANY of the tags
          items:
            description: a tag name
            type: string
          type: array
        senders:
          description: an array of senders to narrow the export to
          items:
            description: a sender address
            type: string
          type: array
        states:
          description: an array of message states to narrow the export to; messages
            with ANY of the states will be included
          items:
            description: a message state
            enum:
            - sent
            - rejected
            - bounced
            - soft-bounced
            - spam
            - unsub
            type: string
          type: array
        api_keys:
          description: an array of api keys to narrow the export to; messsagse sent
            with ANY of the keys will be included
          items:
            description: an API key associated with your account
            type: string
          type: array
      required:
      - key
      type: object
    postSendersVerifyDomain_request:
      properties:
        key:
          description: a valid api key
          type: string
        domain:
          description: domain name at which you can receive email
          type: string
        mailbox:
          description: a mailbox at the domain where the verification email should
            be sent
          type: string
      required:
      - domain
      - key
      - mailbox
      type: object
    postMessagesContent_200_response:
      description: the content of the message
      example:
        headers: "{}"
        from_email: from_email
        attachments:
        - name: name
          type: type
          content: content
        - name: name
          type: type
          content: content
        subject: subject
        html: html
        _id: _id
        to:
          name: name
          email: email
        text: text
        from_name: from_name
        ts: 0
        tags:
        - tags
        - tags
      properties:
        ts:
          description: the Unix timestamp from when this message was sent
          type: integer
        _id:
          description: the message's unique id
          type: string
        from_email:
          description: the email address of the sender
          format: email
          type: string
        from_name:
          description: the alias of the sender (if any)
          type: string
        subject:
          description: the message's subject line
          type: string
        to:
          $ref: '#/components/schemas/postMessagesContent_200_response_to'
        tags:
          description: list of tags on this message
          items:
            description: individual tag on this message
            type: string
          type: array
        headers:
          description: the key-value pairs of the custom MIME headers for the message's
            main document
          properties: {}
          type: object
        text:
          description: "the text part of the message, if any"
          type: string
        html:
          description: "the HTML part of the message, if any"
          type: string
        attachments:
          description: an array of any attachments that can be found in the message
          items:
            $ref: '#/components/schemas/postMessagesContent_200_response_attachments_inner'
          type: array
      type: object
    postSubaccountsInfo_200_response_last_30_days:
      description: stats for this subaccount in the last 30 days
      example:
        rejects: 4
        hard_bounces: 3
        unique_clicks: 7
        unsubs: 1
        complaints: 7
        opens: 1
        clicks: 6
        soft_bounces: 2
        sent: 9
        unique_opens: 1
      properties:
        sent:
          description: the number of emails sent for this subaccount in the last 30
            days
          type: integer
        hard_bounces:
          description: the number of emails hard bounced for this subaccount in the
            last 30 days
          type: integer
        soft_bounces:
          description: the number of emails soft bounced for this subaccount in the
            last 30 days
          type: integer
        rejects:
          description: the number of emails rejected for sending this subaccount in
            the last 30 days
          type: integer
        complaints:
          description: the number of spam complaints for this subaccount in the last
            30 days
          type: integer
        unsubs:
          description: the number of unsbuscribes for this subaccount in the last
            30 days
          type: integer
        opens:
          description: the number of times emails have been opened for this subaccount
            in the last 30 days
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent for this subaccount
            in the last 30 days
          type: integer
        clicks:
          description: the number of URLs that have been clicked for this subaccount
            in the last 30 days
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent for this subaccount
            in the last 30 days
          type: integer
      type: object
    postTagsList_200_response_inner:
      description: a user-defined tag
      example:
        rejects: 5
        hard_bounces: 1
        unique_clicks: 4
        unsubs: 7
        complaints: 2
        reputation: 8
        opens: 9
        clicks: 3
        soft_bounces: 5
        tag: tag
        sent: 6
        unique_opens: 2
      properties:
        tag:
          description: the actual tag as a string
          type: string
        reputation:
          description: the tag's current reputation on a scale from 0 to 100.
          maximum: 100
          minimum: 0
          type: integer
        sent:
          description: the total number of messages sent with this tag
          type: integer
        hard_bounces:
          description: the total number of hard bounces by messages with this tag
          type: integer
        soft_bounces:
          description: the total number of soft bounces by messages with this tag
          type: integer
        rejects:
          description: the total number of rejected messages with this tag
          type: integer
        complaints:
          description: the total number of spam complaints received for messages with
            this tag
          type: integer
        unsubs:
          description: the total number of unsubscribe requests received for messages
            with this tag
          type: integer
        opens:
          description: the total number of times messages with this tag have been
            opened
          type: integer
        clicks:
          description: the total number of times tracked URLs in messages with this
            tag have been clicked
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent with this tag
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent with this tag
          type: integer
      type: object
    postTagsInfo_200_response_stats_last_7_days:
      description: stats with this tag in the last 7 days
      example:
        rejects: 9
        hard_bounces: 5
        unique_clicks: 6
        unsubs: 8
        complaints: 6
        opens: 9
        clicks: 3
        soft_bounces: 9
        sent: 4
        unique_opens: 6
      properties:
        sent:
          description: the number of emails sent with this tag in the last 7 days
          type: integer
        hard_bounces:
          description: the number of emails hard bounced with this tag in the last
            7 days
          type: integer
        soft_bounces:
          description: the number of emails soft bounced with this tag in the last
            7 days
          type: integer
        rejects:
          description: the number of emails rejected for sending this sender in the
            last 7 days
          type: integer
        complaints:
          description: the number of spam complaints with this tag in the last 7 days
          type: integer
        unsubs:
          description: the number of unsubscribes with this tag in the last 7 days
          type: integer
        opens:
          description: the number of times emails have been opened with this tag in
            the last 7 days
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent with this tag in
            the last 7 days
          type: integer
        clicks:
          description: the number of URLs that have been clicked with this tag in
            the last 7 days
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent with this tag in
            the last 7 days
          type: integer
      type: object
    postAllowlistsAdd_request:
      properties:
        key:
          description: a valid api key
          type: string
        email:
          description: an email address to add to the allowlist
          format: email
          type: string
        comment:
          description: an optional description of why the email was added to the allowlist
          maxLength: 255
          type: string
      required:
      - email
      - key
      type: object
    postMessagesSend_request:
      properties:
        key:
          description: a valid api key
          type: string
        message:
          $ref: '#/components/schemas/postMessagesSend_request_message'
        async:
          description: "enable a background sending mode that is optimized for bulk\
            \ sending. In async mode, messages/send will immediately return a status\
            \ of \"queued\" for every recipient. To handle rejections when sending\
            \ in async mode, set up a webhook for the 'reject' event. Defaults to\
            \ false for messages with no more than 10 recipients; messages with more\
            \ than 10 recipients are always sent asynchronously, regardless of the\
            \ value of async."
          type: boolean
        ip_pool:
          description: "the name of the dedicated ip pool that should be used to send\
            \ the message. If you do not have any dedicated IPs, this parameter has\
            \ no effect. If you specify a pool that does not exist, your default pool\
            \ will be used instead."
          type: string
        send_at:
          description: "when this message should be sent as a UTC timestamp in YYYY-MM-DD\
            \ HH:MM:SS format. If you specify a time in the past, the message will\
            \ be sent immediately; for future dates, you're limited to one year from\
            \ the date of scheduling."
          format: date-time
          type: string
      required:
      - key
      - message
      type: object
    postMessagesSendRaw_request:
      properties:
        key:
          description: a valid api key
          type: string
        raw_message:
          description: the full MIME document of an email message
          type: string
        from_email:
          description: optionally define the sender address - otherwise we'll use
            the address found in the provided headers
          format: email
          type: string
        from_name:
          description: optionally define the sender alias
          type: string
        to:
          description: "optionally define the recipient to receive the message - otherwise\
            \ we'll use the To, Cc, and Bcc headers provided in the document"
          items:
            description: the email address of the recipient
            type: string
          type: array
        async:
          description: "enable a background sending mode that is optimized for bulk\
            \ sending. In async mode, messages/sendRaw will immediately return a status\
            \ of \"queued\" for every recipient. To handle rejections when sending\
            \ in async mode, set up a webhook for the 'reject' event. Defaults to\
            \ false for messages with no more than 10 recipients; messages with more\
            \ than 10 recipients are always sent asynchronously, regardless of the\
            \ value of async."
          type: boolean
        ip_pool:
          description: "the name of the dedicated ip pool that should be used to send\
            \ the message. If you do not have any dedicated IPs, this parameter has\
            \ no effect. If you specify a pool that does not exist, your default pool\
            \ will be used instead."
          type: string
        send_at:
          description: "when this message should be sent as a UTC timestamp in YYYY-MM-DD\
            \ HH:MM:SS format. If you specify a time in the past, the message will\
            \ be sent immediately; for future dates, you're limited to one year from\
            \ the date of scheduling."
          format: date-time
          type: string
        return_path_domain:
          description: a custom domain to use for the messages's return-path
          type: string
      required:
      - key
      - raw_message
      type: object
    postIpsCheckCustomDns_request:
      properties:
        key:
          description: a valid api key
          type: string
        ip:
          description: a dedicated ip address
          type: string
        domain:
          description: the domain name to test
          type: string
      required:
      - domain
      - ip
      - key
      type: object
    postInboundAddRoute_request:
      properties:
        key:
          description: a valid api key
          type: string
        domain:
          description: an existing inbound domain
          type: string
        pattern:
          description: the search pattern that the mailbox name should match
          type: string
        url:
          description: the webhook URL where the inbound messages will be published
          type: string
      required:
      - domain
      - key
      - pattern
      - url
      type: object
    postIpsDeletePool_200_response:
      description: information about the status of the pool that was deleted
      example:
        deleted: true
        pool: pool
      properties:
        pool:
          description: the name of the pool
          type: string
        deleted:
          description: whether the pool was deleted
          type: boolean
      type: object
    postMessagesSendTemplate_request:
      properties:
        key:
          description: a valid api key
          type: string
        template_name:
          description: the immutable slug of a template that exists in the user's
            account. Make sure you don't use the template name as this one might change.
          type: string
        template_content:
          description: "an array of template content to send. Each item in the array\
            \ should be a struct with two keys - name: the name of the content block\
            \ to set the content for, and content: the actual content to put into\
            \ the block"
          items:
            $ref: '#/components/schemas/postMessagesSendTemplate_request_template_content_inner'
          type: array
        message:
          $ref: '#/components/schemas/postMessagesSendTemplate_request_message'
        async:
          description: "enable a background sending mode that is optimized for bulk\
            \ sending. In async mode, messages/send will immediately return a status\
            \ of \"queued\" for every recipient. To handle rejections when sending\
            \ in async mode, set up a webhook for the 'reject' event. Defaults to\
            \ false for messages with no more than 10 recipients; messages with more\
            \ than 10 recipients are always sent asynchronously, regardless of the\
            \ value of async."
          type: boolean
        ip_pool:
          description: "the name of the dedicated ip pool that should be used to send\
            \ the message. If you do not have any dedicated IPs, this parameter has\
            \ no effect. If you specify a pool that does not exist, your default pool\
            \ will be used instead."
          type: string
        send_at:
          description: "when this message should be sent as a UTC timestamp in YYYY-MM-DD\
            \ HH:MM:SS format. If you specify a time in the past, the message will\
            \ be sent immediately; for future dates, you're limited to one year from\
            \ the date of scheduling."
          format: date-time
          type: string
      required:
      - key
      - message
      - template_content
      - template_name
      type: object
    postWebhooksInfo_200_response:
      description: the information about the webhook
      example:
        last_sent_at: 2000-01-23T04:56:07.000+00:00
        events_sent: 1
        description: description
        created_at: 2000-01-23T04:56:07.000+00:00
        id: 0
        auth_key: auth_key
        batches_sent: 6
        last_error: last_error
        url: url
        events:
        - send
        - send
      properties:
        id:
          description: a unique integer indentifier for the webhook
          type: integer
        url:
          description: The URL that the event data will be posted to
          type: string
        description:
          description: a description of the webhook
          type: string
        auth_key:
          description: the key used to requests for this webhook
          type: string
        events:
          description: The message events that will be posted to the hook
          items:
            description: the individual message event
            enum:
            - send
            - hard_bounce
            - soft_bounce
            - open
            - click
            - spam
            - unsub
            - reject
            type: string
          type: array
        created_at:
          description: the date and time that the webhook was created as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        last_sent_at:
          description: the date and time that the webhook last successfully received
            events as a UTC string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        batches_sent:
          description: the number of event batches that have ever been sent to this
            webhook
          type: integer
        events_sent:
          description: the total number of events that have ever been sent to this
            webhook
          type: integer
        last_error:
          description: "if we've ever gotten an error trying to post to this webhook,\
            \ the last error that we've seen"
          type: string
      type: object
    postSendersDomains_200_response_inner_dkim:
      description: details about the domain's DKIM record
      example:
        valid: true
        valid_after: 2000-01-23T04:56:07.000+00:00
        error: error
      properties:
        valid:
          description: whether the domain's DKIM record is valid for use with Mandrill
          type: boolean
        valid_after:
          description: "when the domain's DKIM record will be considered valid for\
            \ use with Mandrill as a UTC string in YYYY-MM-DD HH:MM:SS format. If\
            \ set, this indicates that the record is valid now, but was previously\
            \ invalid, and Mandrill will wait until the record's TTL elapses to start\
            \ using it."
          format: date-time
          type: string
        error:
          description: "an error describing the DKIM record, or null if the record\
            \ is correct"
          type: string
      type: object
    postTemplatesList_request:
      properties:
        key:
          description: a valid api key
          type: string
        label:
          description: an optional label to filter the templates
          type: string
      required:
      - key
      type: object
    postUsersInfo_200_response_stats_last_7_days:
      description: stats for this user in the last 7 days
      example:
        rejects: 7
        hard_bounces: 1
        unique_clicks: 6
        unsubs: 4
        complaints: 1
        opens: 5
        clicks: 9
        soft_bounces: 6
        sent: 1
        unique_opens: 9
      properties:
        sent:
          description: the number of emails sent for this user in the last 7 days
          type: integer
        hard_bounces:
          description: the number of emails hard bounced for this user in the last
            7 days
          type: integer
        soft_bounces:
          description: the number of emails soft bounced for this user in the last
            7 days
          type: integer
        rejects:
          description: the number of emails rejected for sending this sender in the
            last 7 days
          type: integer
        complaints:
          description: the number of spam complaints for this user in the last 7 days
          type: integer
        unsubs:
          description: the number of unsubscribes for this user in the last 7 days
          type: integer
        opens:
          description: the number of times emails have been opened for this user in
            the last 7 days
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent for this user in
            the last 7 days
          type: integer
        clicks:
          description: the number of URLs that have been clicked for this user in
            the last 7 days
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent for this user in
            the last 7 days
          type: integer
      type: object
    postUsersPing2_200_response:
      description: a struct with one key "PING" with a static value "PONG!"
      example:
        PING: PONG!
      properties:
        PING:
          description: a simple pong response
          example: PONG!
          type: string
      type: object
    postInboundSendRaw_request:
      properties:
        key:
          description: a valid api key
          type: string
        raw_message:
          description: the full MIME document of an email message
          type: string
        to:
          description: "optionally define the recipients to receive the message -\
            \ otherwise we'll use the To, Cc, and Bcc headers provided in the document"
          items:
            description: the email address of the recipient
            format: email
            type: string
          type: array
        mail_from:
          description: the address specified in the MAIL FROM stage of the SMTP conversation.
            Required for the SPF check.
          format: email
          type: string
        helo:
          description: the identification provided by the client mta in the MTA state
            of the SMTP conversation. Required for the SPF check.
          type: string
        client_address:
          description: the remote MTA's ip address. Optional; required for the SPF
            check.
          type: string
      required:
      - key
      - raw_message
      type: object
    postWebhooksUpdate_200_response:
      description: the information for the updated webhook
      example:
        last_sent_at: 2000-01-23T04:56:07.000+00:00
        events_sent: 1
        description: description
        created_at: 2000-01-23T04:56:07.000+00:00
        id: 0
        auth_key: auth_key
        batches_sent: 6
        last_error: last_error
        url: url
        events:
        - send
        - send
      properties:
        id:
          description: a unique integer indentifier for the webhook
          type: integer
        url:
          description: The URL that the event data will be posted to
          type: string
        description:
          description: a description of the webhook
          type: string
        auth_key:
          description: the key used to requests for this webhook
          type: string
        events:
          description: The message events that will be posted to the hook
          items:
            description: the individual message event
            enum:
            - send
            - hard_bounce
            - soft_bounce
            - open
            - click
            - spam
            - unsub
            - reject
            type: string
          type: array
        created_at:
          description: the date and time that the webhook was created as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        last_sent_at:
          description: the date and time that the webhook last successfully received
            events as a UTC string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        batches_sent:
          description: the number of event batches that have ever been sent to this
            webhook
          type: integer
        events_sent:
          description: the total number of events that have ever been sent to this
            webhook
          type: integer
        last_error:
          description: "if we've ever gotten an error trying to post to this webhook,\
            \ the last error that we've seen"
          type: string
      type: object
    postSendersInfo_200_response_stats_last_7_days:
      description: stats for this sender in the last 7 days
      example:
        rejects: 9
        hard_bounces: 5
        unique_clicks: 6
        unsubs: 8
        complaints: 6
        opens: 9
        clicks: 3
        soft_bounces: 9
        sent: 4
        unique_opens: 6
      properties:
        sent:
          description: the number of emails sent for this sender in the last 7 days
          type: integer
        hard_bounces:
          description: the number of emails hard bounced for this sender in the last
            7 days
          type: integer
        soft_bounces:
          description: the number of emails soft bounced for this sender in the last
            7 days
          type: integer
        rejects:
          description: the number of emails rejected for sending this sender in the
            last 7 days
          type: integer
        complaints:
          description: the number of spam complaints for this sender in the last 7
            days
          type: integer
        unsubs:
          description: the number of unsubscribes for this sender in the last 7 days
          type: integer
        opens:
          description: the number of times emails have been opened for this sender
            in the last 7 days
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent for this sender
            in the last 7 days
          type: integer
        clicks:
          description: the number of URLs that have been clicked for this sender in
            the last 7 days
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent for this sender
            in the last 7 days
          type: integer
      type: object
    postExportsActivity_200_response:
      description: information about the activity export job that was started
      example:
        result_url: result_url
        finished_at: 2000-01-23T04:56:07.000+00:00
        created_at: 2000-01-23T04:56:07.000+00:00
        id: id
        state: state
        type: type
      properties:
        id:
          description: the unique identifier for this Export. Use this identifier
            when checking the export job's status
          type: string
        created_at:
          description: the date and time that the export job was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        type:
          description: the type of the export job
          type: string
        finished_at:
          description: "the date and time that the export job was finished as a UTC\
            \ string in YYYY-MM-DD HH:MM:SS format, or null for jobs that have not\
            \ run"
          format: date-time
          type: string
        state:
          description: the export job's state
          type: string
        result_url:
          description: "the url for the export job's results, if the job is complete"
          type: string
      type: object
    postMetadataDelete_200_response:
      description: the information for the deleted metadata field
      example:
        name: name
        state: active
        view_template: view_template
      properties:
        name:
          description: the unique identifier of the metadata field to update
          type: string
        state:
          description: the current state of the metadata field
          enum:
          - active
          - delete
          - index
          type: string
        view_template:
          description: Mustache template to control how the metadata is rendered in
            your activity log
          type: string
      type: object
    postSendersInfo_200_response_stats:
      description: an aggregate summary of the sender's sending stats
      example:
        last_7_days:
          rejects: 9
          hard_bounces: 5
          unique_clicks: 6
          unsubs: 8
          complaints: 6
          opens: 9
          clicks: 3
          soft_bounces: 9
          sent: 4
          unique_opens: 6
        today:
          rejects: 7
          hard_bounces: 2
          unique_clicks: 1
          unsubs: 1
          complaints: 1
          opens: 1
          clicks: 7
          soft_bounces: 4
          sent: 3
          unique_opens: 6
        last_90_days:
          rejects: 6
          hard_bounces: 4
          unique_clicks: 0
          unsubs: 1
          complaints: 4
          opens: 4
          clicks: 9
          soft_bounces: 0
          sent: 3
          unique_opens: 7
        last_30_days:
          rejects: 6
          hard_bounces: 2
          unique_clicks: 0
          unsubs: 6
          complaints: 5
          opens: 3
          clicks: 7
          soft_bounces: 6
          sent: 1
          unique_opens: 3
        last_60_days:
          rejects: 4
          hard_bounces: 6
          unique_clicks: 5
          unsubs: 7
          complaints: 8
          opens: 3
          clicks: 7
          soft_bounces: 0
          sent: 7
          unique_opens: 3
      properties:
        today:
          $ref: '#/components/schemas/postSendersInfo_200_response_stats_today'
        last_7_days:
          $ref: '#/components/schemas/postSendersInfo_200_response_stats_last_7_days'
        last_30_days:
          $ref: '#/components/schemas/postSendersInfo_200_response_stats_last_30_days'
        last_60_days:
          $ref: '#/components/schemas/postSendersInfo_200_response_stats_last_60_days'
        last_90_days:
          $ref: '#/components/schemas/postSendersInfo_200_response_stats_last_90_days'
      type: object
    postTagsInfo_200_response_stats_today:
      description: stats with this tag so far today
      example:
        rejects: 7
        hard_bounces: 2
        unique_clicks: 1
        unsubs: 1
        complaints: 1
        opens: 1
        clicks: 7
        soft_bounces: 4
        sent: 3
        unique_opens: 6
      properties:
        sent:
          description: the number of emails sent with this tag so far today
          type: integer
        hard_bounces:
          description: the number of emails hard bounced with this tag so far today
          type: integer
        soft_bounces:
          description: the number of emails soft bounced with this tag so far today
          type: integer
        rejects:
          description: the number of emails rejected for sending this sender so far
            today
          type: integer
        complaints:
          description: the number of spam complaints with this tag so far today
          type: integer
        unsubs:
          description: the number of unsubscribes with this tag so far today
          type: integer
        opens:
          description: the number of times emails have been opened with this tag so
            far today
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent with this tag so
            far today
          type: integer
        clicks:
          description: the number of URLs that have been clicked with this tag so
            far today
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent with this tag so
            far today
          type: integer
      type: object
    postSendersTimeSeries_200_response_inner:
      description: the stats for a single hour
      example:
        rejects: 5
        hard_bounces: 6
        unique_clicks: 3
        complaints: 5
        opens: 2
        clicks: 9
        soft_bounces: 1
        time: 2000-01-23T04:56:07.000+00:00
        sent: 0
        unique_opens: 7
      properties:
        time:
          description: the hour as a UTC date string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        sent:
          description: the number of emails that were sent during the hour
          type: integer
        hard_bounces:
          description: the number of emails that hard bounced during the hour
          type: integer
        soft_bounces:
          description: the number of emails that soft bounced during the hour
          type: integer
        rejects:
          description: the number of emails that were rejected during the hour
          type: integer
        complaints:
          description: the number of spam complaints received during the hour
          type: integer
        opens:
          description: the number of emails opened during the hour
          type: integer
        unique_opens:
          description: the number of unique opens generated by messages sent during
            the hour
          type: integer
        clicks:
          description: the number of tracked URLs clicked during the hour
          type: integer
        unique_clicks:
          description: the number of unique clicks generated by messages sent during
            the hour
          type: integer
      type: object
    postInboundDeleteDomain_200_response:
      description: information about the deleted domain
      example:
        domain: domain
        valid_mx: true
        created_at: 2000-01-23T04:56:07.000+00:00
      properties:
        domain:
          description: the domain name that is accepting mail
          type: string
        created_at:
          description: the date and time that the inbound domain was added as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        valid_mx:
          description: true if this inbound domain has successfully set up an MX record
            to deliver mail to the Mandrill servers
          type: boolean
      type: object
    postUsersInfo_200_response_stats_last_60_days:
      description: stats for this user in the last 60 days
      example:
        rejects: 7
        hard_bounces: 3
        unique_clicks: 8
        unsubs: 7
        complaints: 0
        opens: 6
        clicks: 4
        soft_bounces: 3
        sent: 6
        unique_opens: 0
      properties:
        sent:
          description: the number of emails sent for this user in the last 60 days
          type: integer
        hard_bounces:
          description: the number of emails hard bounced for this user in the last
            60 days
          type: integer
        soft_bounces:
          description: the number of emails soft bounced for this user in the last
            60 days
          type: integer
        rejects:
          description: the number of emails rejected for sending this sender in the
            last 60 days
          type: integer
        complaints:
          description: the number of spam complaints for this user in the last 60
            days
          type: integer
        unsubs:
          description: the number of unsubscribes for this user in the last 60 days
          type: integer
        opens:
          description: the number of times emails have been opened for this user in
            the last 60 days
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent for this user in
            the last 60 days
          type: integer
        clicks:
          description: the number of URLs that have been clicked for this user in
            the last 60 days
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent for this user in
            the last 60 days
          type: integer
      type: object
    postExportsInfo_200_response:
      description: the information about the export
      example:
        result_url: result_url
        finished_at: 2000-01-23T04:56:07.000+00:00
        created_at: 2000-01-23T04:56:07.000+00:00
        id: id
        state: state
        type: type
      properties:
        id:
          description: the unique identifier for this Export. Use this identifier
            when checking the export job's status
          type: string
        created_at:
          description: the date and time that the export job was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        type:
          description: "the type of the export job - activity, reject, or allowlist"
          type: string
        finished_at:
          description: the date and time that the export job was finished as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        state:
          description: "the export job's state - waiting, working, complete, error,\
            \ or expired."
          type: string
        result_url:
          description: "the url for the export job's results, if the job is completed."
          type: string
      type: object
    postMetadataList_200_response_inner:
      description: the individual custom metadata field info
      example:
        name: name
        state: active
        view_template: view_template
      properties:
        name:
          description: the unique identifier of the metadata field to update
          type: string
        state:
          description: the current state of the metadata field
          enum:
          - active
          - delete
          - index
          type: string
        view_template:
          description: Mustache template to control how the metadata is rendered in
            your activity log
          type: string
      type: object
    postSendersInfo_request:
      properties:
        key:
          description: a valid api key
          type: string
        address:
          description: the email address of the sender
          format: email
          type: string
      required:
      - address
      - key
      type: object
    postIpsStartWarmup_200_response:
      description: Information about the dedicated IP
      example:
        custom_dns:
          valid: true
          error: error
          enabled: true
        ip: ip
        domain: domain
        pool: pool
        created_at: 2000-01-23T04:56:07.000+00:00
        warmup:
          end_at: 2000-01-23T04:56:07.000+00:00
          warming_up: true
          start_at: 2000-01-23T04:56:07.000+00:00
      properties:
        ip:
          description: the ip address
          type: string
        created_at:
          description: the date and time that the dedicated IP was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        pool:
          description: the name of the pool that this dedicated IP belongs to
          type: string
        domain:
          description: the domain name (reverse dns) of this dedicated IP
          type: string
        custom_dns:
          $ref: '#/components/schemas/postIpsList_200_response_inner_custom_dns'
        warmup:
          $ref: '#/components/schemas/postIpsList_200_response_inner_warmup'
      type: object
    postTagsInfo_200_response:
      description: the detailed information on the tag
      example:
        rejects: 5
        hard_bounces: 6
        stats:
          last_7_days:
            rejects: 9
            hard_bounces: 5
            unique_clicks: 6
            unsubs: 8
            complaints: 6
            opens: 9
            clicks: 3
            soft_bounces: 9
            sent: 4
            unique_opens: 6
          today:
            rejects: 7
            hard_bounces: 2
            unique_clicks: 1
            unsubs: 1
            complaints: 1
            opens: 1
            clicks: 7
            soft_bounces: 4
            sent: 3
            unique_opens: 6
          last_90_days:
            rejects: 6
            hard_bounces: 4
            unique_clicks: 0
            unsubs: 1
            complaints: 4
            opens: 4
            clicks: 9
            soft_bounces: 0
            sent: 3
            unique_opens: 7
          last_30_days:
            rejects: 6
            hard_bounces: 2
            unique_clicks: 0
            unsubs: 6
            complaints: 5
            opens: 3
            clicks: 7
            soft_bounces: 6
            sent: 1
            unique_opens: 3
          last_60_days:
            rejects: 4
            hard_bounces: 6
            unique_clicks: 5
            unsubs: 7
            complaints: 8
            opens: 3
            clicks: 7
            soft_bounces: 0
            sent: 7
            unique_opens: 3
        unsubs: 2
        complaints: 5
        opens: 7
        clicks: 9
        soft_bounces: 1
        tag: tag
        sent: 0
      properties:
        tag:
          description: the actual tag as a string
          type: string
        sent:
          description: the total number of messages sent with this tag
          type: integer
        hard_bounces:
          description: the total number of hard bounces by messages with this tag
          type: integer
        soft_bounces:
          description: the total number of soft bounces by messages with this tag
          type: integer
        rejects:
          description: the total number of rejected messages with this tag
          type: integer
        complaints:
          description: the total number of spam complaints received for messages with
            this tag
          type: integer
        unsubs:
          description: the total number of unsubscribe requests received for messages
            with this tag
          type: integer
        opens:
          description: the total number of times messages with this tag have been
            opened
          type: integer
        clicks:
          description: the total number of times tracked URLs in messages with this
            tag have been clicked
          type: integer
        stats:
          $ref: '#/components/schemas/postTagsInfo_200_response_stats'
      type: object
    postTemplatesAdd_200_response:
      description: the information saved about the new template
      example:
        code: code
        from_email: from_email
        subject: subject
        publish_from_email: publish_from_email
        created_at: 2000-01-23T04:56:07.000+00:00
        from_name: from_name
        is_broken_template: true
        labels:
        - labels
        - labels
        publish_name: publish_name
        publish_subject: publish_subject
        updated_at: 2000-01-23T04:56:07.000+00:00
        publish_text: publish_text
        name: name
        text: text
        publish_from_name: publish_from_name
        published_at: 2000-01-23T04:56:07.000+00:00
        slug: slug
        publish_code: publish_code
      properties:
        slug:
          description: the immutable unique code name of the template
          type: string
        name:
          description: the name of the template
          type: string
        labels:
          description: the list of labels applied to the template
          items:
            description: a single label
            type: string
          type: array
        code:
          description: "the full HTML code of the template, with mc:edit attributes\
            \ marking the editable elements - draft version"
          type: string
        subject:
          description: "the subject line of the template, if provided - draft version"
          type: string
        from_email:
          description: "the default sender address for the template, if provided -\
            \ draft version"
          type: string
        from_name:
          description: "the default sender from name for the template, if provided\
            \ - draft version"
          type: string
        text:
          description: "the default text part of messages sent with the template,\
            \ if provided - draft version"
          type: string
        publish_name:
          description: the same as the template name - kept as a separate field for
            backwards compatibility
          type: string
        publish_code:
          description: "the full HTML code of the template, with mc:edit attributes\
            \ marking the editable elements that are available as published, if it\
            \ has been published"
          type: string
        publish_subject:
          description: "the subject line of the template, if provided"
          type: string
        publish_from_email:
          description: "the default sender address for the template, if provided"
          format: email
          type: string
        publish_from_name:
          description: "the default sender from name for the template, if provided"
          type: string
        publish_text:
          description: "the default text part of messages sent with the template,\
            \ if provided"
          type: string
        published_at:
          description: "the date and time the template was last published as a UTC\
            \ string in YYYY-MM-DD HH:MM:SS format, or null if it has not been published"
          format: date-time
          type: string
        created_at:
          description: the date and time the template was first created as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        updated_at:
          description: the date and time the template was last modified as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        is_broken_template:
          description: indicates if the template is malformed or corrupt
          type: boolean
      type: object
    postTemplatesRender_200_response:
      description: the result of rendering the given template with the content and
        merge field values injected
      example:
        html: html
      properties:
        html:
          description: the rendered HTML as a string
          type: string
      type: object
    postMessagesInfo_200_response_smtp_events_inner:
      description: information about a specific smtp event
      example:
        diag: diag
        type: type
        ts: 5
      properties:
        ts:
          description: the Unix timestamp when the event occured
          type: integer
        type:
          description: the message's state as a result of this event
          type: string
        diag:
          description: the SMTP response from the recipient's server
          type: string
      type: object
    postMessagesParse_200_response_to_inner:
      description: the information on a single recipient
      example:
        name: name
        email: email
      properties:
        email:
          description: the email address of the recipient
          type: string
        name:
          description: the alias of the recipient (if any)
          type: string
      type: object
    postMessagesSearchTimeSeries_request:
      properties:
        key:
          description: a valid api key
          type: string
        query:
          description: the search terms to find matching messages for
          type: string
        date_from:
          description: start date
          format: date-time
          type: string
        date_to:
          description: end date
          format: date-time
          type: string
        tags:
          description: "an array of tag names to narrow the search to, will return\
            \ messages that contain ANY of the tags"
          items:
            description: a single tag name
            type: string
          type: array
        senders:
          description: "an array of sender addresses to narrow the search to, will\
            \ return messages sent by ANY of the senders"
          items:
            description: a single sender email address
            type: string
          type: array
      required:
      - key
      type: object
    postIpsSetCustomDns_request:
      properties:
        key:
          description: a valid api key
          type: string
        ip:
          description: a dedicated ip address
          type: string
        domain:
          description: a domain name to set as the dedicated IP's custom dns name.
          type: string
      required:
      - domain
      - ip
      - key
      type: object
    postRejectsList_request:
      properties:
        key:
          description: a valid api key
          type: string
        email:
          description: an optional email address to search by
          format: email
          type: string
        include_expired:
          description: whether to include rejections that have already expired.
          type: boolean
        subaccount:
          description: an optional unique identifier for the subaccount to limit the
            denylist
          maxLength: 255
          type: string
      required:
      - key
      type: object
    postSubaccountsDelete_request:
      properties:
        key:
          description: a valid api key
          type: string
        id:
          description: the unique identifier of the subaccount to delete
          type: string
      required:
      - id
      - key
      type: object
    postMessagesListScheduled_200_response_inner:
      description: a scheduled email
      example:
        from_email: from_email
        subject: subject
        created_at: 2000-01-23T04:56:07.000+00:00
        send_at: 2000-01-23T04:56:07.000+00:00
        _id: _id
        to: to
      properties:
        _id:
          description: the scheduled message id
          type: string
        created_at:
          description: "the UTC timestamp when the message was created, in YYYY-MM-DD\
            \ HH:MM:SS format"
          format: date-time
          type: string
        send_at:
          description: "the UTC timestamp when the message will be sent, in YYYY-MM-DD\
            \ HH:MM:SS format"
          format: date-time
          type: string
        from_email:
          description: the email's sender address
          format: email
          type: string
        to:
          description: the email's recipient
          format: email
          type: string
        subject:
          description: the email's subject
          type: string
      type: object
    postSendersDomains_200_response_inner_spf:
      description: details about the domain's SPF record
      example:
        valid: true
        valid_after: 2000-01-23T04:56:07.000+00:00
        error: error
      properties:
        valid:
          description: whether the domain's SPF record is valid for use with Mandrill
          type: boolean
        valid_after:
          description: "when the domain's SPF record will be considered valid for\
            \ use with Mandrill as a UTC string in YYYY-MM-DD HH:MM:SS format. If\
            \ set, this indicates that the record is valid now, but was previously\
            \ invalid, and Mandrill will wait until the record's TTL elapses to start\
            \ using it."
          format: date-time
          type: string
        error:
          description: "an error describing the spf record, or null if the record\
            \ is correct"
          type: string
      type: object
    postTemplatesRender_request_template_content_inner:
      description: the injection of a single piece of content into a single editable
        region
      properties:
        name:
          description: the name of the mc:edit editable region to inject into
          type: string
        content:
          description: the content to inject
          type: string
      required:
      - content
      - name
      type: object
    postAllowlistsDelete_200_response:
      description: a status object containing the address and whether the deletion
        succeeded
      example:
        deleted: true
        email: email
      properties:
        email:
          description: the email address that was removed from the denylist
          type: string
        deleted:
          description: whether the address was deleted successfully
          type: boolean
      type: object
    postSendersVerifyDomain_200_response:
      description: information about the verification that was sent
      example:
        domain: domain
        email: email
        status: status
      properties:
        status:
          description: "\"sent\" indicates that the verification has been sent, \"\
            already_verified\" indicates that the domain has already been verified\
            \ with your account"
          type: string
        domain:
          description: the domain name you provided
          type: string
        email:
          description: the email address the verification email was sent to
          format: email
          type: string
      type: object
    postSendersInfo_200_response_stats_today:
      description: stats for this sender so far today
      example:
        rejects: 7
        hard_bounces: 2
        unique_clicks: 1
        unsubs: 1
        complaints: 1
        opens: 1
        clicks: 7
        soft_bounces: 4
        sent: 3
        unique_opens: 6
      properties:
        sent:
          description: the number of emails sent for this sender so far today
          type: integer
        hard_bounces:
          description: the number of emails hard bounced for this sender so far today
          type: integer
        soft_bounces:
          description: the number of emails soft bounced for this sender so far today
          type: integer
        rejects:
          description: the number of emails rejected for sending this sender so far
            today
          type: integer
        complaints:
          description: the number of spam complaints for this sender so far today
          type: integer
        unsubs:
          description: the number of unsubscribes for this sender so far today
          type: integer
        opens:
          description: the number of times emails have been opened for this sender
            so far today
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent for this sender
            so far today
          type: integer
        clicks:
          description: the number of URLs that have been clicked for this sender so
            far today
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent for this sender
            so far today
          type: integer
      type: object
    postInboundDeleteRoute_request:
      properties:
        key:
          description: a valid api key
          type: string
        id:
          description: the unique identifier of an existing route
          type: string
      required:
      - id
      - key
      type: object
    postMessagesListScheduled_request:
      properties:
        key:
          description: a valid api key
          type: string
        to:
          description: an optional recipient address to restrict results to
          format: email
          type: string
      required:
      - key
      type: object
    postIpsSetCustomDns_200_response:
      description: information about the dedicated IP's new configuration
      example:
        custom_dns:
          valid: true
          error: error
          enabled: true
        ip: ip
        domain: domain
        pool: pool
        created_at: 2000-01-23T04:56:07.000+00:00
        warmup:
          end_at: 2000-01-23T04:56:07.000+00:00
          warming_up: true
          start_at: 2000-01-23T04:56:07.000+00:00
      properties:
        ip:
          description: the ip address
          type: string
        created_at:
          description: the date and time that the dedicated IP was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        pool:
          description: the name of the pool that this dedicated IP belongs to
          type: string
        domain:
          description: the domain name (reverse dns) of this dedicated IP
          type: string
        custom_dns:
          $ref: '#/components/schemas/postIpsList_200_response_inner_custom_dns'
        warmup:
          $ref: '#/components/schemas/postIpsList_200_response_inner_warmup'
      type: object
    postSendersList_200_response_inner:
      description: the information on each sending address in the account
      example:
        rejects: 5
        address: address
        hard_bounces: 6
        unique_clicks: 2
        unsubs: 2
        complaints: 5
        created_at: 2000-01-23T04:56:07.000+00:00
        opens: 7
        clicks: 9
        soft_bounces: 1
        sent: 0
        unique_opens: 3
      properties:
        address:
          description: the sender's email address
          format: email
          type: string
        created_at:
          description: the date and time that the sender was first seen by Mandrill
            as a UTC date string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        sent:
          description: the total number of messages sent by this sender
          type: integer
        hard_bounces:
          description: the total number of hard bounces by messages by this sender
          type: integer
        soft_bounces:
          description: the total number of soft bounces by messages by this sender
          type: integer
        rejects:
          description: the total number of rejected messages by this sender
          type: integer
        complaints:
          description: the total number of spam complaints received for messages by
            this sender
          type: integer
        unsubs:
          description: the total number of unsubscribe requests received for messages
            by this sender
          type: integer
        opens:
          description: the total number of times messages by this sender have been
            opened
          type: integer
        clicks:
          description: the total number of times tracked URLs in messages by this
            sender have been clicked
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent for this sender
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent for this sender
          type: integer
      type: object
    postTemplatesInfo_200_response:
      description: the requested template information
      example:
        code: code
        from_email: from_email
        subject: subject
        publish_from_email: publish_from_email
        created_at: 2000-01-23T04:56:07.000+00:00
        from_name: from_name
        is_broken_template: true
        labels:
        - labels
        - labels
        publish_name: publish_name
        publish_subject: publish_subject
        updated_at: 2000-01-23T04:56:07.000+00:00
        publish_text: publish_text
        name: name
        text: text
        publish_from_name: publish_from_name
        published_at: 2000-01-23T04:56:07.000+00:00
        slug: slug
        publish_code: publish_code
      properties:
        slug:
          description: the immutable unique code name of the template
          type: string
        name:
          description: the name of the template
          type: string
        labels:
          description: the list of labels applied to the template
          items:
            description: a single label
            type: string
          type: array
        code:
          description: "the full HTML code of the template, with mc:edit attributes\
            \ marking the editable elements - draft version"
          type: string
        subject:
          description: "the subject line of the template, if provided - draft version"
          type: string
        from_email:
          description: "the default sender address for the template, if provided -\
            \ draft version"
          type: string
        from_name:
          description: "the default sender from name for the template, if provided\
            \ - draft version"
          type: string
        text:
          description: "the default text part of messages sent with the template,\
            \ if provided - draft version"
          type: string
        publish_name:
          description: the same as the template name - kept as a separate field for
            backwards compatibility
          type: string
        publish_code:
          description: "the full HTML code of the template, with mc:edit attributes\
            \ marking the editable elements that are available as published, if it\
            \ has been published"
          type: string
        publish_subject:
          description: "the subject line of the template, if provided"
          type: string
        publish_from_email:
          description: "the default sender address for the template, if provided"
          format: email
          type: string
        publish_from_name:
          description: "the default sender from name for the template, if provided"
          type: string
        publish_text:
          description: "the default text part of messages sent with the template,\
            \ if provided"
          type: string
        published_at:
          description: "the date and time the template was last published as a UTC\
            \ string in YYYY-MM-DD HH:MM:SS format, or null if it has not been published"
          format: date-time
          type: string
        created_at:
          description: the date and time the template was first created as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        updated_at:
          description: the date and time the template was last modified as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        is_broken_template:
          description: indicates if the template is malformed or corrupt
          type: boolean
      type: object
    postUrlsTimeSeries_request:
      properties:
        key:
          description: a valid api key
          type: string
        url:
          description: an existing URL
          type: string
      required:
      - key
      - url
      type: object
    postTagsInfo_200_response_stats_last_90_days:
      description: stats with this tag in the last 90 days
      example:
        rejects: 6
        hard_bounces: 4
        unique_clicks: 0
        unsubs: 1
        complaints: 4
        opens: 4
        clicks: 9
        soft_bounces: 0
        sent: 3
        unique_opens: 7
      properties:
        sent:
          description: the number of emails sent with this tag in the last 90 days
          type: integer
        hard_bounces:
          description: the number of emails hard bounced with this tag in the last
            90 days
          type: integer
        soft_bounces:
          description: the number of emails soft bounced with this tag in the last
            90 days
          type: integer
        rejects:
          description: the number of emails rejected for sending this sender in the
            last 90 days
          type: integer
        complaints:
          description: the number of spam complaints with this tag in the last 90
            days
          type: integer
        unsubs:
          description: the number of unsubscribes with this tag in the last 90 days
          type: integer
        opens:
          description: the number of times emails have been opened with this tag in
            the last 90 days
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent with this tag in
            the last 90 days
          type: integer
        clicks:
          description: the number of URLs that have been clicked with this tag in
            the last 90 days
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent with this tag in
            the last 90 days
          type: integer
      type: object
    postMetadataAdd_200_response:
      description: the information saved about the new metadata field
      example:
        name: name
        state: active
        view_template: view_template
      properties:
        name:
          description: the unique identifier of the metadata field to update
          type: string
        state:
          description: the current state of the metadata field
          enum:
          - active
          - delete
          - index
          type: string
        view_template:
          description: Mustache template to control how the metadata is rendered in
            your activity log
          type: string
      type: object
    postSendersAddDomain_request:
      properties:
        key:
          description: a valid api key
          type: string
        domain:
          description: a domain name
          type: string
      required:
      - domain
      - key
      type: object
    postInboundCheckDomain_200_response:
      description: information about the inbound domain
      example:
        domain: domain
        valid_mx: true
        created_at: 2000-01-23T04:56:07.000+00:00
      properties:
        domain:
          description: the domain name that is accepting mail
          type: string
        created_at:
          description: the date and time that the inbound domain was added as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        valid_mx:
          description: true if this inbound domain has successfully set up an MX record
            to deliver mail to the Mandrill servers
          type: boolean
      type: object
    postMessagesSearch_200_response_inner:
      description: the information for a single matching message
      example:
        template: template
        metadata: "{}"
        subject: subject
        clicks_detail:
        - ip: ip
          location: location
          ua: ua
          url: url
          ts: 5
        - ip: ip
          location: location
          ua: ua
          url: url
          ts: 5
        tags:
        - tags
        - tags
        opens_detail:
        - ip: ip
          location: location
          ua: ua
          ts: 1
        - ip: ip
          location: location
          ua: ua
          ts: 1
        sender: sender
        opens: 6
        clicks: 5
        _id: _id
        state: sent
        email: email
        ts: 0
      properties:
        ts:
          description: the Unix timestamp from when this message was sent
          type: integer
        _id:
          description: the message's unique id
          type: string
        sender:
          description: the email address of the sender
          format: email
          type: string
        template:
          description: "the unique name of the template used, if any"
          type: string
        subject:
          description: the message's subject line
          type: string
        email:
          description: the recipient email address
          format: email
          type: string
        tags:
          description: list of tags on this message
          items:
            description: individual tag on this message
            type: string
          type: array
        opens:
          description: how many times has this message been opened
          type: integer
        opens_detail:
          description: list of individual opens for the message
          items:
            $ref: '#/components/schemas/postMessagesSearch_200_response_inner_opens_detail_inner'
          type: array
        clicks:
          description: how many times has a link been clicked in this message
          type: integer
        clicks_detail:
          description: list of individual clicks for the message
          items:
            $ref: '#/components/schemas/postMessagesSearch_200_response_inner_clicks_detail_inner'
          type: array
        state:
          description: sending status of this message
          enum:
          - sent
          - bounced
          - rejected
          type: string
        metadata:
          description: any custom metadata provided when the message was sent
          properties: {}
          type: object
      type: object
    postTagsInfo_request:
      properties:
        key:
          description: a valid api key
          type: string
        tag:
          description: an existing tag name
          type: string
      required:
      - key
      - tag
      type: object
    postMessagesContent_200_response_attachments_inner:
      description: information about an individual attachment
      example:
        name: name
        type: type
        content: content
      properties:
        name:
          description: the file name of the attachment
          type: string
        type:
          description: the MIME type of the attachment
          type: string
        content:
          description: the content of the attachment as a base64 encoded string
          type: string
      type: object
    postExportsInfo_request:
      properties:
        key:
          description: a valid api key
          type: string
        id:
          description: an export job identifier
          type: string
      required:
      - id
      - key
      type: object
    postSendersInfo_200_response_stats_last_90_days:
      description: stats for this sender in the last 90 days
      example:
        rejects: 6
        hard_bounces: 4
        unique_clicks: 0
        unsubs: 1
        complaints: 4
        opens: 4
        clicks: 9
        soft_bounces: 0
        sent: 3
        unique_opens: 7
      properties:
        sent:
          description: the number of emails sent for this sender in the last 90 days
          type: integer
        hard_bounces:
          description: the number of emails hard bounced for this sender in the last
            90 days
          type: integer
        soft_bounces:
          description: the number of emails soft bounced for this sender in the last
            90 days
          type: integer
        rejects:
          description: the number of emails rejected for sending this sender in the
            last 90 days
          type: integer
        complaints:
          description: the number of spam complaints for this sender in the last 90
            days
          type: integer
        unsubs:
          description: the number of unsubscribes for this sender in the last 90 days
          type: integer
        opens:
          description: the number of times emails have been opened for this sender
            in the last 90 days
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent for this sender
            in the last 90 days
          type: integer
        clicks:
          description: the number of URLs that have been clicked for this sender in
            the last 90 days
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent for this sender
            in the last 90 days
          type: integer
      type: object
    postTemplatesUpdate_200_response:
      description: the template that was updated
      example:
        code: code
        from_email: from_email
        subject: subject
        publish_from_email: publish_from_email
        created_at: 2000-01-23T04:56:07.000+00:00
        from_name: from_name
        is_broken_template: true
        labels:
        - labels
        - labels
        publish_name: publish_name
        publish_subject: publish_subject
        updated_at: 2000-01-23T04:56:07.000+00:00
        publish_text: publish_text
        name: name
        text: text
        publish_from_name: publish_from_name
        published_at: 2000-01-23T04:56:07.000+00:00
        slug: slug
        publish_code: publish_code
      properties:
        slug:
          description: the immutable unique code name of the template
          type: string
        name:
          description: the name of the template
          type: string
        labels:
          description: the list of labels applied to the template
          items:
            description: a single label
            type: string
          type: array
        code:
          description: "the full HTML code of the template, with mc:edit attributes\
            \ marking the editable elements - draft version"
          type: string
        subject:
          description: "the subject line of the template, if provided - draft version"
          type: string
        from_email:
          description: "the default sender address for the template, if provided -\
            \ draft version"
          type: string
        from_name:
          description: "the default sender from name for the template, if provided\
            \ - draft version"
          type: string
        text:
          description: "the default text part of messages sent with the template,\
            \ if provided - draft version"
          type: string
        publish_name:
          description: the same as the template name - kept as a separate field for
            backwards compatibility
          type: string
        publish_code:
          description: "the full HTML code of the template, with mc:edit attributes\
            \ marking the editable elements that are available as published, if it\
            \ has been published"
          type: string
        publish_subject:
          description: "the subject line of the template, if provided"
          type: string
        publish_from_email:
          description: "the default sender address for the template, if provided"
          format: email
          type: string
        publish_from_name:
          description: "the default sender from name for the template, if provided"
          type: string
        publish_text:
          description: "the default text part of messages sent with the template,\
            \ if provided"
          type: string
        published_at:
          description: "the date and time the template was last published as a UTC\
            \ string in YYYY-MM-DD HH:MM:SS format, or null if it has not been published"
          format: date-time
          type: string
        created_at:
          description: the date and time the template was first created as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        updated_at:
          description: the date and time the template was last modified as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        is_broken_template:
          description: indicates if the template is malformed or corrupt
          type: boolean
      type: object
    postRejectsList_200_response_inner:
      description: the information for each rejection  entry
      example:
        reason: reason
        last_event_at: 2000-01-23T04:56:07.000+00:00
        expires_at: 2000-01-23T04:56:07.000+00:00
        expired: true
        sender:
          rejects: 5
          address: address
          hard_bounces: 6
          unique_clicks: 2
          unsubs: 2
          complaints: 5
          created_at: 2000-01-23T04:56:07.000+00:00
          opens: 7
          clicks: 9
          soft_bounces: 1
          sent: 0
          unique_opens: 3
        subaccount: subaccount
        created_at: 2000-01-23T04:56:07.000+00:00
        detail: detail
        email: email
      properties:
        email:
          description: the email that is blocked
          format: email
          type: string
        reason:
          description: "the type of event (hard-bounce, soft-bounce, spam, unsub,\
            \ custom) that caused this rejection"
          type: string
        detail:
          description: "extended details about the event, such as the SMTP diagnostic\
            \ for bounces or the comment for manually-created rejections"
          type: string
        created_at:
          description: when the email was added to the denylist
          format: date-time
          type: string
        last_event_at:
          description: the timestamp of the most recent event that either created
            or renewed this rejection
          format: date-time
          type: string
        expires_at:
          description: when the denylist entry will expire (this may be in the past)
          format: date-time
          type: string
        expired:
          description: whether the denylist entry has expired
          type: boolean
        sender:
          $ref: '#/components/schemas/postRejectsList_200_response_inner_sender'
        subaccount:
          description: "the subaccount that this denylist entry applies to, or null\
            \ if none."
          type: string
      type: object
    postSendersAddDomain_200_response:
      description: information about the domain
      example:
        valid_signing: true
        verify_txt_key: verify_txt_key
        last_tested_at: 2000-01-23T04:56:07.000+00:00
        dkim:
          valid: true
          valid_after: 2000-01-23T04:56:07.000+00:00
          error: error
        verified_at: 2000-01-23T04:56:07.000+00:00
        domain: domain
        spf:
          valid: true
          valid_after: 2000-01-23T04:56:07.000+00:00
          error: error
        created_at: 2000-01-23T04:56:07.000+00:00
      properties:
        domain:
          description: the sender domain name
          type: string
        created_at:
          description: the date and time that the sending domain was first seen as
            a UTC string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        last_tested_at:
          description: when the domain's DNS settings were last tested as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        spf:
          $ref: '#/components/schemas/postSendersDomains_200_response_inner_spf'
        dkim:
          $ref: '#/components/schemas/postSendersDomains_200_response_inner_dkim'
        verified_at:
          description: "if the domain has been verified, this indicates when that\
            \ verification occurred as a UTC string in YYYY-MM-DD HH:MM:SS format"
          format: date-time
          type: string
        valid_signing:
          description: "whether this domain can be used to authenticate mail, either\
            \ for itself or as a custom signing domain. If this is false but spf and\
            \ dkim are both valid, you will need to verify the domain before using\
            \ it to authenticate mail"
          type: boolean
        verify_txt_key:
          description: a unique key used to verify a domain by adding a TXT record.
            Append this key to 'mandrill_verify.' and add it to your domain's TXT
            records to verify
          type: string
      type: object
    postIpsListPools_200_response_inner:
      description: information about each dedicated IP pool
      example:
        name: name
        created_at: 2000-01-23T04:56:07.000+00:00
        ips:
        - custom_dns:
            valid: true
            error: error
            enabled: true
          ip: ip
          domain: domain
          pool: pool
          created_at: 2000-01-23T04:56:07.000+00:00
          warmup:
            end_at: 2000-01-23T04:56:07.000+00:00
            warming_up: true
            start_at: 2000-01-23T04:56:07.000+00:00
        - custom_dns:
            valid: true
            error: error
            enabled: true
          ip: ip
          domain: domain
          pool: pool
          created_at: 2000-01-23T04:56:07.000+00:00
          warmup:
            end_at: 2000-01-23T04:56:07.000+00:00
            warming_up: true
            start_at: 2000-01-23T04:56:07.000+00:00
      properties:
        name:
          description: this pool's name
          type: string
        created_at:
          description: the date and time that this pool was created as a UTC timestamp
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        ips:
          description: the dedicated IPs in this pool
          items:
            $ref: '#/components/schemas/postIpsListPools_200_response_inner_ips_inner'
          type: array
      type: object
    postSubaccountsInfo_200_response:
      description: the information about the subaccount
      example:
        notes: notes
        custom_quota: 0
        first_sent_at: 2000-01-23T04:56:07.000+00:00
        reputation: 60
        created_at: 2000-01-23T04:56:07.000+00:00
        last_30_days:
          rejects: 4
          hard_bounces: 3
          unique_clicks: 7
          unsubs: 1
          complaints: 7
          opens: 1
          clicks: 6
          soft_bounces: 2
          sent: 9
          unique_opens: 1
        sent_weekly: 1
        hourly_quota: 7
        name: name
        sent_total: 5
        sent_hourly: 2
        id: id
        sent_monthly: 5
        status: active
      properties:
        id:
          description: a unique indentifier for the subaccount
          type: string
        name:
          description: an optional display name for the subaccount
          type: string
        notes:
          description: optional extra text to associate with the subaccount
          type: string
        custom_quota:
          description: "an optional manual hourly quota for the subaccount. If not\
            \ specified, the hourly quota will be managed based on reputation"
          type: integer
        status:
          description: the current sending status of the subaccount
          enum:
          - active
          - paused
          type: string
        reputation:
          description: the subaccount's current reputation on a scale from 0 to 100
          maximum: 100
          minimum: 0
          type: integer
        created_at:
          description: the date and time that the subaccount was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        first_sent_at:
          description: the date and time that the subaccount first sent as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        sent_weekly:
          description: "the number of emails the subaccount has sent so far this week\
            \ (weeks start on midnight Monday, UTC)"
          type: integer
        sent_monthly:
          description: "the number of emails the subaccount has sent so far this month\
            \ (months start on midnight of the 1st, UTC)"
          type: integer
        sent_total:
          description: the number of emails the subaccount has sent since it was created
          type: integer
        sent_hourly:
          description: the number of emails the subaccount has sent in the last hour
          type: integer
        hourly_quota:
          description: "the current hourly quota for the subaccount, either manual\
            \ or reputation-based"
          type: integer
        last_30_days:
          $ref: '#/components/schemas/postSubaccountsInfo_200_response_last_30_days'
      type: object
    postSubaccountsUpdate_request:
      properties:
        key:
          description: a valid api key
          type: string
        id:
          description: the unique identifier of the subaccount to update
          type: string
        name:
          description: an optional display name to further identify the subaccount
          maxLength: 1024
          type: string
        notes:
          description: optional extra text to associate with the subaccount
          type: string
        custom_quota:
          description: "an optional manual hourly quota for the subaccount. If not\
            \ specified, Mandrill will manage this based on reputation"
          minimum: 0
          type: integer
      required:
      - id
      - key
      type: object
    postUsersInfo_200_response_stats_all_time:
      description: tats for the lifetime of the user's account
      example:
        rejects: 9
        hard_bounces: 4
        unique_clicks: 4
        unsubs: 9
        complaints: 0
        opens: 0
        clicks: 5
        soft_bounces: 7
        sent: 1
        unique_opens: 7
      properties:
        sent:
          description: the number of emails sent in the lifetime of the user's account
          type: integer
        hard_bounces:
          description: the number of emails hard bounced in the lifetime of the user's
            account
          type: integer
        soft_bounces:
          description: the number of emails soft bounced in the lifetime of the user's
            account
          type: integer
        rejects:
          description: the number of emails rejected for sending this user so far
            today
          type: integer
        complaints:
          description: the number of spam complaints in the lifetime of the user's
            account
          type: integer
        unsubs:
          description: the number of unsubscribes in the lifetime of the user's account
          type: integer
        opens:
          description: the number of times emails have been opened in the lifetime
            of the user's account
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent in the lifetime
            of the user's account
          type: integer
        clicks:
          description: the number of URLs that have been clicked in the lifetime of
            the user's account
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent in the lifetime
            of the user's account
          type: integer
      type: object
    postInboundRoutes_200_response_inner:
      description: the individual mailbox route
      example:
        pattern: pattern
        id: id
        url: url
      properties:
        id:
          description: the unique identifier of the route
          type: string
        pattern:
          description: the search pattern that the mailbox name should match
          type: string
        url:
          description: the webhook URL where inbound messages will be published
          type: string
      type: object
    postIpsCheckCustomDns_200_response:
      description: information about the dedicated IP's new configuration
      example:
        valid: valid
        error: error
      properties:
        valid:
          description: whether the domain name has a correctly-configured A record
            pointing to the ip address
          type: string
        error:
          description: "if valid is false, this will contain details about why the\
            \ domain's A record is incorrect"
          type: string
      type: object
    postMessagesParse_request:
      properties:
        key:
          description: a valid api key
          type: string
        raw_message:
          description: the full MIME document of an email message
          type: string
      required:
      - key
      - raw_message
      type: object
    postTemplatesUpdate_request:
      properties:
        key:
          description: a valid api key
          type: string
        name:
          description: the immutable name of an existing template
          type: string
        from_email:
          description: the new default sending address
          format: email
          type: string
        from_name:
          description: the new default from name
          type: string
        subject:
          description: the new default subject line
          type: string
        code:
          description: the new code for the template
          type: string
        text:
          description: the new default text part to be used
          type: string
        publish:
          description: set to false to update the draft version of the template without
            publishing
          type: boolean
        labels:
          description: an optional array of up to 10 labels to use for filtering templates
          items:
            description: a single label
            maxLength: 100
            type: string
          type: array
      required:
      - key
      - name
      type: object
    postTagsInfo_200_response_stats_last_60_days:
      description: stats with this tag in the last 60 days
      example:
        rejects: 4
        hard_bounces: 6
        unique_clicks: 5
        unsubs: 7
        complaints: 8
        opens: 3
        clicks: 7
        soft_bounces: 0
        sent: 7
        unique_opens: 3
      properties:
        sent:
          description: the number of emails sent with this tag in the last 60 days
          type: integer
        hard_bounces:
          description: the number of emails hard bounced with this tag in the last
            60 days
          type: integer
        soft_bounces:
          description: the number of emails soft bounced with this tag in the last
            60 days
          type: integer
        rejects:
          description: the number of emails rejected for sending this sender in the
            last 60 days
          type: integer
        complaints:
          description: the number of spam complaints with this tag in the last 60
            days
          type: integer
        unsubs:
          description: the number of unsubscribes with this tag in the last 60 days
          type: integer
        opens:
          description: the number of times emails have been opened with this tag in
            the last 60 days
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent with this tag in
            the last 60 days
          type: integer
        clicks:
          description: the number of URLs that have been clicked with this tag in
            the last 60 days
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent with this tag in
            the last 60 days
          type: integer
      type: object
    postInboundUpdateRoute_request:
      properties:
        key:
          description: a valid api key
          type: string
        id:
          description: the unique identifier of an existing mailbox route
          type: string
        pattern:
          description: the search pattern that the mailbox name should match
          type: string
        url:
          description: "the webhook URL where the inbound messages will be published;\
            \ Validation: webhookexists"
          type: string
      required:
      - id
      - key
      type: object
    postIpsInfo_200_response:
      description: Information about the dedicated ip
      example:
        custom_dns:
          valid: true
          error: error
          enabled: true
        ip: ip
        domain: domain
        pool: pool
        created_at: 2000-01-23T04:56:07.000+00:00
        warmup:
          end_at: 2000-01-23T04:56:07.000+00:00
          warming_up: true
          start_at: 2000-01-23T04:56:07.000+00:00
      properties:
        ip:
          description: the ip address
          type: string
        created_at:
          description: the date and time that the dedicated IP was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        pool:
          description: the name of the pool that this dedicated IP belongs to
          type: string
        domain:
          description: the domain name (reverse dns) of this dedicated IP
          type: string
        custom_dns:
          $ref: '#/components/schemas/postIpsList_200_response_inner_custom_dns'
        warmup:
          $ref: '#/components/schemas/postIpsList_200_response_inner_warmup'
      type: object
    postMessagesParse_200_response:
      description: the parsed message
      example:
        headers: "{}"
        images:
        - name: name
          type: type
          content: content
        - name: name
          type: type
          content: content
        from_email: from_email
        attachments:
        - binary: true
          name: name
          type: type
          content: content
        - binary: true
          name: name
          type: type
          content: content
        subject: subject
        html: html
        to:
        - name: name
          email: email
        - name: name
          email: email
        text: text
        from_name: from_name
      properties:
        subject:
          description: the subject of the message
          type: string
        from_email:
          description: the email address of the sender
          format: email
          type: string
        from_name:
          description: the alias of the sender (if any)
          type: string
        to:
          description: an array of any recipients in the message
          items:
            $ref: '#/components/schemas/postMessagesParse_200_response_to_inner'
          type: array
        headers:
          description: the key-value pairs of the MIME headers for the message's main
            document
          properties: {}
          type: object
        text:
          description: "the text part of the message, if any"
          type: string
        html:
          description: "the HTML part of the message, if any"
          type: string
        attachments:
          description: an array of any attachments that can be found in the message
          items:
            $ref: '#/components/schemas/postMessagesParse_200_response_attachments_inner'
          type: array
        images:
          description: an array of any embedded images that can be found in the message
          items:
            $ref: '#/components/schemas/postMessagesParse_200_response_images_inner'
          type: array
      type: object
    postMessagesCancelScheduled_request:
      properties:
        key:
          description: a valid api key
          type: string
        id:
          description: "a scheduled email id, as returned by any of the messages/send\
            \ calls or messages/list-scheduled"
          type: string
      required:
      - id
      - key
      type: object
    postMessagesSend_200_response_inner:
      description: the sending results for a single recipient
      example:
        reject_reason: hard-bounce
        _id: _id
        queued_reason: attachments
        email: email
        status: sent
      properties:
        email:
          description: the email address of the recipient
          format: email
          type: string
        status:
          description: the sending status of the recipient
          enum:
          - sent
          - queued
          - scheduled
          - rejected
          - invalid
          type: string
        reject_reason:
          description: the reason for the rejection if the recipient status is "rejected"
          enum:
          - hard-bounce
          - soft-bounce
          - spam
          - unsub
          - custom
          - invalid-sender
          - invalid
          - test-mode-limit
          - unsigned
          - rule
          type: string
        queued_reason:
          description: the reason for the email being queued if the response status
            is "queued"
          enum:
          - attachments
          - multiple-recipients
          - free-trial-sends-exhausted
          - hourly-quota-exhausted
          - monthly-limit-reached
          - sending-paused
          - sending-suspended
          - account-suspended
          - sending-backlogged
          type: string
        _id:
          description: the message's unique id
          type: string
      type: object
    postMessagesSendTemplate_request_template_content_inner:
      description: the injection of a single piece of content into a single editable
        region
      properties:
        name:
          description: the name of the mc:edit editable region to inject into
          type: string
        content:
          description: the content to inject
          type: string
      type: object
    postMessagesSearchTimeSeries_200_response_inner:
      description: the stats for a single hour
      example:
        rejects: 5
        hard_bounces: 6
        unique_clicks: 2
        unsubs: 2
        complaints: 5
        opens: 7
        clicks: 3
        soft_bounces: 1
        time: 2000-01-23T04:56:07.000+00:00
        sent: 0
        unique_opens: 9
      properties:
        time:
          description: the hour as a UTC date string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        sent:
          description: the number of emails that were sent during the hour
          type: integer
        hard_bounces:
          description: the number of emails that hard bounced during the hour
          type: integer
        soft_bounces:
          description: the number of emails that soft bounced during the hour
          type: integer
        rejects:
          description: the number of emails that were rejected during the hour
          type: integer
        complaints:
          description: the number of spam complaints received during the hour
          type: integer
        unsubs:
          description: the number of unsubscribes received during the hour
          type: integer
        opens:
          description: the number of emails opened during the hour
          type: integer
        unique_opens:
          description: the number of unique opens generated by messages sent during
            the hour
          type: integer
        clicks:
          description: the number of tracked URLs clicked during the hour
          type: integer
        unique_clicks:
          description: the number of unique clicks generated by messages sent during
            the hour
          type: integer
      type: object
    postMetadataUpdate_request:
      properties:
        key:
          description: a valid api key
          type: string
        name:
          description: the unique identifier of the metadata field to update
          type: string
        view_template:
          description: Mustache template to control how the metadata is rendered in
            your activity log
          maxLength: 1024
          type: string
      required:
      - key
      - name
      - view_template
      type: object
    postIpsCreatePool_request:
      properties:
        key:
          description: a valid api key
          type: string
        pool:
          description: the name of a pool to create
          type: string
      required:
      - key
      - pool
      type: object
    postMessagesSearch_200_response_inner_clicks_detail_inner:
      description: information on an individual click
      example:
        ip: ip
        location: location
        ua: ua
        url: url
        ts: 5
      properties:
        ts:
          description: the unix timestamp from when the message was clicked
          type: integer
        url:
          description: the URL that was clicked on
          type: string
        ip:
          description: the IP address that generated the click
          type: string
        location:
          description: the approximate region and country that the clicking IP is
            located
          type: string
        ua:
          description: the email client or browser data of the click
          type: string
      type: object
    postIpsSetPool_request:
      properties:
        key:
          description: a valid api key
          type: string
        ip:
          description: a dedicated ip address
          type: string
        pool:
          description: the name of the new pool to add the dedicated ip to
          type: string
        create_pool:
          description: "whether to create the pool if it does not exist; if false\
            \ and the pool does not exist, an Unknown_Pool will be thrown."
          type: boolean
      required:
      - ip
      - key
      - pool
      type: object
    postIpsPoolInfo_request:
      properties:
        key:
          description: a valid api key
          type: string
        pool:
          description: a pool name
          type: string
      required:
      - key
      - pool
      type: object
    postRejectsAdd_200_response:
      description: a status object containing the address and the result of the operation
      example:
        added: true
        email: email
      properties:
        email:
          description: the email address you provided
          format: email
          type: string
        added:
          description: whether the operation succeeded
          type: boolean
      type: object
    postMessagesSend_request_message_global_merge_vars_inner:
      description: a single global merge variable
      properties:
        name:
          description: the global merge variable's name. Merge variable names are
            case-insensitive and may not start with _
          type: string
        content:
          description: the global merge variable's content
          type: string
      type: object
    postInboundDomains_200_response_inner:
      description: the individual domain info
      example:
        domain: domain
        valid_mx: true
        created_at: 2000-01-23T04:56:07.000+00:00
      properties:
        domain:
          description: the domain name that is accepting mail
          type: string
        created_at:
          description: the date and time that the inbound domain was added as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        valid_mx:
          description: true if this inbound domain has successfully set up an MX record
            to deliver mail to the Mandrill servers
          type: boolean
      type: object
    postIpsList_200_response_inner_warmup:
      description: information about the ip's warmup status
      example:
        end_at: 2000-01-23T04:56:07.000+00:00
        warming_up: true
        start_at: 2000-01-23T04:56:07.000+00:00
      properties:
        warming_up:
          description: whether the ip is currently in warmup mode
          type: boolean
        start_at:
          description: the start time for the warmup process as a UTC string in YYYY-MM-DD
            HH:MM:SS format
          format: date-time
          type: string
        end_at:
          description: the end date and time for the warmup process as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
      type: object
    postSubaccountsDelete_200_response:
      description: the information for the deleted subaccount
      example:
        custom_quota: 0
        name: name
        first_sent_at: 2000-01-23T04:56:07.000+00:00
        reputation: 60
        created_at: 2000-01-23T04:56:07.000+00:00
        sent_total: 5
        id: id
        sent_monthly: 5
        sent_weekly: 1
        status: active
      properties:
        id:
          description: a unique indentifier for the subaccount
          type: string
        name:
          description: an optional display name for the subaccount
          type: string
        custom_quota:
          description: "an optional manual hourly quota for the subaccount. If not\
            \ specified, the hourly quota will be managed based on reputation"
          type: integer
        status:
          description: the current sending status of the subaccount
          enum:
          - active
          - paused
          type: string
        reputation:
          description: the subaccount's current reputation on a scale from 0 to 100
          maximum: 100
          minimum: 0
          type: integer
        created_at:
          description: the date and time that the subaccount was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        first_sent_at:
          description: the date and time that the subaccount first sent as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        sent_weekly:
          description: "the number of emails the subaccount has sent so far this week\
            \ (weeks start on midnight Monday, UTC)"
          type: integer
        sent_monthly:
          description: "the number of emails the subaccount has sent so far this month\
            \ (months start on midnight of the 1st, UTC)"
          type: integer
        sent_total:
          description: the number of emails the subaccount has sent since it was created
          type: integer
      type: object
    postInboundDeleteRoute_200_response:
      description: the deleted mailbox route information
      example:
        pattern: pattern
        id: id
        url: url
      properties:
        id:
          description: the unique identifier of the route
          type: string
        pattern:
          description: the search pattern that the mailbox name should match
          type: string
        url:
          description: the webhook URL where inbound messages will be published
          type: string
      type: object
    postInboundAddDomain_request:
      properties:
        key:
          description: a valid api key
          type: string
        domain:
          description: "a domain name; Validation: strip_tags, required"
          type: string
      required:
      - domain
      - key
      type: object
    postInboundUpdateRoute_200_response:
      description: the updated mailbox route information
      example:
        pattern: pattern
        id: id
        url: url
      properties:
        id:
          description: the unique identifier of the route
          type: string
        pattern:
          description: the search pattern that the mailbox name should match
          type: string
        url:
          description: the webhook URL where inbound messages will be published
          type: string
      type: object
    postWebhooksAdd_200_response:
      description: the information saved about the new webhook
      example:
        last_sent_at: 2000-01-23T04:56:07.000+00:00
        events_sent: 1
        description: description
        created_at: 2000-01-23T04:56:07.000+00:00
        id: 0
        auth_key: auth_key
        batches_sent: 6
        last_error: last_error
        url: url
        events:
        - send
        - send
      properties:
        id:
          description: a unique integer indentifier for the webhook
          type: integer
        url:
          description: The URL that the event data will be posted to
          type: string
        description:
          description: a description of the webhook
          type: string
        auth_key:
          description: the key used to requests for this webhook
          type: string
        events:
          description: The message events that will be posted to the hook
          items:
            description: the individual message event
            enum:
            - send
            - hard_bounce
            - soft_bounce
            - open
            - click
            - spam
            - unsub
            - reject
            type: string
          type: array
        created_at:
          description: the date and time that the webhook was created as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        last_sent_at:
          description: the date and time that the webhook last successfully received
            events as a UTC string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        batches_sent:
          description: the number of event batches that have ever been sent to this
            webhook
          type: integer
        events_sent:
          description: the total number of events that have ever been sent to this
            webhook
          type: integer
        last_error:
          description: "if we've ever gotten an error trying to post to this webhook,\
            \ the last error that we've seen"
          type: string
      type: object
    postTagsInfo_200_response_stats_last_30_days:
      description: stats with this tag in the last 30 days
      example:
        rejects: 6
        hard_bounces: 2
        unique_clicks: 0
        unsubs: 6
        complaints: 5
        opens: 3
        clicks: 7
        soft_bounces: 6
        sent: 1
        unique_opens: 3
      properties:
        sent:
          description: the number of emails sent with this tag in the last 30 days
          type: integer
        hard_bounces:
          description: the number of emails hard bounced with this tag in the last
            30 days
          type: integer
        soft_bounces:
          description: the number of emails soft bounced with this tag in the last
            30 days
          type: integer
        rejects:
          description: the number of emails rejected for sending this sender in the
            last 30 days
          type: integer
        complaints:
          description: the number of spam complaints with this tag in the last 30
            days
          type: integer
        unsubs:
          description: the number of unsubscribes with this tag in the last 30 days
          type: integer
        opens:
          description: the number of times emails have been opened with this tag in
            the last 30 days
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent with this tag in
            the last 30 days
          type: integer
        clicks:
          description: the number of URLs that have been clicked with this tag in
            the last 30 days
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent with this tag in
            the last 30 days
          type: integer
      type: object
    postTemplatesInfo_request:
      properties:
        key:
          description: a valid api key
          type: string
        name:
          description: the immutable name of an existing template
          type: string
      required:
      - key
      - name
      type: object
    postTemplatesPublish_200_response:
      description: the template that was published
      example:
        code: code
        from_email: from_email
        subject: subject
        publish_from_email: publish_from_email
        created_at: 2000-01-23T04:56:07.000+00:00
        from_name: from_name
        is_broken_template: true
        labels:
        - labels
        - labels
        publish_name: publish_name
        publish_subject: publish_subject
        updated_at: 2000-01-23T04:56:07.000+00:00
        publish_text: publish_text
        name: name
        text: text
        publish_from_name: publish_from_name
        published_at: 2000-01-23T04:56:07.000+00:00
        slug: slug
        publish_code: publish_code
      properties:
        slug:
          description: the immutable unique code name of the template
          type: string
        name:
          description: the name of the template
          type: string
        labels:
          description: the list of labels applied to the template
          items:
            description: a single label
            type: string
          type: array
        code:
          description: "the full HTML code of the template, with mc:edit attributes\
            \ marking the editable elements - draft version"
          type: string
        subject:
          description: "the subject line of the template, if provided - draft version"
          type: string
        from_email:
          description: "the default sender address for the template, if provided -\
            \ draft version"
          type: string
        from_name:
          description: "the default sender from name for the template, if provided\
            \ - draft version"
          type: string
        text:
          description: "the default text part of messages sent with the template,\
            \ if provided - draft version"
          type: string
        publish_name:
          description: the same as the template name - kept as a separate field for
            backwards compatibility
          type: string
        publish_code:
          description: "the full HTML code of the template, with mc:edit attributes\
            \ marking the editable elements that are available as published, if it\
            \ has been published"
          type: string
        publish_subject:
          description: "the subject line of the template, if provided"
          type: string
        publish_from_email:
          description: "the default sender address for the template, if provided"
          format: email
          type: string
        publish_from_name:
          description: "the default sender from name for the template, if provided"
          type: string
        publish_text:
          description: "the default text part of messages sent with the template,\
            \ if provided"
          type: string
        published_at:
          description: "the date and time the template was last published as a UTC\
            \ string in YYYY-MM-DD HH:MM:SS format, or null if it has not been published"
          format: date-time
          type: string
        created_at:
          description: the date and time the template was first created as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        updated_at:
          description: the date and time the template was last modified as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        is_broken_template:
          description: indicates if the template is malformed or corrupt
          type: boolean
      type: object
    postSendersDomains_200_response_inner:
      description: the information on each sending domain for the account
      example:
        valid_signing: true
        verify_txt_key: verify_txt_key
        last_tested_at: 2000-01-23T04:56:07.000+00:00
        dkim:
          valid: true
          valid_after: 2000-01-23T04:56:07.000+00:00
          error: error
        verified_at: 2000-01-23T04:56:07.000+00:00
        domain: domain
        spf:
          valid: true
          valid_after: 2000-01-23T04:56:07.000+00:00
          error: error
        created_at: 2000-01-23T04:56:07.000+00:00
      properties:
        domain:
          description: the sender domain name
          type: string
        created_at:
          description: the date and time that the sending domain was first seen as
            a UTC string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        last_tested_at:
          description: when the domain's DNS settings were last tested as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        spf:
          $ref: '#/components/schemas/postSendersDomains_200_response_inner_spf'
        dkim:
          $ref: '#/components/schemas/postSendersDomains_200_response_inner_dkim'
        verified_at:
          description: "if the domain has been verified, this indicates when that\
            \ verification occurred as a UTC string in YYYY-MM-DD HH:MM:SS format"
          format: date-time
          type: string
        valid_signing:
          description: "whether this domain can be used to authenticate mail, either\
            \ for itself or as a custom signing domain. If this is false but spf and\
            \ dkim are both valid, you will need to verify the domain before using\
            \ it to authenticate mail"
          type: boolean
        verify_txt_key:
          description: a unique key used to verify a domain by adding a TXT record.
            Append this key to 'mandrill_verify.' and add it to your domain's TXT
            records to verify
          type: string
      type: object
    postMessagesParse_200_response_images_inner:
      description: information about an individual image
      example:
        name: name
        type: type
        content: content
      properties:
        name:
          description: the Content-ID of the embedded image
          type: string
        type:
          description: the MIME type of the image
          type: string
        content:
          description: the content of the image as a base64 encoded string
          type: string
      type: object
    postExportsWhitelist_200_response:
      description: information about the allowlist export job that was started
      example:
        result_url: result_url
        finished_at: 2000-01-23T04:56:07.000+00:00
        created_at: 2000-01-23T04:56:07.000+00:00
        id: id
        state: state
        type: type
      properties:
        id:
          description: the unique identifier for this Export. Use this identifier
            when checking the export job's status
          type: string
        created_at:
          description: the date and time that the export job was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        type:
          description: the type of the export job
          type: string
        finished_at:
          description: "the date and time that the export job was finished as a UTC\
            \ string in YYYY-MM-DD HH:MM:SS format, or null for jobs that have not\
            \ run"
          format: date-time
          type: string
        state:
          description: the export job's state
          type: string
        result_url:
          description: "the url for the export job's results, if the job is complete"
          type: string
      type: object
    postSubaccountsPause_request:
      properties:
        key:
          description: a valid api key
          type: string
        id:
          description: the unique identifier of the subaccount to pause
          type: string
      required:
      - id
      - key
      type: object
    postSubaccountsList_request:
      properties:
        key:
          description: a valid api key
          type: string
        q:
          description: an optional prefix to filter the subaccounts' ids and names
          type: string
      required:
      - key
      type: object
    postSubaccountsInfo_request:
      properties:
        key:
          description: a valid api key
          type: string
        id:
          description: the unique identifier of the subaccount to query
          type: string
      required:
      - id
      - key
      type: object
    postUrlsTrackingDomains_200_response_inner:
      description: the individual tracking domain
      example:
        last_tested_at: 2000-01-23T04:56:07.000+00:00
        valid_tracking: true
        domain: domain
        cname:
          valid: true
          valid_after: valid_after
          error: error
        created_at: 2000-01-23T04:56:07.000+00:00
      properties:
        domain:
          description: the tracking domain name
          type: string
        created_at:
          description: the date and time that the tracking domain was added as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        last_tested_at:
          description: when the domain's DNS settings were last tested as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        cname:
          $ref: '#/components/schemas/postUrlsTrackingDomains_200_response_inner_cname'
        valid_tracking:
          description: whether this domain can be used as a tracking domain for email.
          type: boolean
      type: object
    postIpsProvision_request:
      properties:
        key:
          description: a valid api key
          type: string
        warmup:
          description: whether to enable warmup mode for the ip
          type: boolean
        pool:
          description: "the id of the pool to add the dedicated ip to, or null to\
            \ use your account's default pool"
          type: string
      required:
      - key
      type: object
    postUrlsList_200_response_inner:
      description: the individual URL stats
      example:
        unique_clicks: 1
        clicks: 6
        sent: 0
        url: url
      properties:
        url:
          description: the URL to be tracked
          type: string
        sent:
          description: the number of emails that contained the URL
          type: integer
        clicks:
          description: the number of times the URL has been clicked from a tracked
            email
          type: integer
        unique_clicks:
          description: the number of unique emails that have generated clicks for
            this URL
          type: integer
      type: object
    postIpsProvision_200_response:
      description: a description of the provisioning request that was created
      example:
        requested_at: 2000-01-23T04:56:07.000+00:00
      properties:
        requested_at:
          description: the date and time that the request was created as a UTC timestamp
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
      type: object
    postIpsDeletePool_request:
      properties:
        key:
          description: a valid api key
          type: string
        pool:
          description: the name of the pool to delete
          type: string
      required:
      - key
      - pool
      type: object
    postMessagesSearch_request:
      properties:
        key:
          description: a valid api key
          type: string
        query:
          description: search terms to find matching messages
          type: string
        date_from:
          description: start date
          format: date-time
          type: string
        date_to:
          description: end date
          format: date-time
          type: string
        tags:
          description: "an array of tag names to narrow the search to, will return\
            \ messages that contain ANY of the tags"
          items:
            type: string
          type: array
        senders:
          description: "an array of sender addresses to narrow the search to, will\
            \ return messages sent by ANY of the senders"
          items:
            description: a single sender email address
            format: email
            type: string
          type: array
        api_keys:
          description: "an array of API keys to narrow the search to, will return\
            \ messages sent by ANY of the keys"
          items:
            description: a single API key
            type: string
          type: array
        limit:
          description: "the maximum number of results to return, defaults to 100,\
            \ 1000 is the maximum"
          maximum: 1000
          minimum: 100
          type: integer
      required:
      - key
      type: object
    postTemplatesRender_request:
      properties:
        key:
          description: a valid api key
          type: string
        template_name:
          description: the immutable name of a template that exists in the user's
            account
          type: string
        template_content:
          description: "an array of template content to render. Each item in the array\
            \ should be a struct with two keys - name: the name of the content block\
            \ to set the content for, and content: the actual content to put into\
            \ the block"
          items:
            $ref: '#/components/schemas/postTemplatesRender_request_template_content_inner'
          type: array
        merge_vars:
          description: "optional merge variables to use for injecting merge field\
            \ content. If this is not provided, no merge fields will be replaced."
          items:
            $ref: '#/components/schemas/postTemplatesRender_request_merge_vars_inner'
          type: array
      required:
      - key
      - template_content
      - template_name
      type: object
    postMessagesSend_request_message_merge_vars_inner:
      description: per-recipient merge variables
      properties:
        rcpt:
          description: the email address of the recipient that the merge variables
            should apply to
          format: email
          type: string
        vars:
          description: the recipient's merge variables
          items:
            $ref: '#/components/schemas/postMessagesSend_request_message_merge_vars_inner_vars_inner'
          type: array
      required:
      - rcpt
      type: object
    postExportsRejects_200_response:
      description: information about the rejects export job that was started
      example:
        result_url: result_url
        finished_at: 2000-01-23T04:56:07.000+00:00
        created_at: 2000-01-23T04:56:07.000+00:00
        id: id
        state: state
        type: type
      properties:
        id:
          description: the unique identifier for this Export. Use this identifier
            when checking the export job's status
          type: string
        created_at:
          description: the date and time that the export job was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        type:
          description: "the type of the export job - activity, reject, or whitelist"
          type: string
        finished_at:
          description: the date and time that the export job was finished as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        state:
          description: "the export job's state - waiting, working, complete, error,\
            \ or expired."
          type: string
        result_url:
          description: "the url for the export job's results, if the job is completed."
          type: string
      type: object
    postMessagesSend_request_message_to_inner:
      description: a single recipient's information
      properties:
        email:
          description: the email address of the recipient
          format: email
          type: string
        name:
          description: the optional display name to use for the recipient
          type: string
        type:
          description: "the header type to use for the recipient, defaults to \"to\"\
            \ if not provided"
          enum:
          - to
          - cc
          - bcc
          type: string
      required:
      - email
      type: object
    postAllowlistsList_request:
      properties:
        key:
          description: a valid api key
          type: string
        email:
          description: an optional email address or prefix to search by
          format: email
          type: string
      required:
      - key
      type: object
    postSubaccountsResume_200_response:
      description: the information for the resumed subaccount
      example:
        custom_quota: 0
        name: name
        first_sent_at: 2000-01-23T04:56:07.000+00:00
        reputation: 60
        created_at: 2000-01-23T04:56:07.000+00:00
        sent_total: 5
        id: id
        sent_monthly: 5
        sent_weekly: 1
        status: active
      properties:
        id:
          description: a unique indentifier for the subaccount
          type: string
        name:
          description: an optional display name for the subaccount
          type: string
        custom_quota:
          description: "an optional manual hourly quota for the subaccount. If not\
            \ specified, the hourly quota will be managed based on reputation"
          type: integer
        status:
          description: the current sending status of the subaccount
          enum:
          - active
          - paused
          type: string
        reputation:
          description: the subaccount's current reputation on a scale from 0 to 100
          maximum: 100
          minimum: 0
          type: integer
        created_at:
          description: the date and time that the subaccount was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        first_sent_at:
          description: the date and time that the subaccount first sent as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        sent_weekly:
          description: "the number of emails the subaccount has sent so far this week\
            \ (weeks start on midnight Monday, UTC)"
          type: integer
        sent_monthly:
          description: "the number of emails the subaccount has sent so far this month\
            \ (months start on midnight of the 1st, UTC)"
          type: integer
        sent_total:
          description: the number of emails the subaccount has sent since it was created
          type: integer
      type: object
    postMessagesReschedule_request:
      properties:
        key:
          description: a valid api key
          type: string
        id:
          description: "a scheduled email id, as returned by any of the messages/send\
            \ calls or messages/list-scheduled"
          type: string
        send_at:
          description: "the new UTC timestamp when the message should sent. Mandrill\
            \ can't time travel, so if you specify a time in past the message will\
            \ be sent immediately; for future dates, you're limited to one year from\
            \ the date of scheduling."
          format: date-time
          type: string
      required:
      - id
      - key
      - send_at
      type: object
    postSubaccountsAdd_request:
      properties:
        key:
          description: a valid api key
          type: string
        id:
          description: a unique identifier for the subaccount to be used in sending
            calls
          maxLength: 255
          type: string
        name:
          description: an optional display name to further identify the subaccount
          maxLength: 1024
          type: string
        notes:
          description: optional extra text to associate with the subaccount
          type: string
        custom_quota:
          description: "an optional manual hourly quota for the subaccount. If not\
            \ specified, Mandrill will manage this based on reputation"
          minimum: 0
          type: integer
      required:
      - id
      - key
      type: object
    postAllowlistsDelete_request:
      properties:
        key:
          description: a valid api key
          type: string
        email:
          description: the email address to remove from the allowlist
          format: email
          type: string
      required:
      - email
      - key
      type: object
    postUrlsCheckTrackingDomain_request:
      properties:
        key:
          description: a valid api key
          type: string
        domain:
          description: an existing tracking domain name
          type: string
      required:
      - domain
      - key
      type: object
    postAllowlistsList_200_response_inner:
      description: the information for each allowlist entry
      example:
        created_at: 2000-01-23T04:56:07.000+00:00
        detail: detail
        email: email
      properties:
        email:
          description: the email that is allowlisted
          format: email
          type: string
        detail:
          description: a description of why the email was allowlisted
          type: string
        created_at:
          description: when the email was added to the allowlist
          format: date-time
          type: string
      type: object
    postUrlsTrackingDomains_200_response_inner_cname:
      description: details about the domain's CNAME record
      example:
        valid: true
        valid_after: valid_after
        error: error
      properties:
        valid:
          description: whether the domain's CNAME record is valid for use with Mandrill
          type: boolean
        valid_after:
          description: "when the domain's CNAME record will be considered valid for\
            \ use with Mandrill as a UTC string in YYYY-MM-DD HH:MM:SS format. If\
            \ set, this indicates that the record is valid now, but was previously\
            \ invalid, and Mandrill will wait until the record's TTL elapses to start\
            \ using it."
          type: string
        error:
          description: "an error describing the CNAME record, or null if the record\
            \ is correct"
          type: string
      type: object
    postIpsStartWarmup_request:
      properties:
        key:
          description: a valid api key
          type: string
        ip:
          description: a dedicated ip address
          type: string
      required:
      - ip
      - key
      type: object
    postTagsInfo_200_response_stats:
      description: an aggregate summary of the tag's sending stats
      example:
        last_7_days:
          rejects: 9
          hard_bounces: 5
          unique_clicks: 6
          unsubs: 8
          complaints: 6
          opens: 9
          clicks: 3
          soft_bounces: 9
          sent: 4
          unique_opens: 6
        today:
          rejects: 7
          hard_bounces: 2
          unique_clicks: 1
          unsubs: 1
          complaints: 1
          opens: 1
          clicks: 7
          soft_bounces: 4
          sent: 3
          unique_opens: 6
        last_90_days:
          rejects: 6
          hard_bounces: 4
          unique_clicks: 0
          unsubs: 1
          complaints: 4
          opens: 4
          clicks: 9
          soft_bounces: 0
          sent: 3
          unique_opens: 7
        last_30_days:
          rejects: 6
          hard_bounces: 2
          unique_clicks: 0
          unsubs: 6
          complaints: 5
          opens: 3
          clicks: 7
          soft_bounces: 6
          sent: 1
          unique_opens: 3
        last_60_days:
          rejects: 4
          hard_bounces: 6
          unique_clicks: 5
          unsubs: 7
          complaints: 8
          opens: 3
          clicks: 7
          soft_bounces: 0
          sent: 7
          unique_opens: 3
      properties:
        today:
          $ref: '#/components/schemas/postTagsInfo_200_response_stats_today'
        last_7_days:
          $ref: '#/components/schemas/postTagsInfo_200_response_stats_last_7_days'
        last_30_days:
          $ref: '#/components/schemas/postTagsInfo_200_response_stats_last_30_days'
        last_60_days:
          $ref: '#/components/schemas/postTagsInfo_200_response_stats_last_60_days'
        last_90_days:
          $ref: '#/components/schemas/postTagsInfo_200_response_stats_last_90_days'
      type: object
    postUrlsTimeSeries_200_response_inner:
      description: the information for a single hour
      example:
        unique_clicks: 1
        clicks: 6
        time: 2000-01-23T04:56:07.000+00:00
        sent: 0
      properties:
        time:
          description: the hour as a UTC date string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        sent:
          description: the number of emails that were sent with the URL during the
            hour
          type: integer
        clicks:
          description: the number of times the URL was clicked during the hour
          type: integer
        unique_clicks:
          description: the number of unique clicks generated for emails sent with
            this URL during the hour
          type: integer
      type: object
    postIpsDelete_200_response:
      description: a description of the ip that was removed from your account.
      example:
        deleted: deleted
        ip: ip
      properties:
        ip:
          description: the ip address
          type: string
        deleted:
          description: a boolean indicating whether the ip was successfully deleted
          type: string
      type: object
    postUsersInfo_200_response_stats_last_30_days:
      description: stats for this user in the last 30 days
      example:
        rejects: 3
        hard_bounces: 9
        unique_clicks: 5
        unsubs: 1
        complaints: 6
        opens: 2
        clicks: 6
        soft_bounces: 6
        sent: 8
        unique_opens: 6
      properties:
        sent:
          description: the number of emails sent for this user in the last 30 days
          type: integer
        hard_bounces:
          description: the number of emails hard bounced for this user in the last
            30 days
          type: integer
        soft_bounces:
          description: the number of emails soft bounced for this user in the last
            30 days
          type: integer
        rejects:
          description: the number of emails rejected for sending this sender in the
            last 30 days
          type: integer
        complaints:
          description: the number of spam complaints for this user in the last 30
            days
          type: integer
        unsubs:
          description: the number of unsubscribes for this user in the last 30 days
          type: integer
        opens:
          description: the number of times emails have been opened for this user in
            the last 30 days
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent for this user in
            the last 30 days
          type: integer
        clicks:
          description: the number of URLs that have been clicked for this user in
            the last 30 days
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent for this user in
            the last 30 days
          type: integer
      type: object
    postUsersInfo_200_response_stats:
      description: an aggregate summary of the account's sending stats
      example:
        last_7_days:
          rejects: 7
          hard_bounces: 1
          unique_clicks: 6
          unsubs: 4
          complaints: 1
          opens: 5
          clicks: 9
          soft_bounces: 6
          sent: 1
          unique_opens: 9
        all_time:
          rejects: 9
          hard_bounces: 4
          unique_clicks: 4
          unsubs: 9
          complaints: 0
          opens: 0
          clicks: 5
          soft_bounces: 7
          sent: 1
          unique_opens: 7
        today:
          rejects: 7
          hard_bounces: 5
          unique_clicks: 1
          unsubs: 3
          complaints: 9
          opens: 2
          clicks: 7
          soft_bounces: 2
          sent: 5
          unique_opens: 4
        last_90_days:
          rejects: 7
          hard_bounces: 3
          unique_clicks: 4
          unsubs: 3
          complaints: 5
          opens: 4
          clicks: 6
          soft_bounces: 3
          sent: 7
          unique_opens: 0
        last_30_days:
          rejects: 3
          hard_bounces: 9
          unique_clicks: 5
          unsubs: 1
          complaints: 6
          opens: 2
          clicks: 6
          soft_bounces: 6
          sent: 8
          unique_opens: 6
        last_60_days:
          rejects: 7
          hard_bounces: 3
          unique_clicks: 8
          unsubs: 7
          complaints: 0
          opens: 6
          clicks: 4
          soft_bounces: 3
          sent: 6
          unique_opens: 0
      properties:
        today:
          $ref: '#/components/schemas/postUsersInfo_200_response_stats_today'
        last_7_days:
          $ref: '#/components/schemas/postUsersInfo_200_response_stats_last_7_days'
        last_30_days:
          $ref: '#/components/schemas/postUsersInfo_200_response_stats_last_30_days'
        last_60_days:
          $ref: '#/components/schemas/postUsersInfo_200_response_stats_last_60_days'
        last_90_days:
          $ref: '#/components/schemas/postUsersInfo_200_response_stats_last_90_days'
        all_time:
          $ref: '#/components/schemas/postUsersInfo_200_response_stats_all_time'
      type: object
    postTemplatesTimeSeries_request:
      properties:
        key:
          description: a valid api key
          type: string
        name:
          description: the name of an existing template
          type: string
      required:
      - key
      - name
      type: object
    postSendersInfo_200_response:
      description: the detailed information on the sender
      example:
        rejects: 5
        address: address
        hard_bounces: 6
        stats:
          last_7_days:
            rejects: 9
            hard_bounces: 5
            unique_clicks: 6
            unsubs: 8
            complaints: 6
            opens: 9
            clicks: 3
            soft_bounces: 9
            sent: 4
            unique_opens: 6
          today:
            rejects: 7
            hard_bounces: 2
            unique_clicks: 1
            unsubs: 1
            complaints: 1
            opens: 1
            clicks: 7
            soft_bounces: 4
            sent: 3
            unique_opens: 6
          last_90_days:
            rejects: 6
            hard_bounces: 4
            unique_clicks: 0
            unsubs: 1
            complaints: 4
            opens: 4
            clicks: 9
            soft_bounces: 0
            sent: 3
            unique_opens: 7
          last_30_days:
            rejects: 6
            hard_bounces: 2
            unique_clicks: 0
            unsubs: 6
            complaints: 5
            opens: 3
            clicks: 7
            soft_bounces: 6
            sent: 1
            unique_opens: 3
          last_60_days:
            rejects: 4
            hard_bounces: 6
            unique_clicks: 5
            unsubs: 7
            complaints: 8
            opens: 3
            clicks: 7
            soft_bounces: 0
            sent: 7
            unique_opens: 3
        unsubs: 2
        complaints: 5
        created_at: 2000-01-23T04:56:07.000+00:00
        opens: 7
        clicks: 9
        soft_bounces: 1
        sent: 0
      properties:
        address:
          description: the sender's email address
          format: email
          type: string
        created_at:
          description: the date and time that the sender was first seen by Mandrill
            as a UTC date string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        sent:
          description: the total number of messages sent by this sender
          type: integer
        hard_bounces:
          description: the total number of hard bounces by messages by this sender
          type: integer
        soft_bounces:
          description: the total number of soft bounces by messages by this sender
          type: integer
        rejects:
          description: the total number of rejected messages by this sender
          type: integer
        complaints:
          description: the total number of spam complaints received for messages by
            this sender
          type: integer
        unsubs:
          description: the total number of unsubscribe requests received for messages
            by this sender
          type: integer
        opens:
          description: the total number of times messages by this sender have been
            opened
          type: integer
        clicks:
          description: the total number of times tracked URLs in messages by this
            sender have been clicked
          type: integer
        stats:
          $ref: '#/components/schemas/postSendersInfo_200_response_stats'
      type: object
    postIpsList_200_response_inner_custom_dns:
      description: "information about the ip's custom dns, if it has been configured"
      example:
        valid: true
        error: error
        enabled: true
      properties:
        enabled:
          description: a boolean indicating whether custom dns has been configured
            for this ip
          type: boolean
        valid:
          description: whether the ip's custom dns is currently valid
          type: boolean
        error:
          description: "if the ip's custom dns is invalid, this will include details\
            \ about the error"
          type: string
      type: object
    postIpsPoolInfo_200_response:
      description: Information about the dedicated ip pool
      example:
        name: name
        created_at: 2000-01-23T04:56:07.000+00:00
        ips:
        - custom_dns:
            valid: true
            error: error
            enabled: true
          ip: ip
          domain: domain
          pool: pool
          created_at: 2000-01-23T04:56:07.000+00:00
          warmup:
            end_at: 2000-01-23T04:56:07.000+00:00
            warming_up: true
            start_at: 2000-01-23T04:56:07.000+00:00
        - custom_dns:
            valid: true
            error: error
            enabled: true
          ip: ip
          domain: domain
          pool: pool
          created_at: 2000-01-23T04:56:07.000+00:00
          warmup:
            end_at: 2000-01-23T04:56:07.000+00:00
            warming_up: true
            start_at: 2000-01-23T04:56:07.000+00:00
      properties:
        name:
          description: this pool's name
          type: string
        created_at:
          description: the date and time that this pool was created as a UTC timestamp
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        ips:
          description: the dedicated IPs in this pool
          items:
            $ref: '#/components/schemas/postIpsListPools_200_response_inner_ips_inner'
          type: array
      type: object
    postSubaccountsAdd_200_response:
      description: the information saved about the new subaccount
      example:
        custom_quota: 0
        name: name
        first_sent_at: 2000-01-23T04:56:07.000+00:00
        reputation: 60
        created_at: 2000-01-23T04:56:07.000+00:00
        sent_total: 5
        id: id
        sent_monthly: 5
        sent_weekly: 1
        status: active
      properties:
        id:
          description: a unique indentifier for the subaccount
          type: string
        name:
          description: an optional display name for the subaccount
          type: string
        custom_quota:
          description: "an optional manual hourly quota for the subaccount. If not\
            \ specified, the hourly quota will be managed based on reputation"
          type: integer
        status:
          description: the current sending status of the subaccount
          enum:
          - active
          - paused
          type: string
        reputation:
          description: the subaccount's current reputation on a scale from 0 to 100
          maximum: 100
          minimum: 0
          type: integer
        created_at:
          description: the date and time that the subaccount was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        first_sent_at:
          description: the date and time that the subaccount first sent as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        sent_weekly:
          description: "the number of emails the subaccount has sent so far this week\
            \ (weeks start on midnight Monday, UTC)"
          type: integer
        sent_monthly:
          description: "the number of emails the subaccount has sent so far this month\
            \ (months start on midnight of the 1st, UTC)"
          type: integer
        sent_total:
          description: the number of emails the subaccount has sent since it was created
          type: integer
      type: object
    postInboundSendRaw_200_response_inner:
      description: the individual recipient information
      example:
        pattern: pattern
        email: email
        url: url
      properties:
        email:
          description: the email address of the matching recipient
          type: string
        pattern:
          description: the mailbox route pattern that the recipient matched
          type: string
        url:
          description: the webhook URL that the message was posted to
          type: string
      type: object
    postMessagesSendTemplate_200_response_inner:
      description: the sending results for a single recipient
      example:
        reject_reason: hard-bounce
        _id: _id
        queued_reason: attachments
        email: email
        status: sent
      properties:
        email:
          description: the email address of the recipient
          format: email
          type: string
        status:
          description: the sending status of the recipient
          enum:
          - sent
          - queued
          - rejected
          - invalid
          type: string
        reject_reason:
          description: the reason for the rejection if the recipient status is "rejected"
          enum:
          - hard-bounce
          - soft-bounce
          - spam
          - unsub
          - custom
          - invalid-sender
          - invalid
          - test-mode-limit
          - unsigned
          - rule
          type: string
        queued_reason:
          description: the reason for the email being queued if the response status
            is "queued"
          enum:
          - attachments
          - multiple-recipients
          - free-trial-sends-exhausted
          - hourly-quota-exhausted
          - monthly-limit-reached
          - sending-paused
          - sending-suspended
          - account-suspended
          - sending-backlogged
          type: string
        _id:
          description: the message's unique id
          type: string
      type: object
    postUsersInfo_200_response_stats_today:
      description: stats for this user so far today
      example:
        rejects: 7
        hard_bounces: 5
        unique_clicks: 1
        unsubs: 3
        complaints: 9
        opens: 2
        clicks: 7
        soft_bounces: 2
        sent: 5
        unique_opens: 4
      properties:
        sent:
          description: the number of emails sent for this user so far today
          type: integer
        hard_bounces:
          description: the number of emails hard bounced for this user so far today
          type: integer
        soft_bounces:
          description: the number of emails soft bounced for this user so far today
          type: integer
        rejects:
          description: the number of emails rejected for sending this sender so far
            today
          type: integer
        complaints:
          description: the number of spam complaints for this user so far today
          type: integer
        unsubs:
          description: the number of unsubscribes for this user so far today
          type: integer
        opens:
          description: the number of times emails have been opened for this user so
            far today
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent for this user so
            far today
          type: integer
        clicks:
          description: the number of URLs that have been clicked for this user so
            far today
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent for this user so
            far today
          type: integer
      type: object
    postMessagesInfo_request:
      properties:
        key:
          description: a valid api key
          type: string
        id:
          description: "the unique id of the message to get - passed as the \"_id\"\
            \ field in webhooks, send calls, or search calls"
          type: string
      required:
      - id
      - key
      type: object
    postUrlsSearchDeprecated_request:
      properties:
        key:
          description: a valid api key
          type: string
        q:
          description: a search query
          type: string
      required:
      - key
      - q
      type: object
    postRejectsDelete_200_response:
      description: a status object containing the address and whether the deletion
        succeeded.
      example:
        deleted: true
        subaccount: subaccount
        email: email
      properties:
        email:
          description: the email address that was removed from the denylist
          format: email
          type: string
        deleted:
          description: whether the address was deleted successfully.
          type: boolean
        subaccount:
          description: "the subaccount denylist that the address was removed from,\
            \ if any"
          type: string
      type: object
    postAllowlistsAdd_200_response:
      description: a status object containing the address and the result of the operation
      example:
        added: true
        email: email
      properties:
        email:
          description: the email address you provided
          type: string
        added:
          description: whether the operation succeeded
          type: boolean
      type: object
    postSendersCheckDomain_200_response:
      description: information about the sender domain
      example:
        valid_signing: true
        verify_txt_key: verify_txt_key
        last_tested_at: 2000-01-23T04:56:07.000+00:00
        dkim:
          valid: true
          valid_after: 2000-01-23T04:56:07.000+00:00
          error: error
        verified_at: 2000-01-23T04:56:07.000+00:00
        domain: domain
        spf:
          valid: true
          valid_after: 2000-01-23T04:56:07.000+00:00
          error: error
        created_at: 2000-01-23T04:56:07.000+00:00
      properties:
        domain:
          description: the sender domain name
          type: string
        created_at:
          description: the date and time that the sending domain was first seen as
            a UTC string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        last_tested_at:
          description: when the domain's DNS settings were last tested as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        spf:
          $ref: '#/components/schemas/postSendersDomains_200_response_inner_spf'
        dkim:
          $ref: '#/components/schemas/postSendersDomains_200_response_inner_dkim'
        verified_at:
          description: "if the domain has been verified, this indicates when that\
            \ verification occurred as a UTC string in YYYY-MM-DD HH:MM:SS format"
          format: date-time
          type: string
        valid_signing:
          description: "whether this domain can be used to authenticate mail, either\
            \ for itself or as a custom signing domain. If this is false but spf and\
            \ dkim are both valid, you will need to verify the domain before using\
            \ it to authenticate mail"
          type: boolean
        verify_txt_key:
          description: a unique key used to verify a domain by adding a TXT record.
            Append this key to 'mandrill_verify.' and add it to your domain's TXT
            records to verify
          type: string
      type: object
    postInboundCheckDomain_request:
      properties:
        key:
          description: a valid api key
          type: string
        domain:
          description: an existing inbound domain
          type: string
      required:
      - domain
      - key
      type: object
    postExportsList_200_response_inner:
      description: the individual export info
      example:
        result_url: result_url
        finished_at: 2000-01-23T04:56:07.000+00:00
        created_at: 2000-01-23T04:56:07.000+00:00
        id: id
        state: state
        type: type
      properties:
        id:
          description: the unique identifier for this Export. Use this identifier
            when checking the export job's status
          type: string
        created_at:
          description: the date and time that the export job was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        type:
          description: "the type of the export job - activity, reject, or allowlist"
          type: string
        finished_at:
          description: the date and time that the export job was finished as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        state:
          description: "the export job's state - waiting, working, complete, error,\
            \ or expired."
          type: string
        result_url:
          description: "the url for the export job's results, if the job is completed."
          type: string
      type: object
    postMetadataDelete_request:
      properties:
        key:
          description: a valid api key
          type: string
        name:
          description: the unique identifier of the metadata field to update
          type: string
      required:
      - key
      - name
      type: object
    postTemplatesAdd_request:
      properties:
        key:
          description: a valid api key
          type: string
        name:
          description: the name for the new template - must be unique
          type: string
        from_email:
          description: a default sending address for emails sent using this template
          format: email
          type: string
        from_name:
          description: a default from name to be used
          type: string
        subject:
          description: a default subject line to be used
          type: string
        code:
          description: the HTML code for the template with mc:edit attributes for
            the editable elements
          type: string
        text:
          description: a default text part to be used when sending with this template
          type: string
        publish:
          description: set to false to add a draft template without publishing
          type: boolean
        labels:
          description: an optional array of up to 10 labels to use for filtering templates
          items:
            description: a single label
            maxLength: 100
            type: string
          type: array
      required:
      - key
      - name
      type: object
    postUsersInfo_200_response_stats_last_90_days:
      description: stats for this user in the last 90 days
      example:
        rejects: 7
        hard_bounces: 3
        unique_clicks: 4
        unsubs: 3
        complaints: 5
        opens: 4
        clicks: 6
        soft_bounces: 3
        sent: 7
        unique_opens: 0
      properties:
        sent:
          description: the number of emails sent for this user in the last 90 days
          type: integer
        hard_bounces:
          description: the number of emails hard bounced for this user in the last
            90 days
          type: integer
        soft_bounces:
          description: the number of emails soft bounced for this user in the last
            90 days
          type: integer
        rejects:
          description: the number of emails rejected for sending this sender in the
            last 90 days
          type: integer
        complaints:
          description: the number of spam complaints for this user in the last 90
            days
          type: integer
        unsubs:
          description: the number of unsubscribes for this user in the last 90 days
          type: integer
        opens:
          description: the number of times emails have been opened for this user in
            the last 90 days
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent for this user in
            the last 90 days
          type: integer
        clicks:
          description: the number of URLs that have been clicked for this user in
            the last 90 days
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent for this user in
            the last 90 days
          type: integer
      type: object
    postInboundAddRoute_200_response:
      description: the added mailbox route information
      example:
        pattern: pattern
        id: id
        url: url
      properties:
        id:
          description: the unique identifier of the route
          type: string
        pattern:
          description: the search pattern that the mailbox name should match
          type: string
        url:
          description: the webhook URL where inbound messages will be published
          type: string
      type: object
    postRejectsAdd_request:
      properties:
        key:
          description: a valid api key
          type: string
        email:
          description: an email address to block
          format: email
          type: string
        comment:
          description: an optional comment describing the rejection
          type: string
        subaccount:
          description: an optional unique identifier for the subaccount to limit the
            denylist entry
          maxLength: 255
          type: string
      required:
      - email
      - key
      type: object
    postIpsSetPool_200_response:
      description: Information about the updated state of the dedicated IP
      example:
        custom_dns:
          valid: true
          error: error
          enabled: true
        ip: ip
        domain: domain
        pool: pool
        created_at: 2000-01-23T04:56:07.000+00:00
        warmup:
          end_at: 2000-01-23T04:56:07.000+00:00
          warming_up: true
          start_at: 2000-01-23T04:56:07.000+00:00
      properties:
        ip:
          description: the ip address
          type: string
        created_at:
          description: the date and time that the dedicated IP was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        pool:
          description: the name of the pool that this dedicated IP belongs to
          type: string
        domain:
          description: the domain name (reverse dns) of this dedicated IP
          type: string
        custom_dns:
          $ref: '#/components/schemas/postIpsList_200_response_inner_custom_dns'
        warmup:
          $ref: '#/components/schemas/postIpsList_200_response_inner_warmup'
      type: object
    postWebhooksDelete_200_response:
      description: the information for the deleted webhook
      example:
        last_sent_at: 2000-01-23T04:56:07.000+00:00
        events_sent: 1
        description: description
        created_at: 2000-01-23T04:56:07.000+00:00
        id: 0
        auth_key: auth_key
        batches_sent: 6
        last_error: last_error
        url: url
        events:
        - send
        - send
      properties:
        id:
          description: a unique integer indentifier for the webhook
          type: integer
        url:
          description: The URL that the event data will be posted to
          type: string
        description:
          description: a description of the webhook
          type: string
        auth_key:
          description: the key used to requests for this webhook
          type: string
        events:
          description: The message events that will be posted to the hook
          items:
            description: the individual message event
            enum:
            - send
            - hard_bounce
            - soft_bounce
            - open
            - click
            - spam
            - unsub
            - reject
            type: string
          type: array
        created_at:
          description: the date and time that the webhook was created as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        last_sent_at:
          description: the date and time that the webhook last successfully received
            events as a UTC string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        batches_sent:
          description: the number of event batches that have ever been sent to this
            webhook
          type: integer
        events_sent:
          description: the total number of events that have ever been sent to this
            webhook
          type: integer
        last_error:
          description: "if we've ever gotten an error trying to post to this webhook,\
            \ the last error that we've seen"
          type: string
      type: object
    postMessagesInfo_200_response:
      description: the information for the message
      example:
        template: template
        metadata: "{}"
        subject: subject
        clicks_detail:
        - ip: ip
          location: location
          ua: ua
          url: url
          ts: 5
        - ip: ip
          location: location
          ua: ua
          url: url
          ts: 5
        tags:
        - tags
        - tags
        opens_detail:
        - ip: ip
          location: location
          ua: ua
          ts: 1
        - ip: ip
          location: location
          ua: ua
          ts: 1
        sender: sender
        opens: 6
        clicks: 1
        _id: _id
        state: sent
        smtp_events:
        - diag: diag
          type: type
          ts: 5
        - diag: diag
          type: type
          ts: 5
        email: email
        ts: 0
      properties:
        ts:
          description: the Unix timestamp from when this message was sent
          type: integer
        _id:
          description: the message's unique id
          type: string
        sender:
          description: the email address of the sender
          format: email
          type: string
        template:
          description: "the unique name of the template used, if any"
          type: string
        subject:
          description: the message's subject line
          type: string
        email:
          description: the recipient email address
          format: email
          type: string
        tags:
          description: list of tags on this message
          items:
            description: individual tag on this message
            type: string
          type: array
        opens:
          description: how many times has this message been opened
          type: integer
        opens_detail:
          description: list of individual opens for the message
          items:
            $ref: '#/components/schemas/postMessagesSearch_200_response_inner_opens_detail_inner'
          type: array
        clicks:
          description: how many times has a link been clicked in this message
          type: integer
        clicks_detail:
          description: list of individual clicks for the message
          items:
            $ref: '#/components/schemas/postMessagesSearch_200_response_inner_clicks_detail_inner'
          type: array
        state:
          description: sending status of this message
          enum:
          - sent
          - bounced
          - rejected
          type: string
        metadata:
          description: any custom metadata provided when the message was sent
          properties: {}
          type: object
        smtp_events:
          description: a log of up to 3 smtp events for the message
          items:
            $ref: '#/components/schemas/postMessagesInfo_200_response_smtp_events_inner'
          type: array
      type: object
    postRejectsDelete_request:
      properties:
        key:
          description: a valid api key
          type: string
        email:
          description: an email address
          format: email
          type: string
        subaccount:
          description: an optional unique identifier for the subaccount to limit the
            denylist deletion
          maxLength: 255
          type: string
      required:
      - email
      - key
      type: object
    postIpsInfo_request:
      properties:
        key:
          description: a valid api key
          type: string
        ip:
          description: a dedicated IP address
          type: string
      required:
      - key
      type: object
    postSendersInfo_200_response_stats_last_60_days:
      description: stats for this sender in the last 60 days
      example:
        rejects: 4
        hard_bounces: 6
        unique_clicks: 5
        unsubs: 7
        complaints: 8
        opens: 3
        clicks: 7
        soft_bounces: 0
        sent: 7
        unique_opens: 3
      properties:
        sent:
          description: the number of emails sent for this sender in the last 60 days
          type: integer
        hard_bounces:
          description: the number of emails hard bounced for this sender in the last
            60 days
          type: integer
        soft_bounces:
          description: the number of emails soft bounced for this sender in the last
            60 days
          type: integer
        rejects:
          description: the number of emails rejected for sending this sender in the
            last 60 days
          type: integer
        complaints:
          description: the number of spam complaints for this sender in the last 60
            days
          type: integer
        unsubs:
          description: the number of unsubscribes for this sender in the last 60 days
          type: integer
        opens:
          description: the number of times emails have been opened for this sender
            in the last 60 days
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent for this sender
            in the last 60 days
          type: integer
        clicks:
          description: the number of URLs that have been clicked for this sender in
            the last 60 days
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent for this sender
            in the last 60 days
          type: integer
      type: object
    postMetadataUpdate_200_response:
      description: the information for the updated metadata field
      example:
        name: name
        state: active
        view_template: view_template
      properties:
        name:
          description: the unique identifier of the metadata field to update
          type: string
        state:
          description: the current state of the metadata field
          enum:
          - active
          - delete
          - index
          type: string
        view_template:
          description: Mustache template to control how the metadata is rendered in
            your activity log
          type: string
      type: object
    postWebhooksInfo_request:
      properties:
        key:
          description: a valid api key
          type: string
        id:
          description: the unique identifier of a webhook belonging to this account
          type: integer
      required:
      - id
      - key
      type: object
    postUsersInfo_200_response:
      description: "the user information including username, key, reputation, quota,\
        \ and historical sending stats"
      example:
        hourly_quota: 6
        backlog: 1
        stats:
          last_7_days:
            rejects: 7
            hard_bounces: 1
            unique_clicks: 6
            unsubs: 4
            complaints: 1
            opens: 5
            clicks: 9
            soft_bounces: 6
            sent: 1
            unique_opens: 9
          all_time:
            rejects: 9
            hard_bounces: 4
            unique_clicks: 4
            unsubs: 9
            complaints: 0
            opens: 0
            clicks: 5
            soft_bounces: 7
            sent: 1
            unique_opens: 7
          today:
            rejects: 7
            hard_bounces: 5
            unique_clicks: 1
            unsubs: 3
            complaints: 9
            opens: 2
            clicks: 7
            soft_bounces: 2
            sent: 5
            unique_opens: 4
          last_90_days:
            rejects: 7
            hard_bounces: 3
            unique_clicks: 4
            unsubs: 3
            complaints: 5
            opens: 4
            clicks: 6
            soft_bounces: 3
            sent: 7
            unique_opens: 0
          last_30_days:
            rejects: 3
            hard_bounces: 9
            unique_clicks: 5
            unsubs: 1
            complaints: 6
            opens: 2
            clicks: 6
            soft_bounces: 6
            sent: 8
            unique_opens: 6
          last_60_days:
            rejects: 7
            hard_bounces: 3
            unique_clicks: 8
            unsubs: 7
            complaints: 0
            opens: 6
            clicks: 4
            soft_bounces: 3
            sent: 6
            unique_opens: 0
        created_at: created_at
        reputation: 0
        username: username
        public_id: public_id
      properties:
        username:
          description: the username of the user (used for SMTP authentication)
          type: string
        created_at:
          description: the date and time that the user's Mandrill account was created
            as a UTC string in YYYY-MM-DD HH:MM:SS format
          type: string
        public_id:
          description: "a unique, permanent identifier for this user"
          type: string
        reputation:
          description: "the reputation of the user on a scale from 0 to 100, with\
            \ 75 generally being a \"good\" reputation"
          type: integer
        hourly_quota:
          description: the maximum number of emails Mandrill will deliver for this
            user each hour. Any emails beyond that will be accepted and queued for
            later delivery. Users with higher reputations will have higher hourly
            quotas
          type: integer
        backlog:
          description: the number of emails that are queued for delivery due to exceeding
            your monthly or hourly quotas
          type: integer
        stats:
          $ref: '#/components/schemas/postUsersInfo_200_response_stats'
      type: object
    postSubaccountsPause_200_response:
      description: the information for the paused subaccount
      example:
        custom_quota: 0
        name: name
        first_sent_at: 2000-01-23T04:56:07.000+00:00
        reputation: 60
        created_at: 2000-01-23T04:56:07.000+00:00
        sent_total: 5
        id: id
        sent_monthly: 5
        sent_weekly: 1
        status: active
      properties:
        id:
          description: a unique indentifier for the subaccount
          type: string
        name:
          description: an optional display name for the subaccount
          type: string
        custom_quota:
          description: "an optional manual hourly quota for the subaccount. If not\
            \ specified, the hourly quota will be managed based on reputation"
          type: integer
        status:
          description: the current sending status of the subaccount
          enum:
          - active
          - paused
          type: string
        reputation:
          description: the subaccount's current reputation on a scale from 0 to 100
          maximum: 100
          minimum: 0
          type: integer
        created_at:
          description: the date and time that the subaccount was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        first_sent_at:
          description: the date and time that the subaccount first sent as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        sent_weekly:
          description: "the number of emails the subaccount has sent so far this week\
            \ (weeks start on midnight Monday, UTC)"
          type: integer
        sent_monthly:
          description: "the number of emails the subaccount has sent so far this month\
            \ (months start on midnight of the 1st, UTC)"
          type: integer
        sent_total:
          description: the number of emails the subaccount has sent since it was created
          type: integer
      type: object
    postMessagesParse_200_response_attachments_inner:
      description: information about an individual attachment
      example:
        binary: true
        name: name
        type: type
        content: content
      properties:
        name:
          description: the file name of the attachment
          type: string
        type:
          description: the MIME type of the attachment
          type: string
        binary:
          description: "if this is set to true, the attachment is not pure-text, and\
            \ the content will be base64 encoded"
          type: boolean
        content:
          description: the content of the attachment as a base64 encoded string
          type: string
      type: object
    postMessagesContent_200_response_to:
      description: the message recipient's information
      example:
        name: name
        email: email
      properties:
        email:
          description: the email address of the recipient
          format: email
          type: string
        name:
          description: the alias of the recipient (if any)
          type: string
      type: object
    postWebhooksList_200_response_inner:
      description: the individual webhook info
      example:
        last_sent_at: 2000-01-23T04:56:07.000+00:00
        events_sent: 1
        description: description
        created_at: 2000-01-23T04:56:07.000+00:00
        id: 0
        auth_key: auth_key
        batches_sent: 6
        last_error: last_error
        url: url
        events:
        - send
        - send
      properties:
        id:
          description: a unique integer indentifier for the webhook
          type: integer
        url:
          description: The URL that the event data will be posted to
          type: string
        description:
          description: a description of the webhook
          type: string
        auth_key:
          description: the key used to requests for this webhook
          type: string
        events:
          description: The message events that will be posted to the hook
          items:
            description: the individual message event
            enum:
            - send
            - hard_bounce
            - soft_bounce
            - open
            - click
            - spam
            - unsub
            - reject
            type: string
          type: array
        created_at:
          description: the date and time that the webhook was created as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        last_sent_at:
          description: the date and time that the webhook last successfully received
            events as a UTC string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        batches_sent:
          description: the number of event batches that have ever been sent to this
            webhook
          type: integer
        events_sent:
          description: the total number of events that have ever been sent to this
            webhook
          type: integer
        last_error:
          description: "if we've ever gotten an error trying to post to this webhook,\
            \ the last error that we've seen"
          type: string
      type: object
    postTagsDelete_200_response:
      description: the tag that was deleted
      example:
        rejects: 5
        hard_bounces: 1
        unique_clicks: 4
        unsubs: 7
        complaints: 2
        reputation: 8
        opens: 9
        clicks: 3
        soft_bounces: 5
        tag: tag
        sent: 6
        unique_opens: 2
      properties:
        tag:
          description: the actual tag as a string
          type: string
        reputation:
          description: the tag's current reputation on a scale from 0 to 100.
          maximum: 100
          minimum: 0
          type: integer
        sent:
          description: the total number of messages sent with this tag
          type: integer
        hard_bounces:
          description: the total number of hard bounces by messages with this tag
          type: integer
        soft_bounces:
          description: the total number of soft bounces by messages with this tag
          type: integer
        rejects:
          description: the total number of rejected messages with this tag
          type: integer
        complaints:
          description: the total number of spam complaints received for messages with
            this tag
          type: integer
        unsubs:
          description: the total number of unsubscribe requests received for messages
            with this tag
          type: integer
        opens:
          description: the total number of times messages with this tag have been
            opened
          type: integer
        clicks:
          description: the total number of times tracked URLs in messages with this
            tag have been clicked
          type: integer
        unique_opens:
          description: the number of unique opens for emails sent with this tag
          type: integer
        unique_clicks:
          description: the number of unique clicks for emails sent with this tag
          type: integer
      type: object
    postIpsDelete_request:
      properties:
        key:
          description: a valid api key
          type: string
        ip:
          description: the dedicated ip to remove from your account
          type: string
      required:
      - ip
      - key
      type: object
    postSubaccountsUpdate_200_response:
      description: the information for the updated subaccount
      example:
        custom_quota: 0
        name: name
        first_sent_at: 2000-01-23T04:56:07.000+00:00
        reputation: 60
        created_at: 2000-01-23T04:56:07.000+00:00
        sent_total: 5
        id: id
        sent_monthly: 5
        sent_weekly: 1
        status: active
      properties:
        id:
          description: a unique indentifier for the subaccount
          type: string
        name:
          description: an optional display name for the subaccount
          type: string
        custom_quota:
          description: "an optional manual hourly quota for the subaccount. If not\
            \ specified, the hourly quota will be managed based on reputation"
          type: integer
        status:
          description: the current sending status of the subaccount
          enum:
          - active
          - paused
          type: string
        reputation:
          description: the subaccount's current reputation on a scale from 0 to 100
          maximum: 100
          minimum: 0
          type: integer
        created_at:
          description: the date and time that the subaccount was created as a UTC
            string in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        first_sent_at:
          description: the date and time that the subaccount first sent as a UTC string
            in YYYY-MM-DD HH:MM:SS format
          format: date-time
          type: string
        sent_weekly:
          description: "the number of emails the subaccount has sent so far this week\
            \ (weeks start on midnight Monday, UTC)"
          type: integer
        sent_monthly:
          description: "the number of emails the subaccount has sent so far this month\
            \ (months start on midnight of the 1st, UTC)"
          type: integer
        sent_total:
          description: the number of emails the subaccount has sent since it was created
          type: integer
      type: object
x-doc-structure:
  resources:
    allowlists:
      title: Allowlists
      description: "Add, list, or delete from your Rejection Allowlist."
      paths:
      - /allowlists/add
      - /allowlists/list
      - /allowlists/delete
    exports:
      title: Exports
      description: "Start an export, or get information on export jobs in progress."
      paths:
      - /exports/info
      - /exports/list
      - /exports/rejects
      - /exports/whitelist
      - /exports/allowlist
      - /exports/activity
    inbound:
      title: Inbound
      description: Manage your inbound domains and routes.
      paths:
      - /inbound/domains
      - /inbound/add-domain
      - /inbound/check-domain
      - /inbound/delete-domain
      - /inbound/routes
      - /inbound/add-route
      - /inbound/update-route
      - /inbound/delete-route
      - /inbound/send-raw
    ips:
      title: IPs
      description: "Request and manage Dedicated IPs for your account, and set up\
        \ reverse DNS."
      paths:
      - /ips/list
      - /ips/info
      - /ips/provision
      - /ips/start-warmup
      - /ips/cancel-warmup
      - /ips/set-pool
      - /ips/delete
      - /ips/list-pools
      - /ips/pool-info
      - /ips/create-pool
      - /ips/delete-pool
      - /ips/check-custom-dns
      - /ips/set-custom-dns
    messages:
      title: Messages
      description: "Send, schedule, or get information on your emails."
      paths:
      - /messages/send
      - /messages/send-template
      - /messages/search
      - /messages/search-time-series
      - /messages/info
      - /messages/content
      - /messages/parse
      - /messages/send-raw
      - /messages/list-scheduled
      - /messages/cancel-scheduled
      - /messages/reschedule
    metadata:
      title: Metadata
      description: Manage your custom metadata fields in your account.
      paths:
      - /metadata/list
      - /metadata/add
      - /metadata/update
      - /metadata/delete
    rejects:
      title: Rejects
      description: "Add, list, or delete from your Rejection Denylist."
      paths:
      - /rejects/add
      - /rejects/list
      - /rejects/delete
    senders:
      title: Senders
      description: Manage or get information about your sending domains.
      paths:
      - /senders/list
      - /senders/domains
      - /senders/add-domain
      - /senders/check-domain
      - /senders/verify-domain
      - /senders/info
      - /senders/time-series
    subaccounts:
      title: Subaccounts
      description: "Manage your subaccounts, including the abilitiy to pause and resume\
        \ sending."
      paths:
      - /subaccounts/list
      - /subaccounts/add
      - /subaccounts/info
      - /subaccounts/update
      - /subaccounts/delete
      - /subaccounts/pause
      - /subaccounts/resume
    tags:
      title: Tags
      description: "List, delete, or get information on your Tags and their sending\
        \ statistics."
      paths:
      - /tags/list
      - /tags/delete
      - /tags/info
      - /tags/time-series
      - /tags/all-time-series
    templates:
      title: Templates
      description: Manage the Templates in your account.
      paths:
      - /templates/add
      - /templates/info
      - /templates/update
      - /templates/publish
      - /templates/delete
      - /templates/list
      - /templates/time-series
      - /templates/render
    urls:
      title: URLs
      description: Manage your tracking domains. Some of these endpoints may be deprecated.
      paths:
      - /urls/list
      - /urls/search
      - /urls/time-series
      - /urls/tracking-domains
      - /urls/add-tracking-domain
      - /urls/check-tracking-domain
    users:
      title: Users
      description: "Get information about your account, or ping Transactional."
      paths:
      - /users/info
      - /users/ping
      - /users/ping2
      - /users/senders
    webhooks:
      title: Webhooks
      description: Manage the webhooks in your account.
      paths:
      - /webhooks/list
      - /webhooks/add
      - /webhooks/info
      - /webhooks/update
      - /webhooks/delete
    whitelists:
      title: Whitelists
      description: "Add, list, or delete from your Rejection Allowlist. These endpoints\
        \ are being replaced with a series of functionally identical endpoints called\
        \ /allowlists."
      paths:
      - /whitelists/add
      - /whitelists/list
      - /whitelists/delete
x-original-swagger-version: "2.0"

